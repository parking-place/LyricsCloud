# 1.0.14 Phase 5 최종 후보 인수

## 범위

- OFL·저작권·재배포·지원 문자/fallback·문제 해결 고지
- 24개 migration, production dependency와 Noto font 자산 license inventory, release 환경·manifest 봉인
- 1.0.12 개인 흐름과 1.0.14 font 회귀 통합, `OPS-NF-002` 공유 진입 재판정

최신 schema는 `1004_web_font_selection.sql`이다. 사전 provider gate는 미완료로 숨기지 않고 공유 진입은 owner/actor·초대/철회·RLS·복구 승인 주체 부재 때문에 NO-GO다. 1.1.0을 자동 시작하지 않는다.

## 완료 증거

P4 후보 `1753ee0`의 Actions 두 실행과 공개 인수를 재사용했다. P5 후보 `a11e1b5cb9b58cc195a0b2c9f2c148f868d74dee`는 Actions push `34658944912`의 verify(21분 47초), Chromium 340 PASS·36 skip, release browser matrix 10 PASS와 네 dev image 게시/서명을 통과했고 PR `34658954668`의 독립 verify도 PASS했다.

같은 후보를 개발 서버에 배포해 `1.0.14`·`dev`·`p5`, schema `1004_web_font_selection.sql`, 네 서비스 healthy, font `4,644,748` bytes·승인 SHA-256·1년 immutable 응답을 확인했다. 공개 desktop/mobile에서 계정 font 저장/재진입, 200줄 합성 composition 저장/재진입, editor DOM 유지, font 차단 fallback·추가 입력 exact 저장/reload와 overflow 부재가 PASS했고 fixture를 제거했다.

실제 Windows/Android/iOS의 1.0.14 폰트 전환과 물리 저사양 기기는 미실행이다. `OPS-100-001` backup 예외도 열린 상태로 정식 릴리스 판단에 명시한다.
