# 0.8.0 Phase 4 휴지통·탈퇴 검증 기록

## 범위와 결정

- `LC-080-P4-01`~`LC-080-P4-10`
- `DEC-08-A`: 삭제 시각부터 정확히 30일 뒤 자료 자동 완전 삭제
- `DEC-09-A`: 탈퇴 즉시 접근 차단, 7일 안 명시적 철회, 이후 계정·자료 완전 삭제
- Accepted `PROD-0010`: 곡과 같은 삭제 작업의 활성 가사만 함께 숨기고 같은 batch만 함께 복원
- Accepted `PROD-0011`: soft delete·복원 중 즐겨찾기·핀 보존

구현 계약은 [자료·계정 수명주기](../architecture/0.8.0-DATA-LIFECYCLE.md)에 기록했다.

## 로컬 검증

- `pnpm check`: 통과
- `AUTH_DATABASE_INTEGRATION=true pnpm test`: 54 files / 201 tests 통과
- `pnpm test:migration:0802`, `0800`, `0200`: 전진 2회, rollback, recovery 통과
- 수명주기 PC·모바일 E2E: 3 pass / desktop 전용 세션 계약의 모바일 1 expected skip
- 전체 `pnpm test:e2e`: 204 pass / 16 expected skip
- `pnpm build`: production build 통과
- web·collaboration·worker·migrate production image: fresh DB 0802 migration, 네 service readiness, worker 첫 purge 성공 기록, DB·collaboration 재시작과 durable recovery 통과
- build artifact secret scan: 통과
- 시각 증적: `evidence/0.8.0-phase4-trash-desktop.png`, `evidence/0.8.0-phase4-trash-mobile.png`, `evidence/0.8.0-phase4-withdrawal-confirm-desktop.png`, `evidence/0.8.0-phase4-withdrawal-pending-desktop.png`

## 확인한 경계

- 자료·owner 템플릿의 `purge_at`은 `deleted_at + 30 days`가 아니면 DB 제약이 거부한다.
- 곡 복원은 같은 `deletion_batch_id`의 가사만 되돌리고, 독립 삭제 가사는 부모 복원 또는 활성 곡 이동을 명시해야 한다.
- 다중 복원·완전 삭제는 모든 row를 잠근 뒤 한 transaction에서 처리하며 제목 확인이나 owner가 다르면 부분 적용 없이 실패한다.
- 직접 table delete 권한은 회수하고 현재 owner의 휴지통 행만 hard-delete 함수로 제거한다.
- 탈퇴는 10분 이내 생성된 현재 세션만 요청할 수 있고 즉시 모든 기존 세션과 일반 자료 API를 차단한다.
- 탈퇴 후 Google 재인증 세션은 철회 화면만 열 수 있고 `철회` 확인 뒤에만 일반 접근이 돌아온다.
- 30일·7일 경계 직전/정각/직후와 반복 purge를 고정 시계로 검증했고 다른 owner의 자료를 보존했다.
- 브라우저 계정 prefix local/session storage, IndexedDB와 개인 Cache Storage를 탈퇴 시 정리한다.
- live DB 완전 삭제와 인프라 백업 보존 가능성을 사용자 문구에서 구분한다.

## 원격·개발 검증

- 기능 SHA `77e0af8f373cc74dae106dfa694a439de0f2292d`를 `phase/0.8.0-p4-lifecycle`에 push하고 원격 branch와 일치함을 확인했다.
- GitHub Actions `34125931148`: verify와 web·collaboration·worker·migrate 발행 job이 모두 성공했다.
- Docker Hub의 `0.8.0`·전체 SHA·`Dev`·`Dev-latest`는 서비스별 같은 digest였다: web `sha256:a5357e038946a351ca7b76c1b4bfbee9f4c1476eea3f63988fe562d6952f9ee4`, collaboration `sha256:87db9bf213ea6dbe7a3018da54d0d87a2c69550975ff0e1acb20faef62a62cb8`, worker `sha256:43f66396cecc954e6ad06b6fb900d27ead48911872b83351015e551d8487bd91`, migrate `sha256:50e0fb761cfdce68127b6975fe347b776c8462f699bb645020e1a689826feea7`.
- 개발 서버의 tracked checkout·`BUILD_ID`·공개 live/ready가 기능 SHA와 일치했고 version `0.8.0`, schema `0802_lifecycle.sql`, PostgreSQL·web·collaboration·worker health를 확인했다. 배포 뒤 약 1.2GB build cache를 정리하고 실행 컨테이너와 volume을 보존했다.
- 공개 HTTPS 합성 계정으로 곡·소속 가사 휴지통 영향 요약, 교차 owner 격리, 원자 복원, 잘못된 제목 완전 삭제 거부와 정확한 제목 삭제를 확인했다.
- 탈퇴 요청의 즉시 기존 세션 차단과 `Clear-Site-Data`, pending 전용 화면, 재인증 합성 세션의 명시적 철회, worker 성공 run을 확인했다. 합성 계정·세션·자료는 cascade 정리했다.
- 완료 문서 commit은 같은 branch에서 CI·네 image 발행·동일 SHA 개발 재배포·공개 smoke를 반복해 최종 SHA로 기록한다.
- 릴리스 서버·`main`·`Release`·`latest`는 변경하지 않았다.
