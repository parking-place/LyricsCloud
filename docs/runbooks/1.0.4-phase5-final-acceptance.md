# 1.0.4 Phase 5 최종 후보 인수

## 후보 범위

- 기준: P4 최종 문서·개발 인수 SHA `3ed27706bba38edd58b3740fbf57cf0506d7772a`.
- 변경: 요구 추적·README·사용자/지원/보안/셀프호스팅·architecture/runbook 색인, P5 runtime metadata와 후보 검증기.
- migration/schema 변경 없음. 최신 schema는 `1000_prompt_modes.sql`이다.
- 1.0.4는 정식 main/tag/Release/릴리스 서버 대상이 아니라 1.0.5의 선행 개발 기준이다.

## P4 재사용 증거

- PostgreSQL 18 migration 2회, unit/integration 263 PASS와 기존 beta-only 4 skip.
- Chromium PC/mobile 전체 310건 중 279 PASS·환경 조건 31 skip.
- Chromium/Firefox/WebKit 5-project 새 기능 10 PASS, check와 production build PASS.
- 동일 SHA 개발 서버 네 서비스 healthy와 collaboration 재시작·재연결·CRLF/emoji raw 보존 PASS.

## 최종 게이트

최종 후보 `6c0aedca7d01270293d14e90dd6912cffe937cff`의 GitHub Actions push run `34476629460`에서 전체 verify와 web·collaboration·migrate·worker dev image 게시/서명을 통과했다. 같은 SHA를 개발 서버에 배포해 공개 live/ready의 `1.0.4`·`dev`·`p5`, schema `1000_prompt_modes.sql`, 네 서비스 healthy를 확인했다. 실제 collaboration 컨테이너 재시작 전후 같은 document reconnect와 CRLF·emoji 원문 일치가 PASS했고 합성 fixture를 제거했다.

최초 후보 run `34475963937`은 검증 성공 뒤 임시 DB를 강제 삭제하면서 닫히는 pool에 `57P01`이 발생해 실패했다. 제품 assertion은 완화하지 않고 `scripts/verify-0500-migration.mjs`가 연결 종료를 기다려 일반 drop을 재시도하도록 수정했으며 로컬 3회 연속과 위 최종 CI에서 통과했다. 신규 P0/P1, 원문 유실, 인증 우회, 교차 사용자 노출, 무음 저장 실패는 확인되지 않았다.
