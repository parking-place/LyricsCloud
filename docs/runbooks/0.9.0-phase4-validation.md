# 0.9.0 Phase 4 접근성·상태 검증 기록

## 범위

- `LC-090-P4-01`~`LC-090-P4-09`
- 15개 화면의 제목·landmark·레이블·키보드 포커스 순서
- 공통 빈 화면·로딩·오류·오프라인·권한 없음·삭제 상태 표현
- 모달·시트·확인의 focus trap, Escape 닫기와 호출 지점 복원
- 밝음·어두움 테마 대비, 색 외 상태 단서, 200% 확대와 reduced motion
- 저장 실패 재시도 시 본문·CodeMirror cursor 보존

## 구현 결과

- 15개 화면에 하나의 주 제목과 명명된 `main` landmark를 보장하고 편집 화면의 모바일 숨김 레이블을 보조 기술에는 유지했다.
- 밝음·어두움 공통 의미 색상과 빈 화면·위험 영역·프롬프트 토큰 대비를 보정했다.
- `StatePanel`을 도입해 빈 화면과 오류를 종류·제목·설명·다음 행동으로 구분했다.
- 비네이티브 dialog의 키보드 경계를 공통화해 최상위 modal의 Tab 순환, Escape, 처리 중 닫기 차단, 호출 지점 초점 복원을 일관되게 적용했다.
- 저장 실패 후 재시도 시 가사 본문뿐 아니라 편집 cursor 위치도 그대로 남는 회귀 검사를 추가했다.

## 로컬 수용 결과

- `pnpm check`: 통과
- production web build: 통과
- 57 files / 208 unit·DB tests: 통과
- `pnpm test:audit:0901`: 15화면·15상태·7개 제안·4 viewport, 60개 PNG 생성 통과
- `pnpm test:pwa:0903`: 통과
- Axe WCAG 2.1 A/AA serious·critical 검사: 15개 화면, 밝음·어두움, PC·모바일에서 오류 0
- dialog focus·상태 분리·200% 확대·reduced motion 집중 E2E: PC·모바일 11 통과·1 의도된 제외
- PC/mobile 전체 E2E: 240개 중 219 통과, PWA Chromium 단일 실행·모바일 하드웨어 키보드 등 의도된 21개 제외, 실패 0
- 편집 위치 복귀 부하 시나리오 추가 3회 반복: 3개 통과
- 네 production image와 일회성 fresh PostgreSQL volume: migration, web·collaboration·worker readiness, same-origin proxy, revision restore, DB·collaboration restart와 pending projection 복구 통과
- production 산출물 secret scan, shell 문법과 `git diff --check`: 통과

## 원격 CI·image·개발 배포

기능 SHA와 문서 최종 SHA 각각에 대해 원격 CI·네 Docker Hub image tag/digest·동일 SHA 개발 서버 배포·공개 smoke를 기록한 뒤 Phase를 종료한다.

## Phase 5 인계

- 자동 검사에서 중대 접근성 오류는 0이다.
- Phase 5는 실제 브라우저·기기에서 OS 한글 IME, 화면 읽기, touch/keyboard 복사, PWA 설치와 네트워크 전환을 최종 출시 행렬로 묶는다.
- build 중 Yjs 중복 import 경고는 기존 알려진 경고이며 자동·수동 동기화와 production recovery는 정상이다.
