# 1.1.7b P1 contract/runtime handoff

## Fixed inputs

- product baseline: annotated `v1.1.7a` / `fc2463cdb47d9fd7d0042779f602c6ddb7d734cf`
- completed 1.1.7a release record: `1db47751c55f45bea34551d603b9e4f67aabd9cc`
- b plan overlay: `458030ada5474a53223b33cfb4337fefbdbb1b71`
- selected source: remote head `c230c024aeb0297b1c130e3f3e8b507a43a7e871`, functional commit `0375825fe004fc74869250eefa14add267c4b3ae`
- implementation branch: `phase/1.1.7b-p1-contract-runtime`

No whole source cherry-pick is allowed. Native app/routes, migration 1150, 1.1.8 runtime metadata, Windows CI, source STATUS and visual snapshots are excluded.

## Contract result

Runtime/health/build metadata uses `1.1.7b`; plan paths use `3.Redesign-phase/1.1.7.b/Nphase.md`; all eleven private package manifests remain `1.1.7`. Development tags are full SHA, `dev-1.1.7b-pN`, `Dev`, and `Dev-latest`. Formal `v1.1.7b`, `1.1.7b`, `Release`, `latest`, and `Release-latest` require a separate annotated-tag run and authorization; this P1 manifest keeps `productionAuthorized=false`.

Current b artifacts cover 30 web migrations through `1151_profile_customization.sql`; no 1150 file was added. Existing 1150 database objects/history must be preserved without down/drop. The three environment matrix remains fresh web, populated a without 1150, and PC/development with 1150+1151.

## Local acceptance

- version/publication/status/strict-tag/environment/HMAC regression: 42 PASS
- `node scripts/validate-101-environment.mjs`: PASS for six service contracts
- `node scripts/validate-1002-release-artifacts.mjs`: development candidate PASS; `--require-release` rejects the false authorization as required
- Docker Node 24.20.0 / pnpm 11.25.0 `pnpm check`: PASS
- production web Docker build: PASS
- disposable PostgreSQL 18, migrations applied twice: PASS
- `AUTH_DATABASE_INTEGRATION=true pnpm test`: 393 PASS, 5 conditional beta skips

The first version regression attempt had six parser-test failures because the new branch parser consumed `-pN-topic` as part of the version. The parser was narrowed to stable numeric or exact `1.1.7a`/`1.1.7b`; the unchanged matrix then passed 42/42. Host Node 20 and its existing `node_modules` were not used as acceptance evidence.

## Remote and development evidence

Pending until the candidate commit is pushed:

- candidate SHA / PR:
- Actions run and verify/image jobs:
- four signed image digests/provenance:
- development checkout/deploy/public live+ready exact SHA:
- public `/auth`, anonymous protected endpoint, CSS/static smoke:
- rollback/cleanup result:

Do not mark P1 complete or start P2 until every pending line has actual evidence. This handoff authorizes neither `main`, a formal tag, release aliases, nor the release server.
