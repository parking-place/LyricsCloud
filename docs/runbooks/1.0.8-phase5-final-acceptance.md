# 1.0.8 Phase 5 최종 후보 인수

## 후보 범위

- 기준: P4 완료·개발 인수 SHA `2998382c2587f000656a18670b6af21698d4225d`.
- 변경: 요구 추적·README·사용자/지원/보안/셀프호스팅·architecture/runbook 색인, 1.0.8 환경·migration·license·release manifest, P5 runtime metadata와 검증기.
- 최신 schema는 `1002_library_manual_order.sql`이다.
- P5 후보 전체 CI와 동일 SHA 개발 인수 뒤 사용자가 승인한 정식 main/tag/image/릴리스 서버 절차로 이동한다.

## P4 재사용 증거

- PostgreSQL 18 migration 반복과 실제 DB unit/integration 298 PASS.
- Chromium desktop/mobile 316 PASS·환경 조건 36 skip.
- Chromium/Firefox/WebKit 5-project 신규 기능 30 PASS.
- Actions `34558503310` 전체 verify·네 dev image와 동일 SHA 개발 서버 재시작 뒤 곡 순서 지속성 PASS.

## 최종 게이트 결과

후보 `800c66593c8801fd55dad013448b1ef4b037d85a`는 GitHub Actions run `34560691621`의 verify를 19분 56초에 통과했고 web·collaboration·migrate·worker dev image 게시·서명도 모두 통과했다. 정확한 SHA를 개발 서버에 배포해 공개 live/ready의 `1.0.8`·`dev`·`p5`, schema `1002_library_manual_order.sql`과 네 서비스 health를 확인했다. 공개 desktop/mobile에서 곡을 이동하고 manual URL·새로고침 뒤 순서·320px overflow를 확인한 뒤 합성 계정과 곡을 제거했다.

중단 조건은 CI/image·SHA·schema·health 불일치, 저장 후 재진입 순서 손실, 인증 우회, 교차 owner 노출, 핀·숨은 곡·보기 설정 유실이다. 신규 P0/P1이 생기면 릴리스 서버를 변경하지 않는다.

최종 판정은 신규 제품 P0/P1·원문 유실·인증 우회·무음 저장 실패 0건이다. 실제 Windows/iOS/Android 물리 입력은 새로 실행하지 않으며 `OPS-100-001` 운영 예외는 미해결로 유지한다. 사용자의 현재 승인에 따라 정식 main·annotated tag·exact digest 릴리스 서버 절차로 이동한다.
