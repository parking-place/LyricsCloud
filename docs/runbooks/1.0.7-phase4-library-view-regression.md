# 1.0.7 P4 목록 보기 교차 회귀 인수

- 선행 P3 완료 SHA: `6f0a5561ce7d66f1b3d4236ca3f8740e8c40b5f2`
- 회귀·개발 인수 후보 SHA: `1c858ef8cdd64e2df1ce9f5fab56eef39103f115`
- 원격: `phase/1.0.7-p4-library-view-regression`, PR #52
- runtime: `1.0.7`, channel `dev`, phase `p4`

## 실제 DB·브라우저 결과

- PostgreSQL 18 migration 연속 2회 PASS.
- 실제 PostgreSQL unit/integration 283 PASS, beta-signup 전용 4건만 해당 runner에서 skip. 보기 owner/type isolation·CAS와 글꼴 설정 독립 갱신 3건 포함.
- production build·전체 workspace check PASS.
- Chromium/Firefox/WebKit desktop, Chromium/WebKit mobile의 1.0.7 신규 기능 30 PASS.
- Chromium desktop/mobile 전체 340건 실행에서 301 PASS, 기존 조건부 36 skip, 이전 “desktop 기본 2열” 가정을 가진 목록 3건이 새 기본 `list`와 충돌해 실패했다.
- 세 기존 목록 회귀가 desktop에서 `grid-medium`을 명시 선택한 뒤 2열·내용·기능을 검사하도록 수정했다. assertion을 완화하지 않았으며 수정된 세 목록과 1.0.7 신규 기능 desktop/mobile 26건이 PASS했다. 나머지 301건은 같은 production 변경에서 이미 PASS했고 후속 변경은 테스트의 보기 선택과 P4 metadata뿐이다.
- built artifact secret scan PASS, `Future_Feature.md` blob `8f42d20b638e245eb775415ccfdabef56a96f694` 유지.

## 수용 사례와 실패 복구

- 검색·정렬·추가 로드 15개 항목의 ID 순서와 URL을 네 보기에서 비교했다.
- 두 owner·세 자료 유형·동일 owner 새 context의 저장 범위를 비교했다.
- 320px·200%·긴 한글 제목에서 키보드 선택·액션 가시성·수평 overflow 부재를 검사했다.
- 보기와 글꼴 설정의 실제 PostgreSQL 동시 저장이 둘 다 보존됨을 확인했다.
- PUT 503/중단은 직전 보기와 오류 안내를 유지하고, stale PUT은 409 후 최신 서버 보기를 다시 읽었다.
- 인증 없는 GET/PUT은 401이며 private 응답은 no-store이다.

## 동일 SHA 개발·재시작 인수

후보 SHA를 개발 서버에 배포했다. 공개 build ID/phase와 checkout이 일치하고 schema `1001_library_view_settings.sql`, PostgreSQL/web/collaboration/worker가 healthy였다. 공개 Chromium desktop/mobile 합성 UI smoke가 저장·새로고침·query/order·유형 격리를 통과했다.

합성 owner가 `grid-large`를 저장한 뒤 web/collaboration/worker를 재시작하고 세 서비스 healthy 복귀, 같은 row version과 mode 조회를 확인했다. 합성 계정/session/자료는 종료 시 삭제했다. 릴리스 서버는 변경하지 않았다.

실제 OS IME/물리 기기는 이번 보기 설정 범위에서 새 입력 기능이 아니며 P3의 Chromium composition 및 직전 사용자 인수를 재사용했다. P5는 원격 전체 CI와 최종 개발 image/문서 인수를 수행한다.
