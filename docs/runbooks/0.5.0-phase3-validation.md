# 0.5.0 Phase 3 검증 기록

## 범위

- 작업 ID: `LC-050-P3-01`~`LC-050-P3-08`
- 브랜치: `phase/0.5.0-p3-prompt-editor`
- 화면: `/prompts/new`, `/prompts/:id`
- 데이터 변경: migration 추가 없음. `0500_prompts.sql`의 prompt sync document·token projection과 `0311_lyric_revisions.sql`의 범용 revision 저장소를 재사용

## 로컬 수용 — 2026-09-07

- `pnpm check`: 경계·TypeScript 검사 통과
- 전체 unit/integration: 35 files, 138 tests 통과
- 0200·0201·0300·0310·0311·0400·0500·0501 migration down/reapply/recovery: 통과
- production build와 standalone asset 준비: 통과
- Playwright PC·모바일 전체 152건: 150 통과, 환경상 의도된 2건 skip
- 신규 프롬프트 편집 E2E: PC·모바일 8건 통과
- 네 production image 복구와 fresh DB migration, web·collaboration·worker, same-origin proxy, DB/서비스 재시작·projection/revision 복구: 통과
- build artifact secret scan: 통과
- 개인 로컬 Compose의 PostgreSQL·web·collaboration·worker health와 schema `0501_prompt_usage.sql`: 통과

합성 fixture로 빈 신규·오프라인 작성/브라우저 종료/초안 복구·자동 생성, 201자 제목 차단과 명시적 초안 폐기, 목록에서 멱등 복제 후 독립 편집, 기존 프롬프트 초기화를 확인했다. 단일 직접 입력과 연속 쉼표를 포함한 쉼표 100개 입력, 앞뒤 공백·한글 IME composition, owner 사용 이력 자동완성의 키보드·touch 선택과 Escape/focus, 로딩·0건·503 실패 후 직접 입력을 검증했다.

NFKC·공백·대소문자 기준 중복은 색과 함께 정규화 키·1-based 위치 문구로 알리고, 개별 제거와 첫 표시 값·원래 순서를 보존하는 일괄 정리를 확인했다. 정리 직전 CRDT 중복 occurrence가 revision에 남고 비파괴 복원 뒤 다시 표시되며, 동일 owner 두 브라우저의 제목·토큰 동시 변경이 화면·Yjs·PostgreSQL 평문 문자열로 수렴한다. 390px에서 모든 토큰과 제거 버튼이 노출되고 문서 가로 overflow가 없음을 검사했다.

## 원격·배포 인수

구현 commit push 뒤 CI·Docker Hub tag/digest·동일 SHA 개발 배포와 공개 smoke 결과를 이 절에 추가한다. 이 항목이 끝나기 전 상태는 `review`다.

릴리스 서버, `main`, `Release`, `latest`는 변경하지 않는다.
