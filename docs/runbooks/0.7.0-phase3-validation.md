# 0.7.0 Phase 3 최근 작업·위치 복원 검증

## 검증 범위

- [PROD-0007](../product/PROD-0007-recent-work.md)의 수정/열람 구분과 복원 우선순위
- `0702_recent_items.sql`의 owner/resource/type FK, 위치 shape, 강제 RLS와 rollback/reapply
- active 곡·가사·라임·프롬프트 통합 최근 목록, 유형·소속 곡·상태·메모 존재 표시
- soft delete·삭제 부모·교차 owner 비노출과 content-free 응답
- 2초 debounce·15초 maximum wait·직렬화 위치 저장
- 같은 viewport cursor/scroll, 다른 viewport·본문 변경 시 송폼 우선, 범위 밖 cursor clamp

## 로컬 결과

- `pnpm check`: architecture boundary와 TypeScript 통과
- `pnpm test` (`AUTH_DATABASE_INTEGRATION=true`): 46 files, 175 tests 통과
- `pnpm test:migration:0200`~`0702`: 전 migration의 멱등 적용·rollback·reapply와 0702 owner/type 무결성, content-free 위치 shape, 강제 RLS 통과
- `pnpm test:e2e`: PC·모바일 180 통과, 프로젝트 중복 방지 8 의도적 skip
- `pnpm build`: `/recent`, recent API와 production standalone build 통과. 기존 Yjs 중복 import 경고 외 실패 없음
- `pnpm check:secrets apps/web/.next test-results`: build·브라우저 증적 비밀값 누출 0건
- 네 production image와 새 named-volume DB: migration, web·collaboration·worker·same-origin proxy readiness, revision·WebSocket 수렴, DB·collaboration 재시작 후 durable projection·ACK·복원 통과
- 종료 `keepalive` 위치 저장 응답이 이전 계정의 갱신 쿠키를 늦게 덮어쓰지 않게 하고 기존 계정 전환·IndexedDB 격리 회귀를 통과
- 모바일 카드 길게 누르기 뒤 합성 click이 생략되어도 다음 명시적 복제 버튼을 stale gesture 상태가 소비하지 않음을 Suno-copy 수직 흐름으로 통과

## 원격·배포 증적

- 기능 SHA: `21cb464dee5accee45c3bef4af8ec3246e4121d3`
- GitHub Actions: `34087267900` — `verify`와 web·collaboration·worker·migrate 게시 통과
- 개발 이미지 digest:
  - web: `sha256:b76b8ba99ef1a6ba75600d21f51b2ad814a4c4d36ac982a2f33dbb45dbe34155`
  - collaboration: `sha256:2e5c2b124225e7882bc2bb7b4540ec7116e6d0be0fedb0789519fb8dc2bcb18e`
  - worker: `sha256:4b5e6dcd7c18796f7a067afe5928fe613ff5db5fb04636b77ad2eaf8528948bd`
  - migrate: `sha256:26a4aabdd8953b12c029a05aeaa5b91b520bde6c63c5b2cce9a99f8e2a57e58a`
- 동일 SHA 개발 배포: checkout·환경 build SHA·live·ready가 모두 `21cb464dee5accee45c3bef4af8ec3246e4121d3`, version `0.7.0`, schema `0702_recent_items.sql`; PostgreSQL·web·collaboration·worker healthy
- 공개 smoke: live/ready exact SHA와 schema, owner 격리·일반화 404, mutation Origin 차단, 본문·메모 없는 최근 목록, 모바일 위치 저장, 인증된 `/recent` 렌더링 통과. 합성 계정·자료는 검증 뒤 제거

보안상 서버 접속 정보와 배포 실행 원문은 추적 제외된 로컬 증적에 둔다. 릴리스 서버·`main`·`Release`·`latest`는 이 Phase에서 변경하지 않는다.
