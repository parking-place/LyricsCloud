# 1.1.7 P2 생성·복귀 흐름 인수

## 범위

- 새 라임·새 프롬프트를 빠른 추가에서 열 때 현재 workspace 경로를 `returnTo`로 전달한다.
- 생성 성공 뒤 편집 화면과 목록 복귀 버튼이 같은 안전한 경로를 유지한다.
- `returnTo`는 같은 origin의 workspace 경로만 허용하며 외부 URL과 계정 경로는 각 자료 목록으로 되돌린다.
- 빠른 추가가 아닌 기존 직접 생성·템플릿 진입에는 새 query를 붙이지 않아 기존 URL·초안 복구 계약을 유지한다.
- API·DB schema·권한·본문 저장·copy 형식은 변경하지 않는다.

## 로컬 검증

기준 source는 main `ce28a63f71853b4254efd7dbdd42cddd62ad8571`, runtime은 `1.1.7/dev/p2`다.

| 검증 | 환경 | 결과 |
|---|---|---|
| 실패 우선 계약 | Node 24.20.0, Vitest | 구현 전 quick-add·new screen 계약 2 FAIL, 기존 안전 경로 1 PASS |
| 계약·단위·config | Node 24.20.0, Vitest | 3 files, 24 PASS |
| 정적 검사 | Node 24.20.0 | architecture boundary, lint, typecheck PASS |
| production build | Node 24.20.0, Next.js 16.3.4 | PASS |
| 신규 생성·복귀 | 격리 PostgreSQL, Chromium desktop·Chromium mobile·WebKit mobile | 3 PASS |
| 영향 회귀 | 같은 3 browser profile | 곡→가사→라임→프롬프트 수직 흐름, 생성·연결 테마, 신규 복귀 총 9 PASS |

첫 원격 후보 `5ae6f30`은 명시되지 않은 기본 복귀 경로까지 편집 URL query로 붙여 기존 생성·템플릿·오프라인 복구 14건을 실패시켰다. 명시적 `returnTo`가 있을 때만 query를 전달하도록 보정한 후보 `206b14f`에서 해당 14건과 신규 2건이 16 PASS했다.

Android/iOS 항목은 browser mobile 대리 환경이며 실제 Android/iOS 앱·물리 기기·OS IME·AT 검증이 아니다.

## 공개 개발 인수

후보 `206b14feb20c6c050662c2eda29c53bfba19e0bb`의 push/PR Actions `34881741352`·`34881745605`와 네 signed dev image가 PASS한 뒤 정확히 같은 SHA를 개발 서버에 배포했다.

개발 digest는 web `sha256:7a877c24d0de7235a2d9389b2d3c72d3600b7f3b62d4c11c7c0f2ccb24411df1`, collaboration `sha256:bc5745e2c6e5a782eaa68b9563b78201cf8030c8dcd4c02d4270bf7a4e60106d`, worker `sha256:d93cdf36c81c2941380ba0239984b936ec27639da7faaef8800c4d85f9943d27`, migrate `sha256:e8a583cf32b6740f56f1c08489762d9784a7f019f752d5e96e5c4f3936c61cef`이며 각 SHA·Phase·공용 개발 tag가 서비스별 동일 digest다.

아래 공개 개발 인수는 모두 PASS했다.

1. `/api/health/ready`의 version/channel/phase/build가 후보 `1.1.7/dev/p2/206b14f...`와 일치했다.
2. 공개 1440/390px×light/dark에서 가사 화면의 새 라임과 새 프롬프트를 생성하고 편집 화면을 거쳐 원래 중첩 가사 URL·exact 한글 본문으로 복귀했다.
3. desktop/mobile에서 focus 가능한 편집기와 가로 overflow 없음이 유지됐다.
4. schema `1140_sharing_stability.sql`, B-1 root와 PostgreSQL·web·collaboration·worker healthy를 확인했다.
5. 합성 fixture를 제거했고 DB volume·secret·allowlist를 보존했다.

## 중단·되돌림

- 외부 URL이 허용되거나 원래 가사 본문·권한 상태가 달라지면 배포를 중단한다.
- 개발 서버 rollback은 직전 exact dev image/SHA로 되돌리며 migration 추가가 없으므로 schema 역변경은 하지 않는다.
- 릴리스 서버는 P2에서 변경하지 않는다.
