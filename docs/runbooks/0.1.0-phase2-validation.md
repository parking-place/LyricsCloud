# 0.1.0 Phase 2 검증 기록

- 검증일: 2026-09-04
- 범위: `LC-010-P2-01`~`LC-010-P2-10`
- 환경: Node.js 24.20.0, pnpm 11.25.0, Next.js 16.3.4, PostgreSQL 18, `openid-client` 6.8.7

## 결과

| 검사 | 결과 |
|---|---|
| 정적 검사 | 9개 검사 대상 workspace와 architecture boundary 통과 |
| 단위·합성 OIDC | 격리 DB 포함 7 files, 28 tests: redirect·쿠키·state 변조·callback 재전송·code 거부·취소·미허용 계정·갱신·만료·로그아웃과 서명된 ID token의 issuer/audience/nonce 검증 통과 |
| 격리 테스트 DB | tmpfs `lyricscloud_test`에 migration 2회 실행, OAuth 거래 1회 소비와 `issuer + subject` 재로그인 사용자 ID 동일 확인 |
| 운영 build | 인증 API 4개를 포함한 Next.js standalone build 통과 |
| provider 정보 저장 | schema와 저장소에 access token, refresh token, ID token, authorization code 열이 없음을 검사 |
| scope | 합성 discovery/token endpoint에서 `openid email profile`과 PKCE `S256` 확인 |
| 원격 CI | [GitHub Actions run 33872573896](https://github.com/parking-place/LyricsCloud/actions/runs/33872573896) 전체 통과 |

## 계약

- `/api/auth/login`은 state, nonce, PKCE verifier를 만들고 거래 해시를 DB에, 암호화된 거래 자료를 10분 HttpOnly·SameSite=Lax 쿠키에 저장한다.
- `/api/auth/callback`은 거래를 원자적으로 한 번만 소비하고, 검증된 `issuer + sub`를 내부 UUID에 연결한 뒤 opaque 세션 원문의 SHA-256 해시만 저장한다.
- 세션은 30일 idle·90일 absolute 제한이며 만료 7일 전부터 idle 만료를 갱신한다. HTTPS 환경에서는 `__Host-` 이름과 Secure를 적용한다.
- `/api/auth/logout`은 같은 origin POST만 받고 서버 세션을 폐기하며 쿠키 삭제와 `Clear-Site-Data` 신호를 반환한다.
- 로그인 후 위치는 단일 `/`로 시작하는 내부 경로만 허용한다.

## 실제 Google 확인 인계

자동 검증은 로컬 합성 OIDC 공급자로 protocol과 실패 경계를 검사했다. 실제 Google Cloud OAuth client와 허용 계정은 저장소에 제공되지 않았으므로 실제 계정 브라우저 로그인은 실행하지 않았으며, 자격 증명을 `.env`에 설정한 뒤 Phase 5의 실계정 통합 시나리오에서 확인한다. 비밀 값은 커밋하거나 채팅에 붙여 넣지 않는다.
