# 0.3.1 Phase 3 검증

2026-09-05에 인증 collaboration WebSocket, owner 전용 opaque document mapping, 증분 update·receipt·snapshot 저장, 평문 본문 projector와 자동 재처리를 구현했다.

- `0310_crdt_sync.sql`을 빈 임시 PostgreSQL에 두 번 적용하고 forced RLS, 다른 owner 비노출, populated rollback 거부, 빈 rollback·재적용을 확인했다.
- `pnpm check`, 27개 파일 93개 unit/DB/protocol test, production build와 collaboration production image build가 통과했다.
- 실제 HTTP bootstrap과 WebSocket 두 연결에서 snapshot, durable ACK 뒤 broadcast, session 취소 후 4404 종료를 검증했다.
- 한글·송폼·emoji를 포함한 100개 update를 투영하고 임계값 압축 뒤 receipt 100개가 보존되며 raw update가 0개가 되는 것을 확인했다.
- 새 store instance가 snapshot+delta로 같은 Yjs 본문을 복구하고, 의도한 projection pending 상태를 재처리해 PostgreSQL 현재본과 일치시켰다.
- protocol 로그 자동 검사에서 owner/resource ID, 본문과 Base64 payload가 포함되지 않음을 확인했다.
- Chromium PC/mobile 기존 전체 E2E 76개가 통과했다.

원격 CI·Docker Hub 네 이미지 다중 태그와 개발 서버 exact-SHA 배포 결과는 Phase 완료 commit에 추가한다.
