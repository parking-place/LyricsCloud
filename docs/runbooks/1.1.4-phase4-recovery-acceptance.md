# 1.1.4 Phase 4 — 공유 복구·권한 회귀 인수

## 후보와 판정

- 후보: `233afd2e75e03c388a4bac71635545c637087c53`
- 판정: P4 PASS. owner 복원과 offline selected writer 재접속은 같은 write epoch에서 두 승인 원문을 보존하며, browser outbox는 64건/1 MiB 상한 안에서 lossless compact되고 ACK 뒤 비워진다.
- 삭제·권한 회수·계정 전환은 기존 capability/epoch와 owner-hash local store를 다시 사용하지 않는다.

## 자동·브라우저 증거

- Node 24 `pnpm check`와 production build: PASS.
- 실제 PostgreSQL 전체: 93 files, 364 PASS, 외부 beta fixture 4건 조건부 skip.
- 기본 Playwright 전체: 353 PASS, 환경 조건부 41 skip.
- 1.1.4 전용 5-browser: Chromium desktop 3 PASS, Firefox/WebKit desktop과 Chromium/WebKit mobile 각 2 PASS. 계정 전환 1건은 desktop 전용이어서 나머지 네 project에서 조건부 skip했다.
- Actions `34783619444`: 전체 verify와 web·collaboration·worker·migrate 개발 이미지 발행·서명 PASS.

## 동일 SHA 개발 인수

- 공개 live/ready: `1.1.4`·`dev`·`p4`, schema `1140_sharing_stability.sql`, 후보 SHA 일치, 네 서비스 healthy.
- 합성 owner/writer/reader 3계정과 desktop/mobile 두 viewport에서 presence, owner revision 복원 중 writer의 90회 초과 offline 편집, 재접속 병합, 반복 network 전환 3회, outbox 상한과 0건 배출을 확인했다.
- reader 회수 뒤 접근 차단, writer에서 다음 계정으로 전환 뒤 이전 IndexedDB·본문·권한·메모 비노출을 확인했다.
- web/collaboration/worker 실제 재시작 뒤 writer/read 지속성과 다음 계정 차단·본문/메모 격리를 다시 확인했다. 합성 계정·자료는 즉시 제거했다.

## 증거 등급과 잔여 위험

- S0/S1: 단위·실제 PostgreSQL 자동 회귀.
- S2: 공개 브라우저의 3계정·2 viewport·offline/reconnect 인수.
- S3: 실제 서비스 재시작 전후 인수.
- S4/S5 미실행: 실제 장시간 wall-clock soak, 물리 Windows/Linux/macOS/Android/iOS, 실제 OS IME와 기기 절전/복귀. 자동 브라우저 엔진·viewport·가속 반복은 이를 대체하지 않는다.

P5는 이 후보와 잔여 위험을 요구 추적·사용자/지원/보안/자가호스팅 문서, 환경·license·migration·release manifest에 봉인하고 최종 동일 SHA 개발 인수와 승인된 정식 릴리스 gate를 판정한다.
