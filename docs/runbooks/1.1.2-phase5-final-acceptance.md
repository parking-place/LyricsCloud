# 1.1.2 Phase 5 최종 후보 인수

## 범위

- selected write W⊆R·본문 전용 writer·actor/epoch ACK·server-auth presence/cursor·강등/회수·거부 원문 복구 계약의 최종 추적
- 27개 migration, production dependency와 bundled font license inventory, release 환경·manifest 봉인
- 전체 CI·네 dev image·동일 SHA 개발 인수와 승인된 정식 릴리스 gate

최신 schema는 `1120_selected_lyric_write.sql`이다. public-link write·guest identity는 제공하지 않으며 1.1.3~1.1.4의 후속 계약보다 먼저 소비하지 않는다.

## 완료 증거

P4 제품 후보 `b79722428c5836e794b74c38c63c404114e00b62`는 Actions push `34721549799`와 PR `34721572892`, 실제 PostgreSQL 권한/복구 통합, 5-browser selected-write 행렬, 동일 SHA 개발 서버의 3계정 live·offline rejected 복구·강등/회수·서비스 재시작을 통과했다.

P5 최종 제품 후보 `8ec3cf9acc48fa6fd47c61f6f4c1a485e600fbec`은 Actions push `34723513936`와 PR `34723521842`의 전체 검증을 통과했다. push 실행의 네 dev image 게시·서명, 27개 migration·환경·license·release manifest 봉인, Vitest 353 PASS·4 skip, 전체 E2E 348 PASS·38 skip, selected-read 5 PASS, public-link 11 PASS·4 skip, selected-write 5 PASS, release 10 PASS가 확인됐다.

같은 SHA를 개발 서버 `1.1.2/dev/p5`, schema `1120_selected_lyric_write.sql`로 배포해 네 서비스 healthy를 확인했다. 공개 smoke는 3계정 live 수렴, writer 강등 뒤 읽기 유지, reader/writer 회수, ACL·메모 격리, offline rejected 복구·재허용 비자동 적용과 실제 web/collaboration/worker 재시작 지속성을 통과했고 합성 fixture를 제거했다. 미해결 제품 P0/P1, W⊆R 위반, 권한 확대, 강등·회수 뒤 새 update ACK, 거부 원문 유실은 0건이므로 승인된 정식 릴리스 gate를 연다.

실제 OS/물리 기기는 P5에서 새로 실행하지 않는다. 자동화 결과를 실기기 증거로 기록하지 않으며 `OPS-100-001` backup 예외도 열린 상태로 정식 릴리스 판단에 명시한다.
