# 1.0.1 Phase 7 — 주 로고·아이콘·build metadata 인수 기록

## 선택과 구현 경계

기존 `lyricscloud-192-0903.svg`와 `lyricscloud-512-0903.svg`의 `L`과 음표 점 조합을 주 로고로 채택하고 원본은 수정하지 않았다. 외부 자산이나 생성형 이미지 없이 light/dark, 32px favicon, PWA maskable, 단색 SVG를 저장소에서 직접 파생했다. 선택 이유·용도·접근성 이름·사용권 경계는 [브랜드 기록](../brand/lyricscloud-logo.md)에 남겼다. 전면 morphism과 motion은 후속 UX 범위로 유지한다.

화면별로 하드코딩했던 `Release · 1.0.0`, 라임 노트 `0.4.0`, 프롬프트 `0.5.0` 표시는 제거했다. 로고 옆 표시는 runtime config의 `APP_VERSION`, `APP_CHANNEL`, `APP_PHASE`를 사용하고, health API도 같은 값을 반환한다. 정식 환경은 `v<version> Release`, 개발 Phase는 `v<version>-pN dev` 형식이다. 개발 배포 스크립트가 브랜치의 pN을 읽어 환경 파일에 원자적으로 반영한다.

## 로컬 수용 증거

- Node 24.20.0, pnpm 11.25.0 workspace TypeScript: PASS
- unit: 169 PASS, 선택적 DB integration 81 SKIP
- production web build: PASS
- P7 브랜드·metadata와 baseline: 3 PASS
- P6 UI 회귀와 P7 브랜드·metadata: 5 PASS. light/dark logo 선택, runtime/health label 일치, favicon/PWA/maskable/단색 asset, 양 테마 primary action, 접힌 rail, 200% 글자, 320/390px를 확인했다.
- 공개 양 테마 화면 직접 대조에서 로그인 카드의 기존 문자 `L` 잔존을 발견해 같은 승인 mark component로 교체했고, 집중 build·브라우저 2건을 재통과했다.
- `deploy-development.sh` shell syntax, hardcoded 중복 버전 검색, tracked diff whitespace: PASS

브라우저 검증은 격리된 `lyricscloud_test` PostgreSQL을 사용했고 시작 시 fixture가 초기화됐다. 제품 DB·Docker volume·기존 로고 원본·migration·저장 형식은 변경하지 않았다. P7 개발 서버 배포와 공개 HTTPS smoke는 후보 push 뒤 같은 SHA로 기록한다.

## 배포와 rollback

중간 Phase push는 `[skip ci]`이며 원격 필수 CI는 P10 통합 후보에서 수행한다. rollback은 이전 immutable SHA로 앱만 되돌리고 PostgreSQL·IndexedDB·Docker volume을 삭제하지 않는다. 구형 브라우저 캐시에 남은 아이콘은 manifest와 service worker 갱신 절차를 따르며 기존 2026-09-03 원본도 보존한다.
