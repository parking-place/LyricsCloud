# 0.9.0 Phase 5 출시 후보 검증 기록

## 범위

- `LC-090-P5-01`~`LC-090-P5-09`
- 지원 브라우저·OS·기기 하한과 실제 iOS Safari·Android Chrome 인수표
- 15개 화면의 PC·좁은 PC·태블릿·모바일 최종 시각 회귀
- Chromium·Firefox·WebKit의 화면·장문 한글·회전·뒤로 가기·Clipboard 거부 회귀
- PWA 설치·종료·재실행·오프라인 복구·업데이트·로그아웃 데이터 경계
- 실기기 결함 등급과 미해결 P0/P1 0 승인

세부 지원 행렬과 실기기 절차는 [출시 후보 인수표](./0.9.0-release-candidate.md), 화면 차이와 목업 제안 추적은 [UI 감사](../architecture/0.9.0-UI-AUDIT.md)에 기록한다.

## 자동·로컬 수용 결과

- `pnpm check`: 통과
- 57 files / 208 unit·DB tests: 통과
- production build: 통과
- `pnpm test:audit:0901`: 기존 15개 화면·상태·제안·viewport 계약 통과
- `pnpm test:release:0905`: 15개 화면×4 viewport의 60 PNG와 7개 추가 제안 완료 상태 통과, iOS·Android 실제 기기는 대기
- 전용 출시 후보 E2E: Chromium·Firefox·WebKit desktop과 Chromium·WebKit mobile에서 15개 화면, 모바일 뒤로 가기·회전, 약 10,000줄 한글 가사 10초 내 조작, 저장·Clipboard 거부·수동 복사 원문 검사를 10/10 통과
- PC/mobile 전체 E2E: 244개 중 219 통과, 전용 출시 후보 profile·PWA Chromium 단일 실행·모바일 하드웨어 키보드 등 의도된 25개 제외, 실패 0
- 전체 E2E 부하에서 발견한 테마 응답 경쟁 조건을 수정하고 초기 설정 GET을 의도적으로 지연한 회귀 검사와 전체 E2E를 재통과
- 네 production image와 일회성 fresh PostgreSQL volume: migration, web·collaboration·worker readiness, same-origin proxy, revision restore, DB·collaboration restart와 pending projection 복구 통과
- production 산출물 secret scan, shell 문법과 `git diff --check`: 통과

## 실제 기기 인수

- iOS Safari: 대기
- Android Chrome: 대기
- 두 실제 기기에서 설치·한글 IME·복사·앱 복귀·오프라인 종료/복구·업데이트·로그아웃 경계를 통과하기 전에는 이 Phase를 완료로 표시하지 않는다.

## 원격 CI·image·개발 배포

- 실제 기기 인수용 기능 SHA: 커밋 후 기록
- GitHub Actions verify와 web·collaboration·worker·migrate image 발행: push 후 기록
- Docker Hub `0.9.0`·전체 SHA·`Dev`·`Dev-latest` tag digest: 발행 후 기록
- 개발 서버 동일 SHA와 공개 HTTPS smoke: 배포 후 기록

## 0.9.1 인계

- 실제 기기에서 발견되는 P0/P1은 Phase 5 안에서 해결하며 P2/P3만 명시적으로 인계한다.
- production build의 기존 Yjs 중복 import 경고는 동기화·복구·10만 자/약 10,000줄 회귀가 통과한 관찰 위험으로 0.9.1 운영·성능 점검에 인계한다.
