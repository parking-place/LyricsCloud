# 0.9.1 Phase 5 백업·복원·업그레이드 RC 검증

- 상태: 완료 — 암호화 backup·독립 restore·upgrade/rollback·서명 artifact·동일 SHA 개발 배포와 공개 smoke 통과
- 기준: [`ADR-0008`](../adr/ADR-0008-backup-restore.md), [`OPS-0002`](../operations/OPS-0002-artifact-verification.md)
- 계약 검사: `pnpm test:release:0915`

## 작업 추적

| 작업 ID | 결과 | 증거 |
|---|---|---|
| `LC-091-P5-01` | 완료 | custom dump가 전체 DB schema·auth/session·resource·CRDT/revision/lifecycle 데이터를 포함하고 격리 restore count/fingerprint로 확인 |
| `LC-091-P5-02` | 완료 | `pg_dump -Fc` stdout을 평문 파일 없이 age recipient로 암호화하고 외부 bind storage marker를 강제 |
| `LC-091-P5-03` | 완료 | public recipient만 backup host에 두고 private identity는 image·Git·저장소와 분리, restore 때만 read-only 주입 |
| `LC-091-P5-04` | 완료 | 생성 시각·ciphertext 크기·SHA-256·24시간 age를 내용 없는 status/metric으로 기록 |
| `LC-091-P5-05` | 완료 | 성공 backup 뒤 30일 초과 archive/manifest만 멱등 정리하고 count 기록 |
| `LC-091-P5-06` | PASS | 별도 자격 증명의 빈 `_restore` DB에 복원, schema/count/검색/forced RLS/CRDT 관계와 fingerprint 통과 |
| `LC-091-P5-07` | PASS | exact digest 이전 RC → 현행 image migration/application 순서와 health 리허설 통과 |
| `LC-091-P5-08` | PASS | transaction migration fault와 신규 web readiness fault 후 이전 app digest rollback·roll-forward 통과 |
| `LC-091-P5-09` | 완료 | 신규 운영자용 설치·backup·restore·upgrade·rollback·장애 중단 기준 runbook 작성 |
| `LC-091-P5-10` | Accepted | Docker Hub, keyless cosign, BuildKit SLSA/SBOM, digest 승인자·secret 경계를 `OPS-0002`에 고정 |

## 로컬·CI 복구 증거

- backup image: PostgreSQL 18 client + digest 고정 Go 1.26.8로 빌드한 공식 age 1.3.2, non-root `postgres`, read-only rootfs 실행
- 예정 실행 2회와 30일 retention: PASS
- 정상/24시간 초과 RPO 판정: PASS
- read-only storage, 용량 부족, ciphertext 손상, 잘못된 identity: 모두 명확한 실패와 기존 정상 archive 보존 PASS
- 독립 restore: schema `0802_lifecycle.sql`, 4개 검색 index, forced RLS 위반 0, CRDT orphan 0, 복원 DB 대상 web·collaboration·인증 목록 product smoke PASS
- owner 격리·검색·CRDT snapshot/projection fingerprint: PASS
- encrypted archive의 가사·라임·프롬프트·revision 합성 평문 canary: 0건
- web·collaboration·worker·migrate·backup image의 수정 가능한 High/Critical 취약점: 0건, layer secret canary: 0건
- restore 측정값: 반복 실행 1.3~1.6초(300초 budget 이내)
- 이전 RC→현행 upgrade: 1.628초, migration fault rollback PASS
- 신규 image health fault→이전 RC application rollback: 2.556초(180초 budget 이내), data canary 보존·roll-forward PASS

최종 구현 SHA `266679f80f7243e047181767e0251acc0a71d53e`의 GitHub Actions `34214593249`에서 60 files/225 unit tests, PC·mobile E2E 223개, Chromium·Firefox·WebKit desktop과 Chromium·WebKit mobile RC 10개가 통과했다. 같은 run에서 예정 backup 2회, 24시간 RPO, 30일 retention, 저장소 read-only·용량 부족·손상·잘못된 키 주입, 격리 restore와 제품 smoke가 모두 통과했고 restore는 671ms였다. 이전 RC `0.9.0`에서 upgrade는 1,661ms, application rollback은 2,815ms였으며 migration/health fault, data canary 보존과 roll-forward도 통과했다.

## 서명 artifact와 개발 배포

- GitHub Actions `34214593249`: 전체 verify와 네 publish job PASS
- web: `sha256:471c79acfbed39f6ff0f5a9cf558c6a2aa7d9fef97877aea129bbae3cc4a5b81`
- collaboration: `sha256:de7b789a5f1de4dfc366231a036ce0ca5dfbddcee22f7fec10a06f977cbcc1b7`
- worker: `sha256:edb5d6c10d13e6cae8891f7c82372050b1077d1d1f93773ff6acf3241a061895`
- migrate: `sha256:248aff1748e525bb35e7fb54809074738edab4e9e659508b50484aa611105732`
- 네 digest 모두 GitHub Actions keyless certificate·transparency log와 BuildKit SLSA의 source·revision·Dockerfile·target을 검증했다.
- 개발 서버 checkout·환경 `BUILD_ID`·공개 live/ready는 동일 구현 SHA, `0.9.1`, schema `0802_lifecycle.sql`로 일치했고 postgres·web·collaboration·worker는 healthy였다.
- 공개 smoke는 보호 API의 `AUTH_REQUIRED`, 비공개 collaboration metrics의 `NOT_FOUND`, 오류 body/header request ID 일치와 `no-store` health를 확인했다.
- 배포 뒤 Docker 정리는 volume·실행 컨테이너·사용 중 image를 보존했다. release 환경과 `main`은 변경하지 않았다.

실제 사용자 자료와 운영 비밀 값은 fixture·출력·Git에 사용하지 않았다. 새 P0/P1 운영 결함은 0건이다. 완료 문서 commit도 동일 CI·서명 image·정확한 SHA 개발 배포·공개 smoke 절차로 다시 검증하며 최종 식별자는 Git 제외 운영 기록에 남긴다.
