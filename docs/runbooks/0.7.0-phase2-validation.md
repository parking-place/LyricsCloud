# 0.7.0 Phase 2 검색 화면·딥링크 검증

## 검증 범위

- PC·360px 모바일 통합 검색, 전체·곡·가사·라임 노트·프롬프트 URL 필터
- 300ms 입력 debounce, 이전 요청 취소와 sequence 기반 오래된 응답 무시
- 안전한 원문 강조, `<script>`·따옴표·송폼 괄호의 문자 표시
- 곡·가사·라임·프롬프트 `returnTo`, 가사 본문 첫 일치 selection·스크롤·포커스
- 뒤로 가기의 검색어·필터·스크롤 복원과 `ArrowUp`·`ArrowDown`·`Enter`·`Escape`
- 삭제·교차 owner·잘못된 ID의 공통 비노출 화면
- owner별 최근 검색어 최대 8개, 재실행·개별 삭제·전체 삭제와 다른 기기 동기화
- 0701 강제 RLS, NFKC 중복 갱신, rollback/reapply

## 로컬 결과

- `pnpm check`: architecture boundary와 TypeScript 통과
- 검색 domain·database·URL targeted test: 3 files, 13 tests 통과. owner별 동시 16건 기록에서도 최신 8개 제한 유지
- `pnpm test:migration:0701`: NFKC, 강제 RLS, 교차 owner 차단, rollback/reapply 통과
- `tests/e2e/unified-search.spec.ts`: desktop·mobile 4 통과, 단일 프로젝트 API/timing 중복 2 의도적 skip
- `pnpm build`: production build 통과. 기존 Yjs 중복 import 경고 외 실패 없음
- 전체 회귀: 43 files, 165 tests 및 PC·mobile E2E 177 통과·7 의도적 skip
- 네 production image: fresh disposable DB migration, DB·service restart 복구, web·collaboration·worker readiness와 same-origin proxy 통과

전체 회귀, production image, 원격 CI·네 image 게시, 동일 SHA 개발 배포와 공개 smoke 결과는 Phase 완료 시 아래에 기록한다.

## 원격·배포 증적

- 기능 SHA: 완료 시 기록
- GitHub Actions: 완료 시 기록
- 개발 이미지 digest: 완료 시 기록
- 동일 SHA 개발 배포와 공개 smoke: 완료 시 기록

보안상 서버 접속 정보와 배포 실행 원문은 추적 제외된 로컬 증적에 둔다. 릴리스 서버·`main`·`Release`·`latest`는 이 Phase에서 변경하지 않는다.
