# 1.0.3 Phase 4 프롬프트 모드 교차 인수

## 수용 결과

- 저장된 태그형·문장형 표현을 보기로 오갈 때 입력이나 변환 없이 기존 원문·복사 결과가 바뀌지 않는다.
- 두 탭 중 한쪽에서 문장이 변경되면 다른 탭의 오래된 변환 preview는 원자적으로 거부되며 최신 preview를 다시 확인해야 한다.
- 문장형 offline 편집·재접속·복제·revision 복원에서 구두점·연속 공백·줄바꿈·mode가 raw 그대로 유지된다.
- capability 없는 구버전 collaboration 연결은 `409 PROMPT_MODE_CAPABILITY_REQUIRED`, capability를 선언한 클라이언트는 연결·재연결된다.
- 다른 account의 API·collaboration 접근은 모두 404로 유지된다.

## 자동·실제 환경 증거

- Node 24.20.0, pnpm 11.25.0, PostgreSQL 18: unit/integration 261건, typecheck, production build, migration `1000_prompt_modes.sql` fresh/repeat PASS.
- Chromium PC·mobile 새 기능 8건 PASS. 전체 suite는 306건 중 274 PASS, 환경 조건상 31 skip이었다.
- 전체 suite가 찾은 prompt 비활성 상태의 dark-theme 대비 한 건은 opacity를 제거하고 dashed border로 상태를 구분한 뒤 15화면·양 테마 접근성 감사를 재실행해 PASS했다.
- Firefox desktop 4건, WebKit mobile 4건 PASS. 실제 Windows/iOS/Android 물리 입력은 이번 Phase에서 새로 수행하지 않았으며 자동 브라우저 결과와 혼동하지 않는다.

## 개발 배포·재시작 인수

- 구현·검증 SHA: `611eab02ccd3ba7279a1627e93d92436876657d7`, 원격 Phase branch와 PR #32가 같은 SHA다.
- 개발 metadata: version `1.0.3`, channel `dev`, phase `p4`; schema `1000_prompt_modes.sql`.
- PostgreSQL·web·collaboration·worker healthy, 공개 HTTPS live·ready·auth·정적 경로 PASS.
- 실제 개발 collaboration container를 재시작하고 healthy 복귀 뒤 같은 document key로 재연결했다. 문장 raw 조회와 복제 결과가 byte-equivalent임을 확인했다.
- 합성 사용자와 prompt를 cascade 제거하고 기존 사용자 자료·volume을 보존했다.

## 경계와 인계

- 중간 commit은 `[skip ci]` 정책을 적용했다. 필수 GitHub Actions 전체 CI와 dev image 발행은 P5 최종 후보에서 실행한다.
- `OPS-100-001` 외부 backup 미구축 예외는 사용자의 현재 위험 승인대로 유지한다.
- 자동 요약·AI 변환·Suno API는 범위 밖이며 1.0.3 main·정식 image 별칭·릴리스 서버는 변경하지 않았다.
- P5는 요구 추적, 사용자/지원/보안/자가호스팅 문서, 1.0.3 환경 schema와 최종 후보 인수를 수행한다.
