# 1.0.0 Phase 2 불변 release artifact 검증

- 상태: 완료 — 재현 build·전체 CI·네 signed digest·동일 SHA 개발 배포와 공개 smoke 통과
- 기준: [`OPS-0002`](../operations/OPS-0002-artifact-verification.md), [`OPS-0004`](../operations/OPS-0004-final-release-gate.md)
- 계약 검사: `pnpm test:release:1002`

## 봉인 입력

| 입력 | 계약 |
|---|---|
| 제품·workspace package | `VERSION`과 11개 package manifest, runtime·Compose 기본값이 모두 `1.0.0` |
| dependency | `pnpm-lock.yaml` SHA-256을 release manifest와 license inventory에 고정 |
| migration | [`migrations.1.0.0.json`](../../config/migrations.1.0.0.json)에 17개 파일의 정렬 순서·SHA-256·호환 범위·rollback 제약 고정 |
| 환경 | [`environment-schema.1.0.0.json`](../../config/environment-schema.1.0.0.json)에 서비스별 필수 값·형식·secret/file 경계 고정 |
| license | [`licenses.1.0.0.json`](../../config/licenses.1.0.0.json)에 production dependency 65개의 SPDX 분류와 재배포 주의사항 고정 |
| image | [`release-manifest.1.0.0.json`](../../config/release-manifest.1.0.0.json)에 네 repository·Dockerfile target·nonroot UID·health·attestation·digest 자리 고정 |

tracked release manifest는 source SHA와 digest를 변수로 둔다. image digest는 그 source를 빌드한 뒤에만 결정되므로 digest 값을 source commit에 다시 commit하는 순환을 만들지 않는다. CI가 같은 template에 commit·ref·build 시각·네 검증 digest를 주입해 write-once 승인 목록을 만들며, [`generate-1002-release-manifest.mjs`](../../scripts/generate-1002-release-manifest.mjs)는 기존 파일 덮어쓰기를 거부한다. 기술 gate 통과는 production 배포 승인이 아니며 `OPS-0004`의 별도 명시적 사용자 승인이 계속 필요하다.

## 검증 항목

| 작업 | 현재 결과 | 검증 |
|---|---|---|
| `LC-100-P2-01` | PASS | 11개 package와 runtime/UI/Compose를 `1.0.0`으로 일치 |
| `LC-100-P2-02` | PASS | 고정 Node·pnpm·lockfile, pinned runtime base에서 독립 build 비교와 최종 SHA CI rebuild |
| `LC-100-P2-03` | PASS | CI keyless signature·SLSA provenance와 write-once manifest의 네 digest 승인 목록 |
| `LC-100-P2-04` | PASS | BuildKit SPDX SBOM, pnpm audit, Gitleaks, Trivy, layer canary와 license inventory |
| `LC-100-P2-05` | PASS | 17 migration 순서·checksum, 0.9.0~1.0.0 호환, down migration 금지 |
| `LC-100-P2-06` | PASS | 서비스별 environment schema의 정상·누락·형식 오류와 값 비노출 검사 |
| `LC-100-P2-07` | PASS | UID 65532, read-only rootfs, health/readiness, SIGINT/SIGTERM, DB/backup 경계 |
| `LC-100-P2-08` | PASS | 두 clean build의 runtime config·정규화 filesystem 일치; Next build ID는 결정적으로 고정하고 매 build 회전 암호 키, pnpm `prunedAt`, OCI 시각만 명시적으로 정규화 |
| `LC-100-P2-09` | PASS | 네 digest만 포함하고 `productionAuthorized: false`를 유지하는 mode `600` 승인 목록 생성 |

## 실행 명령

```bash
pnpm test:release:1002
bash scripts/verify-1002-reproducible-images.sh "$(git rev-parse HEAD)"
bash scripts/verify-production-images.sh "$(git rev-parse HEAD)"
bash scripts/verify-0915-upgrade-rollback.sh "$(git rev-parse HEAD)"
```

CI는 dependency audit, Git history secret scan, 모든 migration, unit/build/E2E, fresh DB image smoke, 이전 RC upgrade·rollback, fixable High/Critical container scan, image layer secret canary, BuildKit SBOM/SLSA와 keyless signature를 같은 source SHA에서 수행한다.

## 검증 결과

- 독립 clean build 2회의 runtime config·정규화 filesystem content가 서비스별 일치했다. worker·migrate는 image ID도 일치했고 web·collaboration은 Next preview/server-action 암호 키, pnpm `prunedAt`, OCI 시각만 달랐다. 이후 recovery/Playwright fixture 보완은 production runtime 입력을 바꾸지 않았고 최종 SHA를 CI에서 다시 깨끗하게 build했다.
- GitHub Actions `34228604151`은 60 files/226 unit tests, PC/mobile E2E 223 pass·27 skip, 5-browser RC 10 pass, production build와 fresh DB image smoke를 통과했다.
- 암호화 backup 2회, retention·storage/capacity/corruption/wrong-key 차단, 독립 restore 684ms, owner/search/CRDT 제품 smoke와 plaintext canary 0건을 확인했다.
- 0.9.0 upgrade 1,642ms, migration fault rollback, health fault, application rollback 2,758ms, data canary 보존과 roll-forward가 통과했다.
- dependency/container/secret scan에 차단 결과가 없고 네 image의 SBOM·SLSA provenance·keyless signature가 정확한 digest에서 검증됐다.
- 사용자 실제 기기 인수 `iOS update PASS`, `Android update PASS`를 release artifact 인계에 포함했다.

## 원격·개발 인수

- 구현 SHA `976e2502e039c2684579092ad91fc0bb7fd9a9ee`, GitHub Actions `34228604151`: verify와 네 publish job PASS
- 서명 digest: web `sha256:909e408f727978f635a8b8cd7268bc454ba8b2cd45a525c876f637c4f4fb5a5f`, collaboration `sha256:3fb0b1da282575f2812d47a6fcf265865028eccb6847d2083df077fe42690f28`, worker `sha256:d2af665cda525d0595a6cd0c0e5b27bf4c7e9e5ac5b02bd687b6805828dde69e`, migrate `sha256:5083191e3d052e8c3e04c3cb6ea8c9fb57ec33dd6fa687a66c9146ccc0ff4ad4`
- write-once 승인 목록은 Git 제외 mode `600` manifest에 commit·ref·CI·build 시각·네 digest를 고정했고 `productionAuthorized: false`를 유지한다.
- 개발 checkout·`BUILD_ID`·공개 live/ready는 같은 SHA, version `1.0.0`, schema `0802_lifecycle.sql`로 일치하며 네 장기 서비스가 healthy다.
- 공개 smoke는 `no-store` health, 보호 route `AUTH_REQUIRED`, 공개 collaboration metrics `NOT_FOUND`와 내용 없는 오류 경계를 통과했다.
- 배포 후 Docker 정리는 volume·실행 컨테이너·사용 중 image를 보존했다.
- release server, `main`, 정식 tag, `Release`/`latest` tag와 GitHub Release는 별도 명시적 승인 전까지 변경하지 않았다.

완료 문서 commit은 동일 CI·서명 image·정확한 SHA 개발 배포·공개 smoke 절차로 다시 검증한다.
