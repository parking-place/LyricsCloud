# 1.0.7 P2 목록 보기 저장소·API 인수

- 선행 계약 SHA: `ff25d13e2203aa40aecec0566287e10bb74df82e`
- 구현·개발 인수 후보 SHA: `b37f3101551fc2147698aa064f7bd3bd831bbf64`
- 원격: `phase/1.0.7-p2-library-view-store`, PR #50
- runtime: `1.0.7`, channel `dev`, phase `p2`

## 실패 우선·로컬 검증

`tests/new-feature/1.0.7.contract.test.ts`는 runner에 처음 포함했을 때 미구현 export로 3건 실패했다. 구현 후 닫힌 자료 유형/mode·read-only 기본값·item/query identity 불변 3건과 API 오류/캐시 2건이 PASS했다.

- Node 24.20.0 / pnpm 11.25.0 전체 unit: 200 PASS, 실제 DB 조건부 87 skip.
- boundaries·전체 workspace typecheck·production web build PASS.
- PostgreSQL 18 `library-view-settings` + 기존 display settings integration: 5 PASS.
- `1001_library_view_settings.sql`: 빈 설치·반복·check constraint·forced RLS·owner 차단·rollback 후 재적용 PASS.
- 첫 전체 unit에서 기존 최신 schema 기대가 `1000_prompt_modes.sql`로 고정된 1건이 실패해 실제 새 health 기준 `1001_library_view_settings.sql`로 갱신하고 전체를 재통과했다.
- 첫 DB 명령은 임시 Node 컨테이너의 pnpm shim 부재로 migration 전에 중단됐다. shim을 활성화한 깨끗한 재실행만 PASS로 기록했다.

## 구현 경계

`library_view_settings`는 자료와 `user_settings`를 건드리지 않는 `(owner_id,resource_type)` 선호 행이다. GET 기본값은 행을 만들지 않으며 PUT은 닫힌 body와 행별 CAS만 허용한다. RLS owner는 server auth context에서 주입한다. API 응답은 private/no-store이고 validation 400, origin 403, stale 409를 유지한다. 기존 client와 자료 schema는 변경하지 않는다.

## 동일 SHA 개발 인수

후보 SHA를 `/srv/lyricscloud-dev`에 배포하고 forward migration 후 PostgreSQL/web/collaboration/worker가 healthy임을 확인했다. 공개 live/ready는 build `1.0.7`/후보 SHA/`dev`/`p2`, schema `1001_library_view_settings.sql`과 일치했다.

공개 HTTPS의 합성 두 owner로 다음을 확인했다.

- GET 기본 `list`, version 0, `updatedAt: null` 및 DB row 미생성
- owner A/B와 songs/rhymes 격리
- 글꼴 PUT과 songs mode PUT 동시 성공 및 두 값 보존
- stale 동일 유형 PUT 409 `VERSION_CONFLICT`
- private/no-store 응답
- 합성 계정과 session 삭제

중간 commit은 정책에 따라 `[skip ci]`이며 원격 CI를 PASS로 기록하지 않는다. 목록 UI·지원 browser·320px/200%·실제 기기는 P3/P4에 남는다. 릴리스 서버와 정식 image alias는 변경하지 않았다.
