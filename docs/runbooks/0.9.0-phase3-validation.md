# 0.9.0 Phase 3 PWA 검증 기록

## 범위

- `LC-090-P3-01`~`LC-090-P3-08`
- install manifest·192/512 아이콘·명시적 설치 진입
- immutable Next 정적 자산 전용 service worker cache
- 인증 HTML·API·창작물 non-cache와 private/no-store
- 기존 owner IndexedDB schema 보존 upgrade, offline 재연결과 중복 방지
- dirty 초안이 있는 waiting update 차단과 사용자 승인 activation
- 로그아웃·계정 전환·탈퇴의 로컬 개인 데이터 제거

세부 불변식은 [PWA 설치·캐시·업데이트 계약](../architecture/0.9.0-PWA-CONTRACT.md)에 기록한다.

## 로컬 수용 결과

- `pnpm check`: 통과
- 57 files / 208 unit·DB tests: 통과
- production build와 `/manifest.webmanifest` 정적 생성: 통과
- `pnpm test:pwa:0903`: standalone worker 일치, manifest·아이콘, hashed static allowlist와 private route 선언 통과
- Chromium PWA 집중 E2E: legacy IndexedDB upgrade·queue 보존, 실제 worker cache, API/창작물 비저장, offline cached asset, update guard·승인 reload, offline 편집 종료/복구, logout local data 제거 통과
- PC/mobile 전체 E2E: 234개 중 213 통과, PWA Chromium 단일 실행·모바일 하드웨어 키보드 등 의도된 21개 제외, 실패 0
- 병렬 부하에서 드러난 기존 선택·화면 전환·검색 focus 경합은 실제 대기 조건과 입력 focus 초기화를 보강한 뒤 관련 PC/mobile 시나리오 3회 반복 15개 통과·3개 의도된 제외
- 네 production image와 일회성 fresh PostgreSQL volume: migration, web·collaboration·worker readiness, same-origin proxy, revision restore, DB·collaboration restart와 pending projection 복구 통과
- production 산출물 secret scan, shell 문법과 `git diff --check`: 통과. 로컬 `actionlint` 실행 파일은 없어 원격 CI에서 확인한다.

## 원격 CI·image·개발 배포

- 기능 SHA `10312edb2117c6063adb466aad0d6d859873dd01` push와 원격 branch SHA 일치 확인
- GitHub Actions CI `34153262398`: verify와 web·collaboration·worker·migrate image 발행 모두 통과
- Docker Hub `0.9.0`·전체 SHA·`Dev`·`Dev-latest` tag digest 일치:
  - web `sha256:76d838c134dc7a89adad84f189482036b65c8de14288eeb21e87547384ee270b`
  - collaboration `sha256:448b8672d6e998fa54347fb1d8d03065f6fc50ae98afa47b41060e62711af562`
  - worker `sha256:788431501c3ac38623114ed871741090b63b9e86d0e7075f11a8b699bc632024`
  - migrate `sha256:de6afcab63b370db433355355511797fe5b9e0abfe6b32a702450af9a76d2c6b`
- 개발 서버: tracked clean, checkout·`BUILD_ID`·live·ready가 모두 기능 SHA와 일치, `0.9.0`, `0802_lifecycle.sql`, PostgreSQL·web·collaboration·worker healthy
- 공개 HTTPS smoke: live·ready SHA, manifest, 192/512 SVG, static-only worker 본문, 보호 route·API `private, no-store` 통과
- 공개 Chromium smoke: service worker control, 해시 정적 자산 10개만 Cache Storage에 저장, offline cache hit 통과

문서 최종 SHA도 동일한 CI·image·개발 배포·공개 smoke를 다시 통과한 뒤 Phase 3을 종료한다.
