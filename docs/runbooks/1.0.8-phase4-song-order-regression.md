# 1.0.8 P4 곡 사용자정렬 교차 회귀 인수

- 선행 P3 완료 SHA: `fd8b44bec6d84a8e9a3d8114c830826aa44f9510`
- 회귀·개발 인수 후보 SHA: `72af9f9096d7c171c8d346b889b5cda7ca256a02`
- 원격: `phase/1.0.8-p4-song-order-regression`, PR #57
- runtime: `1.0.8`, channel `dev`, phase `p4`

## 실제 DB·브라우저 결과

- PostgreSQL 18 migration과 실제 DB unit/integration 298건 전체 PASS. 곡 순서 store 5건은 visible anchor·숨은 항목 보존·동시 버전 직렬화·멱등 재전송·다른 owner/cross-pin 거부·cursor 무효화·삭제/복원·bounded rank 재분배를 포함한다.
- 전체 workspace check PASS.
- Chromium desktop/mobile 전체 352건에서 316 PASS·환경 조건부 36 skip. 기존 저장·가입·가사/라임/프롬프트·IME 자동화·offline·두 탭 회귀를 포함한다.
- Chromium/Firefox/WebKit desktop과 Chromium/WebKit mobile의 1.0.8 기능 30건 PASS.
- 같은 5-project 공통 릴리스 브라우저 행렬 10건 PASS.
- 실제 OS/물리 기기는 새로 실행하지 않았으며 자동 엔진/viewport와 기존 사용자 인수로 구분한다.

## 수용 사례와 실패 복구

- 최신순에서 이동하면 manual URL과 서버 순서로 전환되고 새로고침·다른 context에서 복원된다.
- 검색으로 숨은 곡과 pin group을 유지한 채 보이는 anchor 사이로만 이동하며 검색·한글 composition을 보존한다.
- 같은 expected version의 두 이동은 DB lock 아래 하나만 성공하고 다른 하나는 409로 수렴한다. 같은 request ID 응답 유실 재전송은 ledger 결과를 replay하며 중복 이동하지 않는다.
- 다른 owner의 anchor와 cross-pin anchor는 거부되고 두 owner의 rank를 바꾸지 않는다. 인증 없는 이동은 401이다.
- 503/중단은 optimistic 목록을 원복하고 같은 이동 재시도를 제공하며, stale 409는 서버 승인 순서를 다시 읽는다.

## 원격 전체 게이트

GitHub Actions run `34558503310`은 정확한 후보 SHA에서 verify 20분 13초, 네 dev image 게시·서명을 모두 통과했다. 일반 unit/DB 294 PASS·4 조건부 skip과 별도 beta DB 4 PASS, Chromium 316 PASS·36 skip, release browser matrix 10 PASS였다. migration 반복·fresh/upgrade·production image·암호화 복원·rollback·취약점/secret scan도 PASS했다.

## 동일 SHA 개발·재시작 인수

후보 SHA를 개발 서버에 배포했다. checkout/build metadata는 일치했고 schema `1002_library_manual_order.sql`, PostgreSQL/web/collaboration/worker가 healthy였다. 공개 Chromium desktop/mobile 이동·새로고침·manual URL·320px overflow 부재가 PASS했다.

합성 owner가 `B → C → A`를 저장한 뒤 web/collaboration/worker를 실제 재시작했다. 첫 readiness 감시는 3분 제한을 조금 넘겨 timeout됐으나 직후 점검에서 네 서비스 healthy와 정확한 SHA/schema를 확인했고, 공개 API가 재시작 전과 같은 `B → C → A`를 반환했다. 데이터 손실은 없었으며 이 지연은 P5 운영 인수에 사실대로 전달한다. 합성 계정/session/자료는 종료 시 삭제했다. 릴리스 서버는 변경하지 않았다.
