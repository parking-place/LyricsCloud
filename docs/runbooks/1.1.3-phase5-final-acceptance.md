# 1.1.3 Phase 5 최종 후보 인수

## 범위

- public write W⊆R·owner 위험 확인·본문 전용 guest·session/epoch ACK·지속 제한·owner 중지·거부 원문 복구 계약의 최종 추적
- 28개 migration, production dependency와 bundled font license inventory, release 환경·manifest 봉인
- 전체 CI·네 dev image·동일 SHA 개발 인수와 승인된 정식 릴리스 gate

최신 schema는 `1130_public_lyric_guest_write.sql`이다. 장시간 절전·복원 중 offline writer와 계정 전환 격리는 1.1.4의 안정화 범위보다 먼저 소비하지 않는다.

## 완료 증거

P4 제품 후보 `3fd9f0d9ebde0a58c653c94e46f8549643e3a194`는 Actions push `34769556924`와 PR `34769560151`, 실제 PostgreSQL 358 PASS·4 skip, 5-browser guest-write 5/5, 동일 SHA 개발 서버의 selected/public 충돌·두 guest live·offline rejected 복구·회수와 실제 서비스 재시작 전후 인수를 통과했다.

P5 최종 제품 후보 `8abf21a7e7d35cbd9a041e042836da0bb1305b6d`은 Actions push `34771639925`와 PR `34771648925`의 전체 검증을 통과했다. push 실행의 네 dev image 게시·서명, 28개 migration·환경·license·release manifest 봉인, Vitest 358 PASS·4 skip, 전체 E2E 348 PASS·40 skip, selected-read 5 PASS, public-link 9 PASS·4 skip, selected-write 5 PASS, public guest-write 5/5, release 10 PASS가 확인됐다.

같은 SHA를 개발 서버 `1.1.3/dev/p5`, schema `1130_public_lyric_guest_write.sql`로 배포해 네 서비스 healthy를 확인했다. 공개 smoke는 selected/public 충돌과 ACL 보존, 두 guest live 수렴, guest scope·private API/메모/연결 자료 격리, presence, offline rejected-only 복구·비자동 replay, 전체 회수를 통과했다. 실제 web/collaboration/worker 재시작 뒤 같은 흐름을 다시 통과했고 합성 fixture를 제거했다. 미해결 제품 P0/P1, W⊆R 위반, 권한 확대, 제한·중지·회수 뒤 새 update ACK, 거부 원문 유실은 0건이므로 승인된 정식 릴리스 gate를 연다.

실제 OS/물리 기기는 P5에서 새로 실행하지 않는다. 자동화 결과를 실기기 증거로 기록하지 않으며 `OPS-100-001` backup 예외도 열린 상태로 정식 릴리스 판단에 명시한다.
