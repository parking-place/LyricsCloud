# 1.0.1 Phase 8 — 현재 문서·버전·릴리스 도구 인수 기록

## 변경과 보존 경계

root와 11개 package/runtime의 활성 version을 `1.0.1`로 맞추고, README에 승인된 로고·합성 가입 화면·실제 CI badge·기능·설치·베타 CLI·보안/backup 제한·문서 지도를 반영했다. Agent/AGENTS, 계획 색인, 승인 ADR/PROD/OPS, 환경 예제, test-user/OAuth, 개발 배포, backup/restore, security, support와 changelog를 current/reference/history/proposed로 분류했다. 이미 봉인된 v1.0.0 환경 schema·manifest·migration checksum·release notes는 수정하지 않았다.

`release-phase-state.mjs`는 1.0.0 P5 release와 P6 review 전용 8개 ID를 보존하면서 1.0.1 P1~P10, 명시적 추가 Phase, `2.Patch-phase` 경로와 다자리 version/phase를 판정한다. `1.12.91`은 parser fixture일 뿐 예약 버전이 아니다.

개발 image는 SHA, `dev-<version>-p<N>`, `Dev`, `Dev-latest`만 사용하고 숫자 version·`Release`·`latest`·`Release-latest`를 움직이지 않는다. 정식 발행은 정확한 `v<VERSION>` tag와 수동 release intent에서만 version·SHA·세 release alias를 한 build에 묶는다. 기존 1.0.0 P6 candidate 격리는 유지한다.

## 로컬 수용 증거

- Node 24.20.0의 release Phase·발행 경계: 30 PASS. 1.0.0 이력, P1~P10, 추가 Phase, 다자리 숫자, 잘못된 event/ref/SHA/version, dev 보호 tag, 정식 세 alias의 모의 단일 digest를 확인했다.
- 1.0.1 runtime과 봉인된 1.0.0 artifact 계약: 11 package version, 기존 migration 19개, digest-only signed image 계약 PASS.
- 현재 문서 링크·정책: Markdown 508개, canonical 화면 15개 PASS. 1.0.0 최종 release validator도 역사 증거와 현재 1.0.1 Phase 경계를 함께 통과했다.
- workspace lint/typecheck: PASS.
- unit: 169 PASS, DB 환경이 없는 기본 unit 실행의 선택적 integration 81 SKIP.
- production build: PASS. Yjs duplicate import 경고는 재현됐고 P9 `RC-091-001` 재판정 입력으로 넘겼다.
- Chromium의 baseline·양 테마 브랜드/runtime metadata·favicon/PWA asset: 3 PASS.

## 개발 서버와 외부 경계

구현 후보 `17aa226abe48acee9642d6c539809b915362a28e`을 원격 Phase 브랜치와 개발 서버에 동일하게 반영했다. 공개 ready는 version `1.0.1`, 같은 build SHA, channel `dev`, phase `p8`, schema `0901_beta_signup.sql`을 반환했다. postgres·web·collaboration·worker가 모두 healthy였고 공개 live/ready/auth, favicon과 web manifest를 확인했다.

중간 commit은 `[skip ci]`이며 필수 원격 통합 CI는 P10 후보에서 실행한다. 이 Phase는 main, 정식 tag, Docker Hub moving release alias와 릴리스 서버를 변경하지 않았다. 외부 암호화 backup·24시간 RPO·실제 복원 훈련은 사용자의 현재 승인 예외로 미구축이며 성공으로 기록하지 않는다. P9는 격리 암호화 backup/restore 도구를 검증하고 이 운영 위험을 그대로 릴리스 체크리스트에 연결한다.

rollback은 이전 검증 SHA로 애플리케이션만 되돌린다. v1.0.0 artifact, PostgreSQL·브라우저 자료·Docker volume은 삭제하거나 과거 tag를 이동하지 않는다.
