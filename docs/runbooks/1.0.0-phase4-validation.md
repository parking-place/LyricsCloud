# 1.0.0 Phase 4 문서·운영 인계 검증

- 상태: Phase 4 수용 완료
- 범위: `LC-100-P4-01`~`LC-100-P4-09`
- 기준 production: Phase 2 승인 digest, source `084a083d22c5c279baca971be25fa71b0191e128`, schema `0802_lifecycle.sql`
- Phase 3 final evidence SHA: `6a9a5d0510ae1e656fbf488a6d67059d7a8d0393`

## 산출물 대조

| 작업 | 결과 | 증거 |
|---|---|---|
| README와 문서 지도 | PASS | 1.0 기능·범위·요구 조건·개발 quickstart와 사용자/운영/보안 문서 연결 |
| 새 셀프호스트 | PASS | `compose.selfhost.yaml`, [셀프호스팅 안내](../self-hosting.md) |
| OAuth·초대·secret·redirect | PASS | [Google OAuth runbook](./google-oauth-setup.md), 실제 값 비노출 |
| backup·restore·upgrade·rollback | PASS(도구), 운영 예외 | CI 격리 훈련 절차와 정확한 명령 갱신. 공식 릴리스 backup은 미구축·24시간 RPO 미보장 |
| lifecycle·export·PWA | PASS | [사용자 안내](../user-guide.md)에 30일 휴지통·7일 철회·export/backup 차이·offline 초안/update/logout 경계 기록 |
| 관측·사고 대응 | PASS | [수집 정책](../operations/OBSERVABILITY-DATA-CLASSIFICATION.md), [경보](./observability-alerts.md), [사고 양식](./incident-record-template.md) |
| 지원·보안·알려진 제한 | PASS | [지원 정책](../support.md), [보안 정책](../../SECURITY.md), CHANGELOG의 P2/P3·운영 예외 |

## clean-room 셀프호스트 재현

2026-09-09 새 Compose project `lyricscloud-p4-selfhost`, 전용 PostgreSQL volume, 합성 OAuth/DB/session 값과 `127.0.0.1:18080`을 사용했다. 실제 개발·릴리스 자격 증명, DB와 volume은 사용하지 않았다.

첫 기동에서 production collaboration·worker에 `APP_VERSION`·`BUILD_ID`가 전달되지 않아 `CONFIG_INVALID`로 종료되는 새 override 결함을 발견했다. `compose.selfhost.yaml`의 두 서비스에 1.0 version·전체 SHA를 명시하고 collaboration에는 `APP_ORIGIN`도 전달하도록 수정했다. 같은 image로 재기동한 결과는 다음과 같다.

- Compose config: PASS (Docker Engine 26.1.5, Compose 2.26.1)
- migration: one-shot exit 0, latest `0802_lifecycle.sql`
- PostgreSQL, web, collaboration, worker: 모두 healthy
- public live/ready: 200, version `1.0.0`, 합성 40자리 build ID, schema 일치
- 보호 `/api/songs`: 비로그인 401
- 종료: 정확한 disposable project의 container·network·`lyricscloud-p4-selfhost_postgres_data`만 제거, 다른 volume 미변경

## 자동 문서·secret 검사

`node scripts/validate-1004-documentation.mjs`가 Markdown 255개(검증 기록 추가 전), 내부 link target, 15개 canonical 화면명, environment schema·`.env.example`, self-host 명령·Compose contract, DEC-08-A~13-A 기간과 P2/P3/운영 예외 marker를 검사해 PASS했다. 기존 0.7.0 Phase 1·2 계획에서 repository 밖을 가리키던 상대 link 두 개를 발견해 실제 runbook으로 수정했다.

문서·Compose·validator를 합성 OAuth/session/provider canary로 `check-secret-leaks.mjs`에 전달해 일치 0건을 확인했다. Git history Gitleaks와 build/image secret scan은 원격 CI에서 다시 수행한다.

## 정책과 예외 판정

- 30일 휴지통, 7일 탈퇴 철회, backup 30일 보존·24시간 RPO, 관측 7/30/90일 상한은 Accepted DEC/ADR과 일치한다.
- 공식 릴리스 서버는 사용자 지시에 따라 개발과 같은 Google OAuth·DB 자격 증명을 쓰며 release session secret은 별도다.
- 외부 backup repository·일일 timer·archive·실제 restore 훈련은 실행하지 않았다. `OPS-100-001`로 1.0.1+에 넘기며 정상 또는 PASS로 표현하지 않는다.
- 실제 Google 계정 암호 입력은 자동화하지 않았다. Phase 3은 authorization endpoint·client·callback·PKCE/state/nonce와 합성 권한 흐름을 통과했다.

## 원격 CI·개발 서버 봉인

후보 SHA `f746d803b7888dc3183e640831dfc8dab19df604`는 GitHub Actions CI `34248044744`에서 전체 검증과 web·collaboration·worker·migrate 네 image의 발행·keyless 서명 검증을 통과했다. 같은 SHA를 개발 서버에 배포한 뒤 공개 live/ready smoke가 version `1.0.0`, build ID 전체 SHA, schema `0802_lifecycle.sql`을 반환해 PASS했다.

이 증거를 반영한 Phase 4 완료 commit은 동일한 원격 CI·네 signed image·개발 서버 exact-SHA 공개 smoke로 한 번 더 봉인하며, 그 결과 SHA는 STATUS 완료 기록에 남긴다.
