# 1.0.12 P3 개인 창작 수직 흐름 인수

- P2 병합 기준: `73d9a9a879113e65370949d412b888d94abc862e`
- P3 제품·E2E 후보: `9b891a82ce33622705a7195719c2878efaa94aa8`
- 원격: `phase/1.0.12-p3-vertical-flow`, PR #76
- GitHub Actions: push `34630519808`, PR `34630524596`
- runtime: `1.0.12`, channel `dev`, phase `p3`

## PC·모바일 수직 흐름

`tests/e2e/new-feature-1.0.12.spec.ts`는 한 owner가 화면에서 곡을 만들고 첫 가사를 작성한 뒤, 연결된 라임을 현재 CRDT caret에 삽입하고 문장형 프롬프트의 앞뒤 공백·연속 공백·한글·emoji를 그대로 복사하는 흐름을 desktop/mobile에 등록했다. 같은 곡 dashboard에서 수동 Suno 링크를 저장하고 HTTPS·`_blank`·`noopener noreferrer`를 확인한다. 재진입 뒤 가사 제목과 삽입 원문이 남고 Suno용 전체 copy에서는 `[Extend: ...]` marker만 제외된다.

두 번째 흐름은 다른 owner의 가사 조회가 404인지 확인하고 작업 자료 요청을 지연한 뒤 503으로 종료한다. 화면은 다시 시도 가능한 오류를 보이면서 현재 가사 원문을 유지한다. 모바일은 실제 dialog, desktop은 complementary panel을 사용하며 두 viewport 모두 문서 가로 overflow가 없다.

## 로컬 검증

- Node 24.20.0 check와 production build PASS.
- 신규 Chromium desktop/mobile 4건을 독립 실행 두 번 PASS.
- P3 영향 기존 창작·prompt mode·sub-songform/copy·Extend·보기·곡/라임/prompt 정렬·Suno·최근 위치의 desktop/mobile 74건 실행에서 67 PASS·조건부 6 skip. 빌드와 동시에 실행해 `.next`가 교체된 신규 desktop 1건은 검증에서 제외했고, 빌드 완료 뒤 같은 신규 4건 독립 재실행으로 PASS했다.
- 실제 PostgreSQL 18을 사용했으며 합성 계정과 자료는 각 검사에서 제거했다. schema와 API·RLS·rank·copy 계약은 변경하지 않았다.

## 원격·개발 인수

후보 `9b891a82ce33622705a7195719c2878efaa94aa8`의 push Actions `34630519808`과 PR Actions `34630524596`은 전체 verify를 통과했다. migration 반복, unit/integration, production build/image, fresh DB, restore/rollback, image vulnerability·secret, auth/ownership E2E와 Chromium/Firefox/WebKit release matrix를 포함한다. P5 전용 sealed-release step만 의도적으로 skip됐다.

개발 서버 checkout·환경 `BUILD_ID`·공개 ready가 후보 SHA와 일치했고 `1.0.12`, channel `dev`, phase `p3`, schema `1003_song_suno_workspaces.sql`, postgres·web·collaboration·worker healthy를 확인했다. 공개 Chromium의 1440×1000 dark와 390×844 mobile에서 연결 라임 삽입·재진입 저장, 문장형 프롬프트 앞뒤/연속 공백·한글·emoji exact copy, Suno 링크 `_blank`·`noopener`, 가로 overflow 부재를 확인했다. 합성 계정·곡·가사·라임·프롬프트는 종료 시 제거했다.

완료 문서 commit은 같은 제품 tree와 Phase metadata만 유지하며 별도 기능 변경을 포함하지 않는다. 완료 SHA도 개발 서버에서 다시 배포·공개 확인해 문서 commit과 실행 SHA를 일치시킨다.

실제 Windows/iOS/Android 물리 기기와 실제 OS IME는 새로 실행하지 않았다. `NF-REQ-032` 자동 metadata, 1.0.0 P6 review, `OPS-100-001`, `RC-100-001`, `RC-091-002`는 해결됐다고 처리하지 않는다. P3는 공유 owner/actor·RLS·capability를 도입하지 않으며 1.1.0을 자동 시작하지 않는다.
