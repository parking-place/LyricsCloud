# 1.0.10 Phase 4 Suno 작업공간 교차 회귀

## 후보와 범위

- 기준: P3 완료 SHA `fdfb6dabf551f6419679658c2863ffaec7031c2a`.
- P4 제품 코드는 P3와 같고 개발 build phase와 CI metadata를 `p4`로 맞춘다.
- 회귀에서 CI의 `APP_PHASE=p2` 잔존을 발견해 현재 Phase 값으로 최소 수정했다. Suno 저장·UI 계약은 변경하지 않았다.

## 실제 DB와 브라우저

- Node 24.20.0과 격리 PostgreSQL에서 계약·workspace store·owner export 3 files·7 tests PASS.
- 사용자 지정 모델, 서로 다른 링크 3개, 새 store 연결 뒤 재진입, 순서와 idempotent replay, request ID 재사용 거부를 확인했다.
- 다른 owner와 삭제된 곡은 workspace를 볼 수 없고, 곡 복원 시 같은 aggregate가 복원되며 stale version은 부분 쓰기 없이 거부됐다.
- 잘못된 scheme·userinfo·port·host·path와 authority field, 중복 reorder ID를 거부했고 사용자 지정 모델 원문은 정책 범위에서 보존했다.
- Chromium·Firefox·WebKit desktop과 Chromium·WebKit mobile에서 `tests/e2e/new-feature-1.0.10.spec.ts` 20/20 PASS.
- 같은 브라우저 회귀에서 링크 3개 저장·재진입·새 탭 보호, 한글 순차 입력 초안의 네트워크 실패 후 재접속, 잘못된 URL 유지·명시 오류, refresh 503, 다른 owner 404·익명 401을 확인했다.

## 성능·원격 CI

- 같은 격리 PostgreSQL의 0.9.1 성능 게이트는 모든 절대 예산·0% 오류율·3회 CV를 PASS했다. 저장 p95 6.297ms, revision p95 5.543ms, silent loss 0건이었다.
- 최초 후보 `b0c2e29460b038a1be25e0f538e5181cf4dd35ac`의 push run `34610047089`는 절대 예산은 모두 통과했지만 짧은 save/revision 측정의 runner 변동으로 3회 CV 기준만 실패했다.
- CI phase metadata를 바로잡은 최종 제품 후보 `386c37ac1b4bf0e54b7ff008179a5384273c020e`의 단독 push run `34610699744`는 verify 21분 29초, Chromium desktop/mobile 330 PASS·환경 조건부 36 skip, 5-project release matrix 10 PASS와 네 dev image 게시·서명을 모두 통과했다.

## 개발 서버·잔여 경계

- 후보 `386c37ac1b4bf0e54b7ff008179a5384273c020e`를 개발 서버에 배포해 `1.0.10`·`dev`·`p4`, schema `1003_song_suno_workspaces.sql`, PostgreSQL/web/collaboration/worker healthy를 확인했다.
- 공개 API에서 custom model·링크 3개·다른 owner 404·곡 soft-delete/restore를 확인하고 web·collaboration·worker를 실제 재시작했다. 재시작 뒤 같은 workspace aggregate가 남았고 합성 사용자와 자료를 제거했다.
- Phase 완료 문서 SHA도 같은 제품 tree로 다시 배포해 build metadata와 공개 health를 최종 확인한다.

자동 브라우저 viewport와 engine 결과를 실제 Windows/iOS/Android 물리 기기로 기록하지 않는다. 외부 Suno 요청·원곡 삭제는 수행하지 않으며 백업 `OPS-100-001` 사용자 승인 예외는 그대로 유지한다.
