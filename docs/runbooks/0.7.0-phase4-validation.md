# 0.7.0 Phase 4 통합 즐겨찾기·핀 검증

## 검증 범위

- [PROD-0011](../product/PROD-0011-favorites-pins.md)의 독립적인 즐겨찾기·핀 의미와 owner별 전역 핀 순서
- 곡·가사·라임 노트·프롬프트 공통 조회·토글, 소유권 비노출 404와 mutation Origin 경계
- 자료 유형·소속 곡·곡/가사 작업 상태·즐겨찾기/핀 범위의 URL 필터와 잘못된 URL 정규화
- 핀 우선 정렬, 새 핀 append, 해제 compact, 전체 집합 원자 reorder
- PC drag와 모바일·키보드 `앞으로`·`뒤로` 대안, 텍스트 이름과 `aria-pressed`
- soft delete 즉시 비노출과 복원 후 표시 속성 보존, 느린 연속 toggle의 마지막 의도

## 로컬 결과

- `pnpm check`: architecture boundary와 TypeScript 통과
- `pnpm test` (`AUTH_DATABASE_INTEGRATION=true`): 47 files, 177 tests 통과
- `pnpm build`: `/favorites`, saved API와 production standalone build 통과. 기존 Yjs 중복 import 경고 외 실패 없음
- Phase 4 PC·모바일 E2E: 3 통과, API 중복 실행 1 의도적 skip
- 전체 `pnpm test:e2e`: PC·모바일 183 통과, 9 의도적 skip
- `pnpm check:secrets apps/web/.next test-results`: build·브라우저 증적 비밀값 누출 0건
- 네 production image와 fresh named-volume DB: migration, web·collaboration·worker·same-origin proxy readiness, revision·WebSocket 수렴, DB·collaboration 재시작 뒤 durable projection·ACK·revision 복원 통과
- 셸 내비게이션 변경에 맞춰 기존 PC·모바일 기준 screenshot을 갱신했고 전체 브라우저 회귀에서 확인

## 원격·배포 증적

- 기능 SHA: 완료 시 기록
- GitHub Actions: 완료 시 기록
- 개발 이미지 digest: 완료 시 기록
- 동일 SHA 개발 배포와 공개 smoke: 완료 시 기록

보안상 서버 접속 정보와 배포 실행 원문은 추적 제외된 로컬 증적에 둔다. 릴리스 서버·`main`·`Release`·`latest`는 이 Phase에서 변경하지 않는다.
