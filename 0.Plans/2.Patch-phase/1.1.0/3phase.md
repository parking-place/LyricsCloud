# 1.1.0 Phase 3 — PC·모바일 사용자 흐름

- 상태: **완료** (`complete`)
- 단계 목적: 지정 사용자 읽기 공유 기반의 PC·모바일 사용자 흐름을 완료하고 다음 단계에 검증 가능한 입력을 전달한다.
- 문서 작성과 구현/배포 완료는 별개다.

> 구현 담당자는 승인된 범위에서 실패 재현→최소 구현→관련 회귀→검토 가능한 commit을 수행한다. 로컬 Codex의 기존 수정·초안·운영 데이터를 덮어쓰지 않는다.

## 목표

개인 앱에서 공유 가능한 앱으로 전환하는 권한 경계를 승인받고 지정한 계정에 한정한 읽기 공유를 제공한다. 이번 Phase는 아래 작업 체크리스트의 책임만 가진다.

## 선행조건

- [1.1.0 P2](2phase.md)의 산출물·검증/승인과 실제 최종 SHA 인수.
- 현재 실행 STATUS·Agent/AGENTS·수정 파일 담당자 충돌 확인.
- [품질 게이트](../QUALITY-GATES.md)의 격리 환경·지원 런타임·필수 물리 기기 확보.
- 공유 ADR와 마이너 진입 사유·호환·안전성 승인. 패치 횟수로 자동 진입하지 않는다.

## 기준 링크

[버전 목표·수용 사례](README.md) · [세부 계약](../contracts/SHARING.md) · [요구사항](../Requirements-Traceability.md) · [결정 권한](../Decision-Ownership.md) · [버전 규칙](../VERSIONING.md) · [릴리스 규칙](../RELEASE-POLICY.md)

## 포함 범위

`NF-REQ-034`, `NF-REQ-036`. 아래 작업에서 다루는 현재 패치의 범위만 수행한다. 여러 버전에 걸친 요구는 이 패치의 부분 범위를 검증한 것으로 기록한다.

## 제외 범위

다른 패치의 기능·사용자가 선택하지 않은 Future 아이디어·승인 없는 기술 교체·운영 변경은 제외한다.

## 수정 책임 경로와 인터페이스

- `packages/database/src`
- `packages/database/migrations`
- `packages/auth/src`
- `apps/web/src/app`
- `apps/web/src/components`
- `apps/collaboration/src`

경로는 책임 영역이다. 신규 제안 표시는 아직 생성된 파일이 아니며 실제 이름·runner·package export는 P1 인수에서 확정한다. 디렉터리 전체를 리팩터링하라는 권한이 아니다. **입력**은 선행 계약·source SHA·fixture·권한 문맥이고 **출력**은 현 단계의 코드/설계·실행 증거·후속 호환 계약이다.

## 작업 체크리스트

- [x] `LC-NF-1.1.0-P3-01` 소유자에게 private/selected read 설정과 실제 공개될 필드 미리보기를 제공한다.
- [x] `LC-NF-1.1.0-P3-02` 수신자는 자신에게 허용된 자료만 읽으며 개인 workspace·owner 메모를 가져오지 못한다.
- [x] `LC-NF-1.1.0-P3-03` 권한 없음·회수·삭제·로그인 필요를 구분하되 자료 존재를 불필요하게 노출하지 않는다.
- [x] `LC-NF-1.1.0-P3-04` 읽기 화면에서 쓰기·ACL·hard delete·소유권 이전 액션을 제공하지 않고 서버도 차단한다.
- [x] `LC-NF-1.1.0-P3-05` 제안 `tests/e2e/new-feature-1.1.0.spec.ts`에 실제 기능 경로를 등록하고 정상·빈 상태·실패·권한 없음·로딩을 PC와 모바일에서 검사한다. native이면 플랫폼 runner와 UI 테스트로 대체/병행한다.
- [x] `LC-NF-1.1.0-P3-06` 입력·선택·IME·undo·로컬 초안·서버 저장 상태의 연속성을 확인한다. 정상 UI만 보여주기 위해 오류/권한 검사를 제거하지 않는다.

- [x] `LC-NF-1.1.0-P3-07` 현재 보는 사람 UI와 읽기 전용 상태를 구분하고 참가자 이름을 통한 이메일/내부 ID 노출을 막는다. 키보드·화면 낭독으로도 확인 가능하게 한다.

## 구체적 검증

| 수용 ID | 입력·상황 | 기대 결과 |
|---|---|---|
| `AC-1.1.0-01` | owner가 A에게 특정 가사만 공유 | A는 해당 가사만 읽고 연결 라임·메모·다른 버전은 읽지 못한다. |
| `AC-1.1.0-02` | 사용자 B가 ID/URL/API/WS를 직접 조작 | 서버와 RLS가 읽기/쓰기를 모두 거부한다. |
| `AC-1.1.0-03` | 읽는 도중 owner가 권한 회수 | 기존 구독과 이후 API 읽기가 차단되고 캐시로 새 자료가 새지 않는다. |
| `AC-1.1.0-04` | 마이너 전환 승인이 없음 | 현재 1.0.x를 유지하며 ACL schema를 선행 적용하지 않는다. |

P1은 위 기대 결과와 실제 구현 가능 경계를 승인하는 단계다. P2/P3은 관련 재현·수정과 사용자 흐름을 실행하며, P4에서 전체 교차 검증하고 P5에서 실제 결과를 인수한다. 표가 있다는 이유로 테스트 완료로 처리하지 않는다.

## 실행·증거 기록

[공통 명령·검증](../QUALITY-GATES.md)을 먼저 읽는다. 기존 runner의 영향받은 검사를 우선 사용하고 필요할 때만 회귀를 보강한다. 지원 환경에서 관련 DB/E2E를 선택하며 동일 변경의 full suite는 로컬/CI 중 한 곳과 필수 게이트만 따른다. native은 승인된 SDK/플랫폼 명령을 기록한다. 없는 도구·실제 IME·물리 기기 검증을 모사 결과로 통과시켰다고 표시하지 않는다. 문서-only Phase는 링크·범위·결정·설계 검토로 별도 인수한다.

- [P3 인수 기록](../../../docs/runbooks/1.1.0-phase3-sharing-ui.md)에 UI·auth return·read-only sync/presence, 자동/실제 DB, Actions와 동일 SHA 공개 desktop/mobile 인수를 연결했다.
- 후보 `e315d4b6e38aa042482503cfae1f03444724b5e7`, Actions push `34676721051`·PR `34676727416` 단독 재실행 전체 verify, Chromium 342 PASS·36 skip와 release matrix 10 PASS가 완료됐다.
- 같은 SHA 개발 서버에서 owner/reader/stranger desktop/mobile의 grant·최소 필드·live update·presence·회수·비노출·cleanup을 PASS했다. 실제 물리 기기는 새로 실행하지 않았다.

## 완료 조건

- [x] 작업 ID마다 코드/설계·실행/검토 증거·정확한 SHA가 연결되어 있다.
- [x] 현재 패치의 원문·권한·복구·오류 처리가 정상 동작과 함께 검증되었다.
- [x] 미실행·남은 결함·외부 차단·보류한 기술 결정이 숨김없이 기록되었다.
- [x] 현재 상태/담당/변경 파일·관련 문서가 실제 수행 내용과 일치한다.
- [x] 구현 Phase는 CI·동일 SHA 개발 인수를, 설계-only는 승인 증거를 갖췄다.
- [x] main·Release·운영 변경은 별도 현재 승인 없이 수행하지 않았다.

## 산출물

- 1.1.0 P3의 검토 가능한 변경/계약과 수용 사례 증거.
- 변경 파일·추가 migration·실행 명령/환경·결과·미실행/잔여 위험의 인수 기록.
- 후속 소비자가 유지할 API·데이터·copy·권한·UI 상태 계약.

## 다음 Phase 인계

[1.1.0 P4](4phase.md)에 입력·실행 결과·호환 및 중단 조건을 넘긴다. 다음 단계의 승인이 없거나 선행 증거가 부족하면 자동 진행하지 않는다. 문서 작성으로 runtime version을 바꾸지 않는다.
