# 1.0.3 Phase 4 — 실패·권한·복구 회귀

- 상태: **완료** (`complete`, 구현·개발 서버 인수)
- 단계 목적: 프롬프트 태그형·문장형 선택 기반의 실패·권한·복구 회귀을 완료하고 다음 단계에 검증 가능한 입력을 전달한다.
- 문서 작성과 구현/배포 완료는 별개다.

> 구현 담당자는 승인된 범위에서 실패 재현→최소 구현→관련 회귀→검토 가능한 commit을 수행한다. 로컬 Codex의 기존 수정·초안·운영 데이터를 덮어쓰지 않는다.

## 목표

기존 프롬프트를 훼손하지 않고 태그형과 문장형을 선택·저장·복구하는 최소 완결 흐름을 제공한다. 이번 Phase는 아래 작업 체크리스트의 책임만 가진다.

## 선행조건

- [1.0.3 P3](3phase.md)의 산출물·검증/승인과 실제 최종 SHA 인수.
- 현재 실행 STATUS·Agent/AGENTS·수정 파일 담당자 충돌 확인.
- [품질 게이트](../QUALITY-GATES.md)의 격리 환경·지원 런타임·필수 물리 기기 확보.

## 기준 링크

[버전 목표·수용 사례](README.md) · [세부 계약](../contracts/EDITOR-COPY.md) · [요구사항](../Requirements-Traceability.md) · [결정 권한](../Decision-Ownership.md) · [버전 규칙](../VERSIONING.md) · [릴리스 규칙](../RELEASE-POLICY.md)

## 포함 범위

`NF-REQ-022`. 아래 작업에서 다루는 현재 패치의 범위만 수행한다. 여러 버전에 걸친 요구는 이 패치의 부분 범위를 검증한 것으로 기록한다.

## 제외 범위

자동 요약·AI 변환·Suno API는 제외한다.

## 수정 책임 경로와 인터페이스

- `packages/domain/src`
- `packages/editor/src`
- `packages/database/src`
- `packages/database/migrations`
- `apps/web/src/components/prompt-new-screen.tsx`
- `apps/web/src/components`

경로는 책임 영역이다. 신규 제안 표시는 아직 생성된 파일이 아니며 실제 이름·runner·package export는 P1 인수에서 확정한다. 디렉터리 전체를 리팩터링하라는 권한이 아니다. **입력**은 선행 계약·source SHA·fixture·권한 문맥이고 **출력**은 현 단계의 코드/설계·실행 증거·후속 호환 계약이다.

## 작업 체크리스트

- [x] `LC-NF-1.0.3-P4-01` `AC-1.0.3-01`: 저장된 양쪽 표현을 오가되 입력이 없으면 변환 dialog나 저장 변경 없이 기존 태그·문장·복사 결과가 유지됨을 Chromium·Firefox·WebKit에서 확인했다.
- [x] `LC-NF-1.0.3-P4-02` `AC-1.0.3-02`: 두 탭에서 문장 원문이 바뀐 뒤 오래된 변환 preview를 확인하면 원자적으로 거부하고, 최신 preview만 mode/body 함께 저장됨을 실제 PostgreSQL로 확인했다.
- [x] `LC-NF-1.0.3-P4-03` `AC-1.0.3-03`: offline 문장 편집·재접속·복제·명시 변환·revision 복원에서 구두점·연속 공백·줄바꿈·mode가 raw 그대로 유지됨을 확인했다.
- [x] `LC-NF-1.0.3-P4-04` `AC-1.0.3-04`: capability 없는 구버전 collaboration bootstrap은 409로 차단하고 capability 선언 연결은 성공하며 원문은 계속 조회·복제 가능함을 확인했다.
- [x] `LC-NF-1.0.3-P4-05` 실제 PostgreSQL 261건, Chromium 전체 306건, 새 기능 Firefox·WebKit 교차 회귀와 같은 SHA 개발 서버 collaboration 실제 재시작을 수행했다. 31건은 E2E 설정상 의도적 조건 skip이며 물리 기기 입력은 P3 자동 결과와 구분했다.
- [x] `LC-NF-1.0.3-P4-06` 전체 접근성 감사에서 발견한 prompt 비활성 상태 opacity 대비 결함만 최소 CSS 수정하고 15화면·양 테마 감사를 재실행해 PASS했다.

## 구체적 검증

| 수용 ID | 입력·상황 | 기대 결과 |
|---|---|---|
| `AC-1.0.3-01` | 태그형 → 문장형 보기 → 태그형 보기 | 입력하지 않았다면 원문과 기존 복사 출력이 바뀌지 않는다. |
| `AC-1.0.3-02` | 서로 다른 탭에서 mode와 본문 동시 변경 | 혼합 mode/body나 오래된 덮어쓰기가 없다. |
| `AC-1.0.3-03` | 문장형 offline 편집 후 재접속·복제·복원 | 구두점·공백·줄바꿈·mode가 보존된다. |
| `AC-1.0.3-04` | 구버전 클라이언트로 새 mode 접속 | 안내/읽기 제한 또는 호환 처리를 하며 원문을 잃지 않는다. |

P1은 위 기대 결과와 실제 구현 가능 경계를 승인하는 단계다. P2/P3은 관련 재현·수정과 사용자 흐름을 실행하며, P4에서 전체 교차 검증하고 P5에서 실제 결과를 인수한다. 표가 있다는 이유로 테스트 완료로 처리하지 않는다.

## 실행·증거 기록

[공통 명령·검증](../QUALITY-GATES.md)을 먼저 읽는다. 기존 runner의 영향받은 검사를 우선 사용하고 필요할 때만 회귀를 보강한다. 지원 환경에서 관련 DB/E2E를 선택하며 동일 변경의 full suite는 로컬/CI 중 한 곳과 필수 게이트만 따른다. native은 승인된 SDK/플랫폼 명령을 기록한다. 없는 도구·실제 IME·물리 기기 검증을 모사 결과로 통과시켰다고 표시하지 않는다. 문서-only Phase는 링크·범위·결정·설계 검토로 별도 인수한다.

## 완료 조건

- [x] 작업 ID마다 코드·실행 증거와 원격 구현 SHA `611eab02ccd3ba7279a1627e93d92436876657d7`을 연결했다.
- [x] 현재 패치의 원문·권한·복구·오류 처리를 정상 동작과 함께 검증했다.
- [x] 물리 기기 신규 실행 없음과 `OPS-100-001` 외부 backup 예외를 숨김없이 기록했다.
- [x] 현재 상태/담당/변경 파일·관련 문서가 실제 수행 내용과 일치한다.
- [x] 중간 CI는 `[skip ci]` 정책으로 생략했고 같은 SHA 개발 서버·공개 HTTPS·collaboration 재시작 인수를 완료했다. 필수 전체 CI는 P5 책임이다.
- [x] 1.0.3 main·Release·릴리스 서버 변경은 수행하지 않았다.

## 산출물

- 1.0.3 P4의 검토 가능한 변경/계약과 수용 사례 증거.
- 변경 파일·추가 migration·실행 명령/환경·결과·미실행/잔여 위험의 인수 기록.
- 후속 소비자가 유지할 API·데이터·copy·권한·UI 상태 계약.

## 다음 Phase 인계

[1.0.3 P5](5phase.md)는 요구 추적·사용자/운영 문서·환경 schema를 실제 mode/raw 동작에 맞추고, 최종 후보 전체 검증·필수 CI·같은 SHA 개발 인수를 수행한다. P4의 409 capability guard와 원자 변환·raw 보존 계약을 유지하며 `OPS-100-001` 예외와 1.0.3 릴리스 미승인을 그대로 인계한다.
