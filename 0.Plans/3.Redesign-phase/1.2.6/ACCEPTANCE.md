# 1.2.6 수용 기준 — 작성·탐색·모바일 사용 흐름

> 2026-09-23 순서 개정: [1.1.7b 통합](../1.1.7.b/README.md)과 1.2.0 인수가 선행한다. WC 후보의 최초 포팅은 b가 소유하며 이 문서의 같은 작업은 b 해결 SHA·회귀 인수와 남은 범위만 수행한다. 미완료 원격 코드를 완료로 간주하지 않는다.

**계획 기준이며 아래 항목의 실행 결과는 모두 미확인이다.** 테스트 이름/명령은 실제 착수 시 현행 manifest와 대조해 정한다. 존재하지 않는 script가 이미 있다고 가정하지 않는다.

[버전 계획](README.md) · [공통 gate](../QUALITY-GATES.md) · [추적표](../REVIEW-TRACEABILITY.md)

| 수용 ID | 대상 | 통과 기준 | 상태 |
|---|---|---|---|
| `AC-RD-126-01` | 홈·첫 작성 | 빈/재방문 합성 계정에서 첫 곡·새 가사·라임/프롬프트 빠른 추가·최근 작업의 목적을 구별할 수 있다. 기존 자료가 있는 계정에 첫 곡 안내만 보여주지 않으며 제출 실패 때 입력이 남는다. | planned / 미실행 |
| `AC-RD-126-02` | 이어쓰기 | 최근 작업/검색/목록에서 문서로 들어갔다 돌아오면 필터/선택·가능한 cursor/scroll 문맥을 유지한다. 권한 회수/삭제·이전 anchor 소멸에서는 명시된 대안으로 이동한다. | planned / 미실행 |
| `AC-RD-126-03` | 자료·복사 | 라임 삽입·프롬프트/템플릿 적용·연결/해제·원문/선택/Suno 복사를 구별한다. 버튼을 찾을 수 있고 원문·undo·선택 범위가 보존되며 clipboard 실패 시 exact manual copy가 된다. | planned / 미실행 |
| `AC-RD-126-04` | 모바일 | 320/390px·safe area·문서 하단 scroll·실제 가상 키보드·가로 회전에서 프롬프트 탭과 FAB의 클릭 영역이 겹치지 않는다. 자료 4탭·메모·공유·기록이 도달 가능하고 패널을 닫으면 편집 위치로 돌아온다. | planned / 미실행 |
| `AC-RD-126-05` | 접근성·사용성 | 본문 바로가기·landmark·대화상자 focus·drag 버튼 대안·양 테마/200%/forced colors/reduced motion/투명도 fallback을 확인한다. 대표 사용자 과제의 성공·잘못된 행동·도움 요청을 기록하되 참여자/시간 개선 수치를 만들지 않는다. | planned / 미실행 |

## 판정과 증거

각 항목마다 `source SHA / 시험 계층 / 환경 / 합성 fixture / 실행 명령 / 실패 전 결과 / 수정 뒤 결과 / 증거 위치 / 남은 실기기·외부 gate / 담당`을 기록한다. REPRODUCED나 재현 스크립트 exit 0은 결함 발생 증거이며 수정 PASS가 아니다.

실제 구현은 정상·빈·오류·권한 없음·offline·모바일 중 영향받는 상태를 포함한다. 단위 fixture, 실제 HTTP/DB, 브라우저, 물리 OS/AT, 공개 개발 smoke를 서로 대체하지 않는다. 개인정보 없는 합성 입력을 사용하며 비공개 원문 로그는 공개 계획에 복사하지 않는다.

## 중단·되돌림 기준

route·returnTo·선택·원문 계약을 유지한 컴포넌트 단위 rollback을 제공한다. UI 변경으로 CodeMirror를 재생성하거나 지연/오류 상태를 숨기지 않는다. 1.2.0에서 해소된 layout 결함은 재구현하지 않고 회귀로 유지하며 새 사용성 근거 없는 재설계를 강행하지 않는다.

원문 유실·거짓 저장·권한 확대·복구 불가가 확인되면 release blocker로 남긴다. 미실행 필수 검사를 PASS로 바꾸거나 후속 버전에 배정해 완료시키지 않는다. 실제 Phase 완료 절차는 공통 gate와 Agent.md를 함께 따른다.

## 비교 브랜치 추가 인수

아래 항목도 구현 완료 기준에 포함하며 현재는 모두 계획/미실행이다. 원격 PASS를 새 통합 SHA에 자동 승계하지 않는다.

- **[WC-10 — 곡 부분 저장·가사 복제 응답 유실](../BRANCH-COMPARISON-118-P4.md)**: 곡 다단계 저장의 성공 필드/미완료를 구별하고 새 입력을 보존한다. 가사 duplicate는 checkpoint 전 lock·동일 requestId replay·대기 중 metadata 변경 시 중단을 확인한다.

- **[WC-11 — 중첩 modal·도구막대·preview·복사](../BRANCH-COMPARISON-118-P4.md)**: Escape가 최상단만 닫고 touch focus가 복귀하며 도구가 잘리지 않는지 코발트에서 확인한다. Extend 공백 marker·원문/선택 copy와 preview 설정 회귀를 유지한다. UI-07/08 해결로 간주하지 않는다.
