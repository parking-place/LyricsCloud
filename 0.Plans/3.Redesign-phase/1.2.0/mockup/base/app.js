(()=>{'use strict';
const $=(q,el=document)=>el.querySelector(q),$$=(q,el=document)=>[...el.querySelectorAll(q)],esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),KEY='lyricscloud-redesign-120-cobalt-v1';
const menus=[['home','⌂','홈'],['songs','♫','곡'],['rhymes','≋','라임 노트'],['prompts','✧','프롬프트'],['search','⌕','검색'],['recent','◷','최근'],['favorites','☆','즐겨찾기'],['templates','▦','템플릿'],['trash','♧','휴지통'],['settings','⚙','설정']];
const lyricText='[Verse 1]\n창문에 걸린 오후의 빛\n한참을 머물다 이름이 되어\n아무도 적지 않은 이 순간에\n작은 멜로디를 놓아둘게\n\n[Chorus]\n우리는 빛의 조각을 모아\n서로의 하늘을 채워가\n조금은 느려도 괜찮아\n너의 속도로 걸어가\n\n[Verse 2: soft]\n멀리서 들려오는 발자국\n어제의 나에게 인사를 건네\n\n[Chorus]\n우리는 빛의 조각을 모아\n서로의 하늘을 채워가\n\n[Extend]\n두 번째 후렴 이후 악기 편곡 검토';
const seed={profile:{name:'유나',photo:'',google:'Yuna'},display:{theme:'light',font:'sans-serif',size:17,line:2.2,space:0,focus:false},songs:[{id:'s1',title:'빛의 조각',body:'서로의 일상을 비추는 작은 순간들',note:'두 번째 후렴은 조금 더 가볍게. 코러스의 여백을 살려 보기.',status:'가사 작성 중',color:'#b7e5d4',favorite:true,pin:true,model:'v4.5',links:[{title:'첫 번째 데모',url:'https://suno.com/song/synthetic-first',note:'코러스 방향 기록'},{title:'어쿠스틱 버전',url:'https://suno.com/song/synthetic-second',note:'보컬 톤 참고'}]},{id:'s2',title:'밤의 온도',body:'밤 산책에서 발견한 느린 리듬',note:'',status:'아이디어',color:'#eabfb9',favorite:true,pin:false,model:'',links:[]},{id:'s3',title:'아직 이름 붙이지 못한 우리의 아주 긴 여름 이야기 🌿',body:'끝나지 않는 계절의 감각',note:'',status:'보류',color:'#bacff0',favorite:false,pin:false,model:'',links:[]}],lyrics:[{id:'l1',song:'s1',title:'빛의 조각 · 두 번째 가사',body:lyricText,note:'후렴을 확장한 버전',status:'가사 작성 중',favorite:true,pin:true},{id:'l2',song:'s1',title:'빛의 조각 · 첫 스케치',body:'[Verse]\n우리가 지나온 작은 길에\n남겨진 이름을 불러 본다\n\n[Chorus]\n조각난 빛을 따라가',note:'원래 아이디어',status:'아이디어',favorite:false,pin:false},{id:'l3',song:'s3',title:'여름의 빈 페이지',body:'',note:'아직 시작하지 않은 가사',status:'아이디어',favorite:false,pin:false}],rhymes:[{id:'r1',title:'빛과 계절의 단어들',body:'반짝임 / 발자국 / 마지막\n푸른빛 / 눈부신 / 그리운\n여름의 끝 / 이름의 뜻',tags:['빛','계절'],songs:['s1','s3'],favorite:true,pin:false,color:'#aed9cc'},{id:'r2',title:'밤 산책에서',body:'밤의 온도 / 마음의 속도\n거리를 걷다 / 어제를 접다',tags:['도시','밤'],songs:['s2'],favorite:false,pin:true,color:'#e4c9c5'},{id:'r3',title:'다음 문장을 기다리며',body:'',tags:['메모'],songs:[],favorite:false,pin:false,color:'#cdddf0'}],prompts:[{id:'p1',title:'Soft indie pop',mode:'tags',tags:['indie pop','warm vocals','analog synth','84 bpm','warm vocals'],body:'A soft indie pop song. Warm vocals and a gentle pulse.',songs:['s1'],favorite:true,pin:true},{id:'p2',title:'새벽의 드림팝',mode:'sentence',tags:['dream pop','soft drums'],body:'Soft dream pop,  airy vocals.\nKeep the drums gentle.  Let the chorus bloom.',songs:['s1','s2'],favorite:true,pin:false},{id:'p3',title:'Acoustic colors',mode:'tags',tags:['acoustic','fingerstyle','close vocals'],body:'Acoustic fingerstyle. Close vocals.',songs:[],favorite:false,pin:false},{id:'p4',title:'긴 서사 실험',mode:'sentence',tags:[],body:('A gentle evolving arrangement with space for every line.  \n').repeat(23),songs:[],favorite:false,pin:false}],templates:[{id:'t1',title:'Verse → Chorus → Bridge',type:'lyric',mode:'',body:'[Verse]\n\n[Chorus]\n\n[Verse 2]\n\n[Chorus]\n\n[Bridge]\n\n[Outro]',source:'기본',favorite:true},{id:'t2',title:'Warm band',type:'prompt',mode:'tags',body:'live drums, warm bass, organic',source:'기본',favorite:false},{id:'t3',title:'공간을 남기는 편곡',type:'prompt',mode:'sentence',body:' Leave room between each phrase.\nLet the final chord linger.',source:'내',favorite:true},{id:'t4',title:'나의 짧은 후렴',type:'lyric',mode:'',body:'[Hook]\n\n[Verse]\n\n[Hook]',source:'내',favorite:false}],trash:[{id:'z1',kind:'songs',title:'오래된 파도',body:'삭제된 부모 곡',date:'2026.09.20',days:28},{id:'z2',kind:'lyrics',title:'파도 · 초안',body:'[Verse]\n작은 파도가 밀려오면',song:'z1',date:'2026.09.20',days:28},{id:'z3',kind:'rhymes',title:'보관해 둔 단어',body:'파도 / 궤도',tags:['바다'],songs:[],date:'2026.09.21',days:29}],views:{},history:{},recentSearch:['빛','warm'],recovery:'보내지 못한 나의 마지막 문장',share:{accounts:[],public:false,guest:false},schema:1};
let db;try{db=JSON.parse(localStorage.getItem(KEY))||structuredClone(seed)}catch{db=structuredClone(seed)}
let page='home',current='s1',resourceTab='rhymes',resourceScope='linked',resourceQueries={},focus=false,filter='',statusFilter='전체',songFilter='',tagFilter='',sort='최근 수정',templateType='lyric',templateSource='전체',favoriteScope='모두',searchKind='전체',themePreview=db.display.theme,profileDraft=null,displayDraft=null,promptUndo=null,modalReturn=null,selection={start:0,end:0},previewRevision='',demoState='normal',lastCopy='',modalCleanup=null;
const persist=()=>{try{localStorage.setItem(KEY,JSON.stringify(db))}catch{toast('기기 저장 공간을 사용할 수 없습니다. 입력을 복사해 보관하세요.')}};
const uid=p=>p+Date.now().toString(36),find=(kind,id=current)=>db[kind].find(x=>x.id===id),plain=p=>p.mode==='tags'?p.tags.join(', '):p.body;
const btn=(text,action,extra='',cls='')=>`<button class="button ${cls}" data-action="${action}" ${extra}>${text}</button>`;
const goBtn=(text,to,id='',cls='')=>`<button class="button ${cls}" data-go="${to}" ${id?`data-id="${id}"`:''}>${text}</button>`;
const tabs=(items,active,action)=>`<div class="tabs">${items.map(([id,label])=>`<button data-action="${action}" data-value="${id}" class="${active===id?'active':''}" aria-pressed="${active===id}">${label}</button>`).join('')}</div>`;
const heading=(eyebrow,title,desc,actions='')=>`<div class="heading"><div><div class="eyebrow">${eyebrow}</div><h1>${title}</h1>${desc?`<p>${desc}</p>`:''}</div><div class="actions">${actions}</div></div>`;
function toast(text){$('#toast').textContent=text;$('#toast').classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>$('#toast').classList.remove('show'),3400)}
function checkpoint(kind,item){const key=kind+':'+item.id;(db.history[key]??=[]).unshift({at:new Date().toLocaleString('ko-KR'),value:structuredClone(item)});db.history[key]=db.history[key].slice(0,12)}
function theme(v){themePreview=v;document.body.classList.toggle('dark',v==='dark'||v==='system'&&matchMedia('(prefers-color-scheme: dark)').matches)}
function nav(){ $('#dock').innerHTML=menus.map(([id,icon,label])=>`<button data-go="${id}" aria-label="${label}" title="${label}" ${page===id?'aria-current="page"':''} class="${page===id?'active ':''}${['songs','rhymes','prompts','search'].includes(id)?'':'desktop-only'}"><span class="nav-icon">${icon}</span><span class="nav-label">${label}</span></button>`).join('')+'<button class="mobile-only" data-action="more" aria-label="더보기"><span class="nav-icon">•••</span><span class="nav-label">더보기</span></button>';$('#profile-label').textContent=db.profile.name;$('#avatar').textContent=db.profile.photo?'':db.profile.name.slice(0,1);$('#avatar').style.backgroundImage=db.profile.photo?`url("${db.profile.photo}")`:'';$('#context').textContent=menus.find(m=>m[0]===page)?.[2]||({'song':'곡 작업 공간','lyric':'가사 편집','rhyme':'라임 편집','prompt':'프롬프트 편집','auth':'로그인'}[page]||'');}
function navigate(to,id,bypass=false){if(!bypass&&profileDraft&&JSON.stringify(profileDraft)!==JSON.stringify(db.profile)){modal('저장하지 않은 프로필',`<p>바꾼 이름과 사진이 아직 저장되지 않았어요.</p><div class="modal-footer">${btn('계속 편집','close')}${btn('버리고 이동','discard-profile',`data-page="${to}" data-id="${id||''}"`,'primary')}</div>`);return}profileDraft=null;displayDraft=null;if(id)current=id;page=to;filter='';statusFilter='전체';songFilter='';tagFilter='';demoState='normal';focus=to==='lyric'&&db.display.focus;close();render();window.scrollTo(0,0)}
function render(){nav();const views={home, songs:()=>library('songs'),rhymes:()=>library('rhymes'),prompts:()=>library('prompts'),song:songView,lyric:lyricView,rhyme:rhymeView,prompt:promptView,search:searchView,recent:recentView,favorites:favoriteView,templates:templateView,trash:trashView,settings:settingsView,auth:authView};$('#app').innerHTML=(views[page]||home)();$('#app').classList.toggle('focus-mode',focus);if(window.gsap&&!matchMedia('(prefers-reduced-motion: reduce)').matches)gsap.fromTo('#app > *',{opacity:.5,y:8},{opacity:1,y:0,duration:.35,stagger:.025,ease:'power2.out'});bindForm();enableDrag();}
function home(){const s=db.songs[0]||seed.songs[0];return `${heading('YOUR CREATIVE SPACE',`${esc(db.profile.name)}님, 오늘은 어떤 이야기인가요?`,'작은 영감에서 시작해도 좋아요. 나머지는 천천히.',`<span class="date">TUESDAY, SEPTEMBER 22</span>`).replace('class="heading"','class="heading home-heading"')}<div class="home-grid"><section class="module hero"><div class="hero-copy"><p class="eyebrow">A LITTLE SPARK, A NEW SONG</p><h2>생각을 모아,<br><em>한 곡으로.</em></h2><p>마음에 맴도는 한 줄, 아직 이름 없는 멜로디.<br>흩어진 영감을 나만의 음악으로 이어 보세요.</p><div class="actions">${btn('＋ 새 곡 시작하기','create','data-kind="songs"','primary')}${btn('아이디어 담기 ↗','idea','','')}</div></div><div class="orbital" aria-hidden="true"><i class="ring"></i><i class="ring"></i><i class="ring"></i><span class="spark">✦</span></div></section><section class="module ideas"><div class="module-top"><h2>오늘의 조각</h2><span class="label-tag">라임 노트</span></div><div class="idea-word">밤의 온도<br><span>마음의 속도</span></div><div class="ideas-footer"><small>도시 · 밤 &nbsp; / &nbsp; 2개의 연결</small><button class="round" data-go="rhyme" data-id="r2" aria-label="밤 산책에서 라임 열기">↗</button></div></section><section class="module continue"><div class="continue-left"><div class="eyebrow" style="margin:0">PICK UP WHERE YOU LEFT OFF</div><h3>${esc(s.title)}</h3><p>두 번째 가사 · 12분 전 수정 · 메모 1</p><div class="actions">${goBtn('이어서 쓰기 ↗','lyric','l1','primary')}<span class="label-tag">가사 작성 중</span></div></div><div class="lyric-preview"><div class="number">02</div><span class="eyebrow">CHORUS</span><pre>우리는 빛의 조각을 모아\n서로의 하늘을 채워가\n조금은 느려도 괜찮아\n너의 속도로 걸어가</pre><div class="rhythm" aria-hidden="true">${Array.from({length:36},(_,i)=>`<i style="--h:${4+Math.sin(i*1.8)**2*11}px"></i>`).join('')}</div></div></section><section class="module collect"><div class="module-top"><h2>곁에 두는 자료</h2>${goBtn('모두 ↗','favorites','','small')}</div><div class="resource-mini"><div class="resource-icon">✧</div><button data-go="prompt" data-id="p1"><strong>Soft indie pop</strong><p>프롬프트 · warm vocals, analog synth</p></button></div><div class="resource-mini"><div class="resource-icon coral">≋</div><button data-go="rhyme" data-id="r1"><strong>빛과 계절의 단어들</strong><p>라임 노트 · 빛, 계절</p></button></div></section></div><div class="section-heading"><h2>나의 곡 <small>&nbsp; ${db.songs.length}</small></h2>${goBtn('작업 공간 둘러보기 ↗','songs','','small')}</div><div class="project-grid">${db.songs.slice(0,3).map((s,i)=>`<section class="module project"><div class="project-top"><span class="cover">${['✦','☾','≈'][i]}</span>${btn(s.favorite?'★':'☆','favorite',`data-kind="songs" data-id="${s.id}" aria-label="즐겨찾기 전환"`,'small')}</div><button data-go="song" data-id="${s.id}" style="text-align:left;padding:0;grid-column:span 2"><h3>${esc(s.title)}</h3></button><p>${esc(s.body)}</p><div class="project-bottom"><span>${esc(s.status)} · 가사 ${db.lyrics.filter(l=>l.song===s.id).length}</span><button data-go="song" data-id="${s.id}" aria-label="${esc(s.title)} 열기">↗</button></div></section>`).join('')}</div>`}
function library(kind){const label={songs:'곡',rhymes:'라임 노트',prompts:'프롬프트'}[kind],items=getFiltered(kind),view=db.views[kind]||'medium';return heading('MY '+kind.toUpperCase(),`나의 ${label}`,kind==='rhymes'?'어떤 문장은, 단어 하나에서 시작되니까.':kind==='prompts'?'머릿속 사운드를 정확한 언어로.':'각자의 속도로 완성되는 나의 이야기들.',btn('＋ 새 '+label,'create',`data-kind="${kind}"`,'primary'))+`<div class="toolbar"><input id="list-search" aria-label="${label} 검색" placeholder="${kind==='songs'?'제목, 메모, 가사':kind==='rhymes'?'제목, 본문':'제목, 태그, 문장, 곡'} 검색" value="${esc(filter)}"><select id="sort" aria-label="정렬">${['최근 수정','최근 생성','오래된 생성','제목','즐겨찾기 우선','최근 사용','사용자 정렬'].map(t=>`<option ${sort===t?'selected':''}>${t}</option>`).join('')}</select>${kind==='songs'?`<select id="status-filter" aria-label="작업 상태">${['전체','아이디어','가사 작성 중','수정 중','Suno 생성 중','믹싱 중','완성','보류','연결 자료 있음','가사 없음'].map(t=>`<option ${statusFilter===t?'selected':''}>${t}</option>`).join('')}</select>`:`<select id="song-filter" aria-label="연결 곡 필터"><option value="">모든 연결 곡</option>${db.songs.map(s=>`<option value="${s.id}" ${songFilter===s.id?'selected':''}>${esc(s.title)}</option>`).join('')}</select>`}${tabs([['list','목록'],['small','작게'],['medium','중간'],['large','크게']],view,'view')}</div>${kind==='rhymes'?`<div class="chips" style="margin-bottom:20px">${['전체',...new Set(db.rhymes.flatMap(r=>r.tags))].map(t=>btn(esc(t),'tag-filter',`data-value="${esc(t)}"`,(tagFilter||'전체')===t?'blue small':'small')).join('')}</div>`:kind==='prompts'?`<div class="chips" style="margin-bottom:20px">${['전체','즐겨찾기','최근 사용'].map(t=>btn(t,'status-filter',`data-value="${t}"`,statusFilter===t?'blue small':'small')).join('')}</div>`:''}<p class="footnote" style="margin:0 0 14px">${items.length}개의 ${label} · ${sort}</p><div class="list-grid ${view}" id="list-items">${demoState==='empty'?empty(label):demoState==='loading'?empty('불러오는 중','자료를 준비하고 있어요.'):demoState==='error'?empty('자료를 불러오지 못했어요','입력한 검색 조건을 유지하고 다시 시도합니다.'):items.length?items.map(x=>itemCard(kind,x)).join(''):empty('검색 결과 없음','조건을 바꾸거나 검색을 지워 보세요.')}</div><div class="list-end">${btn('조건 지우기','clear-filter','','small')} &nbsp; 모두 확인했어요 · ${items.length}개</div>`}
function getFiltered(kind){let a=db[kind].filter(x=>(x.title+' '+(x.body||'')+' '+(x.note||'')+' '+(x.tags||[]).join(' ')+' '+(x.songs||[]).map(id=>find('songs',id)?.title||'').join(' ')+' '+(kind==='songs'?db.lyrics.filter(l=>l.song===x.id).map(l=>l.body).join(' '):'')).toLowerCase().includes(filter.toLowerCase()));if(kind==='songs'){if(statusFilter==='가사 없음')a=a.filter(x=>!db.lyrics.some(l=>l.song===x.id));else if(statusFilter==='연결 자료 있음')a=a.filter(x=>[...db.rhymes,...db.prompts].some(r=>r.songs.includes(x.id)));else if(statusFilter!=='전체')a=a.filter(x=>x.status===statusFilter)}else{if(songFilter)a=a.filter(x=>x.songs.includes(songFilter));if(tagFilter)a=a.filter(x=>x.tags.includes(tagFilter));if(statusFilter==='즐겨찾기')a=a.filter(x=>x.favorite)}if(sort==='제목')a.sort((a,b)=>a.title.localeCompare(b.title));if(sort==='오래된 생성')a.reverse();if(sort==='즐겨찾기 우선')a.sort((a,b)=>Number(b.favorite)-Number(a.favorite));return a}
function itemCard(kind,x){const route={songs:'song',rhymes:'rhyme',prompts:'prompt',lyrics:'lyric'}[kind],content=kind==='prompts'?plain(x):x.body||'';return `<article class="module item-card" data-card="${x.id}"><div class="module-top" style="width:100%"><span class="label-tag">${kind==='songs'?x.status:kind==='rhymes'?'라임 노트':kind==='lyrics'?'가사':x.mode==='tags'?'태그형':'문장형'}</span><div class="actions">${btn(x.pin?'◆':'◇','pin',`data-kind="${kind}" data-id="${x.id}" aria-label="핀 전환"`,'small')}${btn(x.favorite?'★':'☆','favorite',`data-kind="${kind}" data-id="${x.id}" aria-label="즐겨찾기 전환"`,'small')}</div></div><button data-go="${route}" data-id="${x.id}" style="text-align:left;padding:0"><h3>${esc(x.title)}</h3></button><p class="snippet">${esc(content||'아직 쓰지 않은 첫 문장을 기다리고 있어요.')}</p><div class="meta-row">${kind==='rhymes'?x.tags.map(t=>`<span>#${esc(t)}</span>`).join(''):''}<span>${kind==='songs'?`가사 ${db.lyrics.filter(l=>l.song===x.id).length} · ${x.note?'메모 있음':'메모 없음'}`:kind==='lyrics'?esc(find('songs',x.song)?.title||''):x.songs.map(id=>esc(find('songs',id)?.title||'')).join(' · ')||'연결한 곡 없음'}</span></div><footer><small>오늘 ${kind==='prompts'?'사용 3회':'수정'}</small><div class="actions">${kind!=='songs'?btn('복사','copy-item',`data-kind="${kind}" data-id="${x.id}"`,'small'):''}${sort==='사용자 정렬'?btn('↑','move-item',`data-kind="${kind}" data-id="${x.id}" data-dir="-1" aria-label="앞으로"`,'small')+btn('↓','move-item',`data-kind="${kind}" data-id="${x.id}" data-dir="1" aria-label="뒤로"`,'small'):''}${goBtn('열기 ↗',route,x.id,'small')}</div></footer></article>`}
function empty(title,desc='아직 담아 둔 자료가 없어요. 첫 번째 조각을 남겨 보세요.'){return `<div class="module empty"><h2>${esc(title)}</h2><p>${esc(desc)}</p>${btn('다시 보기','retry','','small')}${btn('빠른 추가','quick','','small')}</div>`}
function songView(){let s=find('songs');if(!s)return empty('곡을 찾을 수 없어요');const ls=db.lyrics.filter(l=>l.song===s.id);return heading('SONG WORKSPACE',esc(s.title),esc(s.body),goBtn('← 곡','songs','','small')+btn('곡 정보','edit-song','','small')+btn('삭제','delete',`data-kind="songs" data-id="${s.id}"`,'small danger'))+`<div class="workspace"><section><div class="module"><div class="module-top"><h2>가사 버전 <small>${ls.length}</small></h2>${btn('＋ 새 가사','create','data-kind="lyrics"','small')}</div>${ls.length?ls.map((l,i)=>`<div class="row"><button data-go="lyric" data-id="${l.id}" style="text-align:left;padding:0"><strong>${esc(l.title)}</strong><small>${i===0?'현재 작업 · ':''}${esc(l.status)} · ${esc(l.body.slice(0,45)||'빈 가사')}</small><small>${esc(l.note)}</small></button><div class="actions">${goBtn('열기','lyric',l.id,'small')}${btn('복제','duplicate',`data-kind="lyrics" data-id="${l.id}"`,'small')}${btn('메모','note',`data-kind="lyrics" data-id="${l.id}"`,'small')}</div></div>`).join(''):empty('첫 가사를 써 볼까요?')}</div><div class="module"><div class="module-top"><h2>곡 작업 메모</h2>${btn('수정','note',`data-kind="songs" data-id="${s.id}"`,'small')}</div><p class="preview" style="margin-top:18px">${esc(s.note||'메모를 추가해 보세요.')}</p></div><div class="module"><div class="module-top"><h2>연결한 자료</h2>${btn('연결 관리','links','data-kind="both"','small')}</div>${tabs([['rhymes','라임 노트'],['prompts','프롬프트']],resourceTab==='prompts'?'prompts':'rhymes','song-resource')}<div>${db[resourceTab==='prompts'?'prompts':'rhymes'].filter(r=>r.songs.includes(s.id)).map(r=>`<div class="row"><div><strong>${esc(r.title)}</strong><small>원본 자료와 연결 · 오늘 수정</small></div>${goBtn('자료 열기',resourceTab==='prompts'?'prompt':'rhyme',r.id,'small')}</div>`).join('')||'<p class="muted">연결된 자료가 없어요.</p>'}</div></div></section><section><div class="module"><div class="module-top"><h2>Suno 작업</h2><span class="label-tag">수동 기록</span></div><label style="margin-top:20px">사용한 모델<select id="suno-model">${['미지정','v4','v4.5','v5','사용자 지정'].map(v=>`<option ${s.model===v?'selected':''}>${v}</option>`).join('')}</select></label><input id="custom-model" placeholder="사용자 지정 모델 이름" aria-label="사용자 지정 모델" style="margin-top:8px">${btn('모델 저장','save-model','','small')}<hr><div class="module-top"><h3>작업 링크 <small>${s.links.length} / 20</small></h3>${btn('＋ 추가','suno-link','','small')}</div>${s.links.map((l,i)=>`<div class="row"><div><strong>${esc(l.title)}</strong><small>${esc(l.note)}</small><small>${esc(l.url)}</small></div><div class="actions">${btn('열기 ↗','suno-open',`data-index="${i}"`,'small')}${btn('수정','suno-link',`data-index="${i}"`,'small')}${btn('↑','suno-move',`data-index="${i}" data-dir="-1" ${i===0?'disabled':''}`,'small')}${btn('↓','suno-move',`data-index="${i}" data-dir="1" ${i===s.links.length-1?'disabled':''}`,'small')}${btn('제거','suno-remove',`data-index="${i}"`,'small')}</div></div>`).join('')}<p class="footnote">모델과 링크를 직접 기록해요. Suno 생성·재생 기능은 제공하지 않아요.</p></div><div class="module"><h3>작업 상태</h3><div class="meta-row"><span>${esc(s.status)}</span><span>오늘 수정</span></div><div class="actions">${btn(s.pin?'◆ 핀 해제':'◇ 핀','pin',`data-kind="songs" data-id="${s.id}"`,'small')}${btn(s.favorite?'★ 즐겨찾기 해제':'☆ 즐겨찾기','favorite',`data-kind="songs" data-id="${s.id}"`,'small')}</div></div></section></div>`}
function sections(text){const re=/^\[([^\]]+)\].*$/gm, matches=[...text.matchAll(re)];return matches.map((m,i)=>({title:m[1],index:m.index,end:matches[i+1]?.index??text.length,text:text.slice(m.index,matches[i+1]?.index??text.length),n:i}));}
const sunoText=text=>{const ss=sections(text);if(!ss.length)return text;return text.slice(0,ss[0].index)+ss.filter(s=>!/^Extend(?:\s|:|$)/i.test(s.title)).map(s=>s.text).join('')};
function lyricView(){const l=find('lyrics');if(!l)return empty('가사를 찾을 수 없어요');const s=find('songs',l.song),siblings=db.lyrics.filter(x=>x.song===l.song),idx=siblings.indexOf(l),ss=sections(l.body);return heading('LYRIC / '+esc(s?.title||''),esc(l.title),'',goBtn('← 곡 작업 공간','song',l.song,'small')+btn('기록·비교','history','data-kind="lyrics"','small')+btn('표시','display','','small')+btn('자료','resources','','small')+btn('공유','share','','small')+btn(focus?'집중 종료':'집중 모드','focus','','small')+btn('Suno용 복사','copy-suno','','primary')).replace('class="heading"','class="heading editor-header"')+`<div class="mobile-tools">${btn('송폼 목차','outline','','small')}${btn('송폼 삽입','songform','','small')}${btn('자료 4탭','resources','','small')}${btn('가사 설정','lyric-meta','','small')}</div><div class="editor-layout"><aside class="module outline"><div class="module-top"><h3>송폼</h3><small>${ss.length}</small></div>${ss.map(s=>`<label><input type="checkbox" data-section="${s.n}"><button data-action="jump-section" data-index="${s.index}">${esc(s.title)}<small>${s.n+1}번째 구간</small></button></label>`).join('')}<div class="stack">${btn('＋ 송폼 삽입','songform','','small')}${btn('선택 복사','copy-sections','','small')}${btn('원문 복사','copy-item',`data-kind="lyrics" data-id="${l.id}"`,'small')}${btn('가사 설정','lyric-meta','','small')}</div></aside><article class="module editor-paper"><div class="save-strip"><span id="save-state"><span class="status-dot"></span> 기기에 보관 · 로컬 시연</span><span id="char-count">${[...l.body].length}자</span></div><label><span class="hidden">가사 제목</span><input id="lyric-title" class="title-input" value="${esc(l.title)}" maxlength="200" style="margin-top:20px"></label><label><span class="hidden">가사 본문</span><textarea id="lyric-body" class="editor-body" style="font-family:${esc(l.display?.font||db.display.font)};font-size:${l.display?.size||db.display.size}px;line-height:${l.display?.line||db.display.line};letter-spacing:${l.display?.space||db.display.space}em" spellcheck="false">${esc(l.body)}</textarea></label><div class="row"><div class="actions">${goBtn('← 이전','lyric',siblings[idx-1]?.id||'',`small ${idx===0?'hidden':''}`)}${goBtn('다음 →','lyric',siblings[idx+1]?.id||'',`small ${idx===siblings.length-1?'hidden':''}`)}</div><small>Suno용 ${[...sunoText(l.body)].length}자 · Extend 제외</small></div></article><aside class="module resource-panel" id="resources"><div class="module-top"><h3>작업 자료</h3>${btn('접기','hide-resources','','small')}</div>${resourcePanel(l)}</aside></div>`}
function resourcePanel(l){let rt=resourceTab;const map={songs:'다른 곡',lyrics:'다른 가사',rhymes:'라임',prompts:'프롬프트'};let rows=db[rt].filter(x=>(x.title+' '+(x.body||'')).toLowerCase().includes((resourceQueries[rt]||'').toLowerCase()));if(['rhymes','prompts'].includes(rt)&&resourceScope==='linked')rows=rows.filter(x=>x.songs.includes(l.song));return `${tabs(Object.entries(map),rt,'resource-tab')}<input id="resource-search" aria-label="작업 자료 검색" placeholder="${map[rt]} 검색" value="${esc(resourceQueries[rt]||'')}">${['rhymes','prompts'].includes(rt)?`<div class="chips" style="margin-top:10px">${btn('연결 자료','resource-scope','data-value="linked"',(resourceScope==='linked'?'blue ':'')+'small')}${btn('전체 자료','resource-scope','data-value="all"',(resourceScope==='all'?'blue ':'')+'small')}</div>`:''}<div class="resource-cards">${rows.map(x=>`<div class="resource-card"><span class="label-tag">${rt==='prompts'?(x.mode==='tags'?'태그형':'문장형'):x.id===l.id?'현재 가사':rt==='songs'?`가사 ${db.lyrics.filter(z=>z.song===x.id).length}`:(x.songs?.includes(l.song)?'연결됨':map[rt])}</span><h3>${esc(x.title)}</h3><pre>${esc(rt==='prompts'?plain(x):x.body?.slice(0,180)||'본문 없음')}</pre><div class="actions">${rt==='rhymes'?btn('전체 삽입','insert-rhyme',`data-id="${x.id}"`,'small')+btn('선택 삽입','select-rhyme',`data-id="${x.id}"`,'small'):''}${['rhymes','prompts'].includes(rt)?btn('복사','copy-item',`data-kind="${rt}" data-id="${x.id}"`,'small'):''}${goBtn(x.id===l.id?'현재 가사':'원본 열기',{songs:'song',lyrics:'lyric',rhymes:'rhyme',prompts:'prompt'}[rt],x.id,'small')}${btn('새 창 ↗','new-window',`data-page="${{songs:'song',lyrics:'lyric',rhymes:'rhyme',prompts:'prompt'}[rt]}" data-id="${x.id}"`,'small')}</div></div>`).join('')||'<p class="footnote">자료가 없어요. 전체 자료에서 찾아보세요.</p>'}</div>`}
function rhymeView(){const r=find('rhymes');if(!r)return empty('라임을 찾을 수 없어요');return heading('RHYME NOTE',esc(r.title),'가사와 독립된, 자유로운 단어의 공간.',goBtn('← 라임 노트','rhymes','','small')+btn('기록','history','data-kind="rhymes"','small')+btn('전체 복사','copy-item',`data-kind="rhymes" data-id="${r.id}"`,'primary'))+`<div class="workspace"><section class="module"><label>노트 제목<input id="rhyme-title" value="${esc(r.title)}" maxlength="200"></label><label style="margin-top:20px">자유 본문<textarea id="rhyme-body" style="min-height:320px;font-size:18px">${esc(r.body)}</textarea></label><div class="row"><span class="status-pill" id="rhyme-count">${[...r.body].length}자 · 기기 보관</span>${btn('선택 복사','copy-selection','data-source="rhyme-body"','small')}</div><div class="actions">${btn('가사 대상을 선택해 삽입','rhyme-target','','small')}${btn('삭제','delete',`data-kind="rhymes" data-id="${r.id}"`,'small danger')}</div></section><aside><section class="module"><h3>단어의 분류</h3><div class="prompt-tags">${r.tags.map((t,i)=>`<span class="tag">${esc(t)}<button data-action="rhyme-tag-remove" data-index="${i}" aria-label="${esc(t)} 태그 제거">×</button></span>`).join('')}</div><div class="actions"><input id="rhyme-tag" placeholder="새 태그" aria-label="새 라임 태그" style="flex:1">${btn('추가','rhyme-tag-add','','small')}</div><hr><h3>연결 곡 <small>${r.songs.length}</small></h3>${r.songs.map(id=>`<p class="footnote">↗ ${esc(find('songs',id)?.title||'삭제된 곡')}</p>`).join('')}${btn('곡 연결 관리','links','data-kind="rhymes"','small')}<hr><label>노트 색상<select id="rhyme-color">${['없음','빨강','노랑','초록','파랑','회색'].map(x=>`<option>${x}</option>`).join('')}</select></label><div class="actions" style="margin-top:15px">${btn(r.pin?'◆ 핀 해제':'◇ 핀','pin',`data-kind="rhymes" data-id="${r.id}"`,'small')}${btn(r.favorite?'★ 즐겨찾기 해제':'☆ 즐겨찾기','favorite',`data-kind="rhymes" data-id="${r.id}"`,'small')}</div></section><p class="footnote">삽입할 가사를 직접 선택해요. 곡 연결을 해제해도 이 노트는 남아 있어요.</p></aside></div>`}
function promptView(){const p=find('prompts');if(!p)return empty('프롬프트를 찾을 수 없어요');let dupe=p.tags.length-new Set(p.tags.map(t=>t.toLowerCase())).size;return heading('PROMPT STUDIO',esc(p.title),'원하는 음악을 구체적인 언어로.',goBtn('← 프롬프트','prompts','','small')+btn('기록','history','data-kind="prompts"','small')+btn('복제','duplicate',`data-kind="prompts" data-id="${p.id}"`,'small')+btn('전체 복사','copy-item',`data-kind="prompts" data-id="${p.id}"`,'primary'))+`<div class="workspace"><section><div class="module"><label>프롬프트 제목<input id="prompt-title" maxlength="200" value="${esc(p.title)}"></label><div style="margin-top:21px">${tabs([['tags','태그형'],['sentence','문장형']],p.mode,'prompt-mode')}</div>${p.mode==='tags'?`<div class="actions"><input id="prompt-tag" list="suggestions" placeholder="태그 입력, 쉼표로 여러 개 추가" aria-label="프롬프트 태그 입력" style="flex:1"><datalist id="suggestions"><option value="warm vocals">사용 8회</option><option value="indie pop">사용 4회</option><option value="soft drums">사용 3회</option></datalist>${btn('추가','prompt-tag-add','','small')}</div><div class="prompt-tags" id="prompt-tags">${p.tags.map((t,i)=>`<span class="tag ${p.tags.indexOf(t)!==i?'duplicate':''}" data-tag-index="${i}"><span class="handle" aria-hidden="true">⠿</span>${esc(t)}<button data-action="tag-move" data-index="${i}" data-dir="-1" aria-label="${esc(t)} 앞으로" ${i===0?'disabled':''}>←</button><button data-action="tag-move" data-index="${i}" data-dir="1" aria-label="${esc(t)} 뒤로" ${i===p.tags.length-1?'disabled':''}>→</button><button data-action="prompt-tag-remove" data-index="${i}" aria-label="${esc(t)} 제거">×</button></span>`).join('')}</div><div class="row"><small>태그 ${p.tags.length}개 · 중복 ${dupe}개${dupe?' · 강조된 위치 확인':''}</small>${btn('중복 정리','dedupe',dupe?'':'disabled','small')}</div>`:`<label>문장 원문<textarea id="prompt-body" style="min-height:250px">${esc(p.body)}</textarea></label><small>공백·쉼표·마침표·줄바꿈을 그대로 보존해요.</small>`}<div class="actions" style="margin-top:20px">${btn('구조 변환 미리보기','convert','','small')}${btn('변환 취소 (undo)','undo-convert',promptUndo?'':'disabled','small')}${btn('템플릿 뒤에 추가','append-template','','blue small')}</div></div><div class="module"><div class="module-top"><h3>복사될 내용</h3><span id="prompt-count" class="label-tag">${[...plain(p)].length}자</span></div><pre id="prompt-preview" class="preview" style="margin-top:16px">${esc(plain(p))}</pre>${[...plain(p)].length>1000?'<p class="notice warning" style="margin-top:15px">1,000자를 넘었어요. 원문은 잘라내지 않고 그대로 복사합니다.</p>':''}</div></section><aside><div class="module"><h3>연결 곡</h3>${p.songs.map(id=>`<div class="row"><strong>${esc(find('songs',id)?.title)}</strong></div>`).join('')||'<p class="footnote">아직 연결된 곡이 없어요.</p>'}${btn('곡 연결 관리','links','data-kind="prompts"','small')}<hr><div class="actions">${btn(p.pin?'◆ 핀 해제':'◇ 핀','pin',`data-kind="prompts" data-id="${p.id}"`,'small')}${btn(p.favorite?'★ 즐겨찾기 해제':'☆ 즐겨찾기','favorite',`data-kind="prompts" data-id="${p.id}"`,'small')}</div><hr>${btn('휴지통으로 이동','delete',`data-kind="prompts" data-id="${p.id}"`,'danger small')}<p class="footnote">프롬프트는 복사해서 사용해요. 가사 본문에 자동 삽입되지 않습니다.</p></div></aside></div>`}
function searchView(){const all=[...db.songs.map(x=>({...x,kind:'곡',route:'song'})),...db.lyrics.map(x=>({...x,kind:'가사',route:'lyric'})),...db.rhymes.map(x=>({...x,kind:'라임',route:'rhyme'})),...db.prompts.map(x=>({...x,kind:'프롬프트',route:'prompt'}))].filter(x=>(searchKind==='전체'||searchKind===x.kind)&&(x.title+' '+x.body+' '+(x.tags||[]).join(' ')).toLowerCase().includes(filter.toLowerCase()));return heading('FIND YOUR NEXT LINE','통합 검색','제목·본문·태그의 정확한 단어 또는 일부를 찾아요.')+`<div class="toolbar"><input id="search-input" value="${esc(filter)}" aria-label="통합 검색어" placeholder="어떤 조각을 찾고 있나요?" style="max-width:none;font-size:20px;padding:17px">${btn('검색어 지우기','clear-search','','small')}</div>${tabs(['전체','곡','가사','라임','프롬프트'].map(x=>[x,x]),searchKind,'search-kind')}${filter?`<p class="footnote">${all.length}개 일치 · ${esc(filter)}</p><section class="module" style="padding:8px 20px">${all.map(x=>`<button class="search-result" data-go="${x.route}" data-id="${x.id}"><span class="label-tag">${x.kind} · 제목/본문</span><strong>${highlight(x.title,filter)}</strong><p>${highlight((x.body||x.tags?.join(', ')||'').slice(0,180),filter)}</p><small>오늘 수정 · ${x.songs?.length||1}개 곡 문맥 · 열기 ↗</small></button>`).join('')||empty('일치하는 조각이 없어요','다른 단어나 유형을 선택해 보세요.')}</section>`:`<section class="module"><div class="module-top"><h3>최근 검색</h3>${btn('전체 지우기','clear-recent-search','','small')}</div>${db.recentSearch.map(q=>`<div class="row"><button data-action="recent-search" data-value="${esc(q)}">◷ &nbsp; ${esc(q)}</button>${btn('지우기','remove-recent-search',`data-value="${esc(q)}"`,'small')}</div>`).join('')}</section>`}`}
function highlight(s,q){return esc(s).split(esc(q)).join(`<mark>${esc(q)}</mark>`)}
function recentView(){return heading('BACK TO YOUR FLOW','최근 작업','마지막으로 머물렀던 문장부터.')+tabs(['전체','곡','가사','라임','프롬프트'].map(x=>[x,x]),searchKind,'search-kind')+`<h3 style="margin:25px 0 15px">오늘</h3><div class="list-grid">${[...(searchKind==='전체'||searchKind==='가사'?db.lyrics.slice(0,1).map(x=>itemCard('lyrics',x)):[]),...(searchKind==='전체'||searchKind==='라임'?db.rhymes.slice(0,1).map(x=>itemCard('rhymes',x)):[]),...(searchKind==='전체'||searchKind==='프롬프트'?db.prompts.slice(0,1).map(x=>itemCard('prompts',x)):[]),...(searchKind==='곡'?db.songs.map(x=>itemCard('songs',x)):[])].join('')}</div><p class="footnote">12:42 수정 · 마지막 위치: Chorus / 11:10 열람 · 독립 자료</p><h3 style="margin:25px 0 15px">어제</h3>${goBtn('빛의 조각 작업 공간 열기','song','s1','small')}`}
function favoriteView(){let all=['songs','lyrics','rhymes','prompts'].flatMap(kind=>db[kind].map(x=>({kind,x})));return heading('CLOSE AT HAND','즐겨찾기','지금 필요한 조각들을 가까이에.')+tabs(['모두','핀','즐겨찾기'].map(x=>[x,x]),favoriteScope,'favorite-scope')+(favoriteScope!=='즐겨찾기'?`<h3 style="margin:20px 0 16px">지금 집중 중 · ${all.filter(({x})=>x.pin).length}</h3><div class="list-grid">${all.filter(({x})=>x.pin).map(({kind,x})=>itemCard(kind,x)).join('')}</div>`:'')+(favoriteScope!=='핀'?`<h3 style="margin:25px 0 16px">즐겨찾는 자료</h3><div class="list-grid">${all.filter(({x})=>x.favorite).map(({kind,x})=>itemCard(kind,x)).join('')||empty('즐겨찾기를 담아 보세요')}</div>`:'')}
function templateView(){let list=db.templates.filter(t=>t.type===templateType&&(templateSource==='전체'||t.source===templateSource));return heading('A PLACE TO BEGIN','템플릿','좋은 구조가 새로운 시작을 도와줘요.',btn('＋ 내 템플릿','new-template','','primary'))+tabs([['lyric','가사 구조'],['prompt','프롬프트']],templateType,'template-type')+`<div class="toolbar"><select id="template-source" aria-label="템플릿 출처">${['전체','기본','내'].map(s=>`<option ${templateSource===s?'selected':''}>${s}</option>`).join('')}</select><select id="template-sort" aria-label="템플릿 정렬"><option>즐겨찾기 우선</option><option>최근 사용</option><option>최근 수정</option><option>이름순</option></select></div><div class="list-grid">${list.map(t=>`<section class="module item-card"><div class="module-top" style="width:100%"><span class="label-tag">${t.source==='기본'?'기본 · 읽기 전용':'내 템플릿'} ${t.mode==='tags'?'· 태그형':t.mode==='sentence'?'· 문장형':''}</span>${btn(t.favorite?'★':'☆','favorite',`data-kind="templates" data-id="${t.id}"`,'small')}</div><h3>${esc(t.title)}</h3><pre class="preview" style="width:100%">${esc(t.body)}</pre><div class="actions" style="margin-top:18px">${btn('이 템플릿으로 시작','template-start',`data-id="${t.id}"`,'primary small')}${btn('복제','duplicate',`data-kind="templates" data-id="${t.id}"`,'small')}${t.source==='내'?btn('수정','new-template',`data-id="${t.id}"`,'small')+btn('삭제','delete-template',`data-id="${t.id}"`,'small'):''}</div></section>`).join('')}</div><p class="footnote">여기서는 새 자료를 만듭니다. 기존 프롬프트에 추가하려면 프롬프트 편집기의 ‘템플릿 뒤에 추가’를 사용하세요.</p>`}
function trashView(){return heading('ROOM FOR A SECOND CHANCE','휴지통','삭제한 자료는 30일 동안 다시 꺼낼 수 있어요.')+`<div class="toolbar"><select id="trash-kind" aria-label="휴지통 자료 유형"><option value="">모든 자료</option><option value="songs">곡</option><option value="lyrics">가사</option><option value="rhymes">라임</option><option value="prompts">프롬프트</option></select>${btn('전체 선택 / 해제','trash-select','','small')}${btn('선택 복원','trash-restore','','small')}${btn('선택 완전 삭제','trash-delete','','danger small')}</div><section class="module">${db.trash.map(x=>`<div class="row" data-trash-kind="${x.kind}"><label class="check"><input type="checkbox" data-trash="${x.id}"><span><strong>${esc(x.title)}</strong><small>${{songs:'곡',lyrics:'가사',rhymes:'라임',prompts:'프롬프트'}[x.kind]} · 원래 위치 ${x.song?'삭제된 부모 곡':'개인 공간'} · ${x.date} 삭제</small><small>${x.days}일 후 자동 삭제 예정</small></span></label><div class="actions">${btn('복원','trash-restore',`data-id="${x.id}"`,'small')}${btn('완전 삭제','trash-delete',`data-id="${x.id}"`,'danger small')}</div></div>`).join('')||empty('휴지통이 비었어요')}</section>`}
function settingsView(){profileDraft??=structuredClone(db.profile);displayDraft??=structuredClone(db.display);return heading('MAKE IT YOURS','설정','쓰는 감각도, 나를 표현하는 방식도.',btn('계정 도구','account','','small')+btn('키보드 도움말','help','','small'))+`<div class="workspace"><section><div class="module"><div class="module-top"><h2>화면 및 작성</h2><span class="label-tag">계정 기본값</span></div><div class="stack" style="margin-top:22px"><div class="two"><label>테마<select id="theme-select">${[['light','라이트'],['dark','다크'],['system','시스템']].map(([v,t])=>`<option value="${v}" ${displayDraft.theme===v?'selected':''}>${t}</option>`).join('')}</select></label><label>글꼴<select id="display-font"><option value="sans-serif">기본 산세리프</option><option value="serif" ${displayDraft.font==='serif'?'selected':''}>세리프</option><option value="monospace">고정폭</option></select></label></div><div class="three"><label>글자 크기<input id="display-size" type="number" min="14" max="28" value="${displayDraft.size}"></label><label>줄 간격<input id="display-line" type="number" min="1.2" max="2.4" step=".1" value="${displayDraft.line}"></label><label>자간<input id="display-space" type="number" min="-.05" max=".2" step=".01" value="${displayDraft.space}"></label></div><div class="row"><span>집중 모드로 가사 열기</span><button class="switch" id="focus-switch" role="switch" aria-label="집중 모드로 가사 열기" aria-checked="${displayDraft.focus}" data-action="focus-switch"><i></i></button></div><div class="setting-preview" id="setting-preview" style="font-size:${displayDraft.size}px;line-height:${displayDraft.line};font-family:${displayDraft.font};letter-spacing:${displayDraft.space}em">한 줄의 노래가 시작되는 순간.\nA little light, a new beginning.\n小さな光。 ✦ ♫ 🌿</div><div class="actions">${btn('제품 기본값','display-default','','small')}${btn('취소','display-cancel','','small')}${btn('화면 설정 저장','display-save','','primary')}</div></div></div><p class="footnote">가사별 표시 설정이 있으면 그 가사의 설정을 우선합니다.</p></section><aside><div class="module"><h2>나의 프로필</h2><div class="photo-pair"><div><span class="avatar" id="saved-photo" style="${db.profile.photo?`background-image:url('${db.profile.photo}')`:''}">${db.profile.photo?'':esc(db.profile.name[0])}</span><small>저장된 사진</small></div><div><span class="avatar" id="draft-photo" style="${profileDraft.photo?`background-image:url('${profileDraft.photo}')`:''}">${profileDraft.photo?'':esc(profileDraft.name[0]||'유')}</span><small>선택한 미저장 사진</small></div></div><label>사진 선택<input id="photo-input" type="file" accept="image/png,image/jpeg,image/webp"></label><small>PNG · JPEG · WebP / 최대 2MiB</small><div class="actions" style="margin-top:12px">${btn('Google 기본 사진으로','photo-default','','small')}</div><hr><label>닉네임 · 1–120자<input id="profile-name" value="${esc(profileDraft.name)}" maxlength="120"></label><small>현재 저장 이름: ${esc(db.profile.name)} · 사용자 지정</small><div class="actions" style="margin:10px 0 18px">${btn('Google 기본 이름으로','name-default','','small')}</div><label>Google 계정 이메일<input value="creator@example.invalid" readonly></label><p class="footnote">이 프로필 사진은 비공개예요. 공개 가사의 이름 표시는 공유할 때 따로 선택합니다.</p><div id="profile-result" class="footnote" aria-live="polite"></div><div class="actions" style="margin-top:20px">${btn('취소','profile-cancel','','small')}${btn('프로필 저장','profile-save','','primary')}</div></div></aside></div>`}
function authView(){return `<section class="module auth-page"><div class="eyebrow">YOUR WORDS, YOUR WORLD</div><h1>다시, 나의 창작 공간으로.</h1><p class="muted" style="margin:20px 0">LyricsCloud는 가사와 자료를 한곳에서 쓰는 초대 베타 서비스예요.</p><div class="stack">${btn('G &nbsp; Google로 로그인','auth-login','','primary')}${btn('초대 코드로 가입 시작','signup')}<p class="notice">독립 목업의 합성 로그인 화면입니다. 실제 계정에 연결하지 않아요.</p><div class="actions">${btn('이용 안내','legal','data-kind="terms"','small')}${btn('개인정보 안내','legal','data-kind="privacy"','small')}</div></div></section>`}
function modal(title,html){const d=$('#modal');if(!d.open){modalReturn=document.activeElement;d.showModal()}$('#modal-content').innerHTML=`<div class="modal-heading"><h2>${title}</h2><button class="icon-button" data-action="close" aria-label="닫기">×</button></div>${html}`;setTimeout(()=>$('#modal-content button, #modal-content input, #modal-content textarea, #modal-content select')?.focus(),20)}
function snapshotModal(){const root=$('#modal-content'),clone=root.cloneNode(true);$$('input,textarea,select',root).forEach((n,i)=>{const c=$$('input,textarea,select',clone)[i];if(n.tagName==='TEXTAREA')c.textContent=n.value;else if(n.tagName==='SELECT')Array.from(c.options).forEach((o,j)=>o.toggleAttribute('selected',n.selectedIndex===j));else{c.setAttribute('value',n.value);c.toggleAttribute('checked',n.checked)}});return clone.innerHTML}
function close(){if($('#modal').open){$('#modal').close();if(modalCleanup){modalCleanup();modalCleanup=null}modalReturn?.focus?.()}}
$('#modal').addEventListener('click',e=>{if(e.target===$('#modal'))close()});$('#modal').addEventListener('cancel',e=>{e.preventDefault();close()});
async function copy(text){lastCopy=text;try{await navigator.clipboard.writeText(text);toast(`${[...text].length}자 복사했어요.`)}catch{modal('직접 복사',`<p class="muted">브라우저가 클립보드 접근을 허용하지 않았어요. 아래 원문을 선택해 복사해 주세요.</p><textarea id="manual-copy" readonly style="margin-top:18px">${esc(text)}</textarea><div class="modal-footer">${btn('전체 선택','select-copy')}${btn('복사 완료','close','','primary')}</div>`);$('#manual-copy').select()}}
function insert(text,lyricId=current){const l=find('lyrics',lyricId);if(!l)return;checkpoint('lyrics',l);const pos=lyricId===current?selection:{start:l.body.length,end:l.body.length};l.body=l.body.slice(0,pos.start)+text+l.body.slice(pos.end);selection={start:pos.start+text.length,end:pos.start+text.length};persist();close();page='lyric';current=lyricId;render();$('#lyric-body')?.setSelectionRange(selection.start,selection.end);toast(`‘${l.title}’에 선택한 원문을 삽입했어요.`)}
function links(kind){const s=page==='song'?find('songs'):null,item=s?null:find(kind);const options=s?['rhymes','prompts'].flatMap(k=>db[k].map(x=>({x,k}))):db.songs.map(x=>({x,k:'songs'}));modal(s?'곡 자료 연결 관리':'곡 연결 관리',`<input id="link-search" placeholder="제목 검색" aria-label="연결 자료 검색"><div class="stack" id="link-options" style="margin-top:18px">${options.map(({x,k})=>`<label class="check" data-link-label="${esc(x.title.toLowerCase())}"><input type="checkbox" data-link-kind="${k}" value="${x.id}" ${(s?x.songs.includes(s.id):item.songs.includes(x.id))?'checked':''}>${esc(x.title)} <small>${k==='rhymes'?'라임':k==='prompts'?'프롬프트':''}</small></label>`).join('')}</div><p class="footnote">연결 해제는 자료 원본을 삭제하지 않아요.</p><div class="modal-footer">${btn('취소','close')}${btn('변경 적용','apply-links',`data-kind="${kind}"`,'primary')}</div>`);$('#link-search').oninput=e=>$$('[data-link-label]').forEach(n=>n.classList.toggle('hidden',!n.dataset.linkLabel.includes(e.target.value.toLowerCase())))}
function create(kind,templateId){let t=templateId?find('templates',templateId):null;modal(({songs:'새 곡',lyrics:'새 가사',rhymes:'새 라임 노트',prompts:'새 프롬프트'})[kind],`<form id="create-form" class="stack"><label>제목 *<input id="new-title" required maxlength="200" placeholder="제목을 적어 주세요" value="${t?esc(t.title):''}"></label>${kind==='lyrics'?`<label>부모 곡<select id="new-song">${db.songs.map(s=>`<option value="${s.id}" ${current===s.id?'selected':''}>${esc(s.title)} · 가사 ${db.lyrics.filter(l=>l.song===s.id).length}</option>`).join('')}</select></label><label>시작 방법<select id="new-lyric-template"><option value="">빈 가사</option>${db.templates.filter(t=>t.type==='lyric').map(t=>`<option value="${t.id}" ${templateId===t.id?'selected':''}>${esc(t.title)}</option>`).join('')}</select></label>`:''}${kind==='songs'?`<div class="two"><label>상태<select id="new-status">${['아이디어','가사 작성 중','수정 중','Suno 생성 중','믹싱 중','완성','보류'].map(x=>`<option>${x}</option>`).join('')}</select></label><label>색상<input type="color" id="new-color" value="#b7e5d4"></label></div><label>설명<textarea id="new-body"></textarea></label><label>작업 메모<textarea id="new-note"></textarea></label><div class="actions"><label class="check"><input id="new-pin" type="checkbox">핀</label><label class="check"><input id="new-favorite" type="checkbox">즐겨찾기</label></div>`:kind==='lyrics'?'':`<label>본문${kind==='prompts'?' · 태그는 쉼표로 구분':''}<textarea id="new-body">${esc(t?.body||'')}</textarea></label>${kind==='prompts'?`<label>형식<select id="new-mode"><option value="tags" ${t?.mode==='tags'?'selected':''}>태그형</option><option value="sentence" ${t?.mode==='sentence'?'selected':''}>문장형</option></select></label>`:''}<p class="notice">합성 기기 초안을 먼저 보관합니다. 현재 제품의 유효 제목 자동 생성 흐름은 시연 도구에서 확인할 수 있어요. 이 제안은 명시적 만들기 버튼을 사용합니다.</p>`}<p id="form-error" class="footnote" role="alert"></p><div class="modal-footer">${btn('취소','cancel-create')}${btn(kind==='lyrics'?'만들고 편집':'만들기','submit-create',`data-kind="${kind}"`,'primary')}</div></form>`);$('#create-form').onsubmit=e=>{e.preventDefault();submitCreate(kind)}}
function submitCreate(kind){const title=$('#new-title').value;if(!title.trim()){ $('#form-error').textContent='제목을 입력해 주세요.';return}let item={id:uid(kind[0]),title,body:$('#new-body')?.value||'',favorite:!!$('#new-favorite')?.checked,pin:!!$('#new-pin')?.checked};if(kind==='songs')Object.assign(item,{status:$('#new-status').value,note:$('#new-note').value,color:$('#new-color').value,links:[],model:''});if(kind==='lyrics'){const sid=$('#new-song')?.value;if(!sid){$('#form-error').textContent='먼저 곡을 만들어 주세요.';return}Object.assign(item,{song:sid,body:find('templates',$('#new-lyric-template').value)?.body||'',status:'아이디어',note:''})}if(kind==='rhymes')Object.assign(item,{tags:[],songs:[],color:''});if(kind==='prompts')Object.assign(item,{mode:$('#new-mode').value,tags:item.body.split(',').map(t=>t.trim()).filter(Boolean),songs:[]});db[kind].unshift(item);persist();navigate({songs:'song',lyrics:'lyric',rhymes:'rhyme',prompts:'prompt'}[kind],item.id);toast('새 합성 자료를 기기에 만들었어요.')}
function history(kind){let item=find(kind),list=db.history[kind+':'+item.id]||[];if(!list.length)list=[{at:'2026.09.21 18:42 · 첫 스케치',value:{...structuredClone(item),body:kind==='prompts'?'A warm and quiet arrangement.':item.body.replace(/빛/g,'꿈')}}];modal('수정 기록 · 현재와 비교',`<div class="tabs">${btn('수정 기록','history','data-kind="'+kind+'"','small')}${kind==='lyrics'?btn('이름 있는 가사 버전','version-list','','small'):''}</div><label>기록 선택<select id="history-select">${list.map((x,i)=>`<option value="${i}">${esc(x.at)}</option>`).join('')}</select></label><div class="two" style="margin-top:20px"><div><h3>현재 본문</h3><pre class="diff">${esc(kind==='prompts'?plain(item):item.body)}</pre></div><div><h3>선택한 기록</h3><pre id="history-preview" class="diff old">${esc(kind==='prompts'?plain(list[0].value):list[0].value.body)}</pre></div></div><p class="notice">현재 내용을 기록으로 남긴 뒤 복원해요. ${kind==='prompts'?'제목·형식·내용 snapshot':'제목·메모'+(kind==='rhymes'?'·태그':'')+'는 유지하고 본문만'}${kind==='prompts'?'을':'을'} 복원합니다. 기기 안의 합성 이력입니다.</p><div class="modal-footer">${btn('취소','close')}${btn('현재 보존 후 복원','restore-history',`data-kind="${kind}"`,'primary')}</div>`);window.__historyList=list;$('#history-select').onchange=e=>$('#history-preview').textContent=kind==='prompts'?plain(list[e.target.value].value):list[e.target.value].value.body}
function sharing(){const l=find('lyrics');modal('이 가사 공유',`<div class="notice">가사 제목과 본문만 기본 공유됩니다. 비공개 메모·연결 자료·수정 기록은 포함되지 않아요. 아래는 합성 권한 시연입니다.</div><div class="row"><div><strong>내 공유 코드</strong><small>상대가 이 코드를 사용해 나를 허용할 수 있어요.</small></div>${btn('LC-DEMO-YUNA · 복사','copy-code','','small')}</div><div class="two"><label>공유할 계정 코드<input id="share-code" placeholder="LC-XXXX-XXXX"></label><label>기간<select id="share-period"><option>회수까지</option><option>7일</option><option>30일</option><option>90일</option></select></label></div><div class="actions" style="margin-top:12px">${btn('읽기 권한 부여','share-account','','blue small')}</div>${db.share.accounts.map((a,i)=>`<div class="row"><div><strong>공유 계정 ${i+1} · ${esc(a.code)}</strong><small>${esc(a.period)} · ${a.write?'공동 작성':'읽기 전용'}</small></div><div class="actions">${btn(a.write?'읽기로':'쓰기로','share-permission',`data-index="${i}"`,'small')}${btn('회수','share-revoke-account',`data-index="${i}"`,'small')}</div></div>`).join('')}<hr><h3>공개 링크</h3><p class="footnote">${db.share.public?'활성 합성 링크 · '+esc(db.share.period||'7일')+' · '+(db.share.guest?'게스트 작성 허용':'읽기 전용'):'아직 공개하지 않았어요.'}</p><div class="actions" style="margin-top:14px">${btn(db.share.public?'새 링크로 교체':'공개 범위 선택','public-create','','small')}${db.share.public?btn('공개 링크 회수','public-revoke','','small')+btn(db.share.guest?'게스트 쓰기 중지':'게스트 쓰기 허용','guest-toggle','','small'):''}</div><hr><h3>역할별 미리보기</h3><div class="chips" style="margin-top:12px">${[['owner','소유자'],['reader','계정 읽기'],['writer','계정 쓰기'],['public','공개 읽기'],['guest','게스트 쓰기'],['revoked','접근 종료']].map(([v,t])=>btn(t,'share-role',`data-value="${v}"`,'small')).join('')}</div><p class="footnote">참여자: 나 · 공동 작성자 1 · 게스트 02 (모의 상태)</p>`)}
function roleView(role){let l=find('lyrics'),can=['owner','writer','guest'].includes(role),labels={owner:'소유자',reader:'계정 읽기',writer:'계정 공동 작성',public:'공개 읽기',guest:'게스트 02 · 본문 작성',revoked:'접근할 수 없음'};modal(labels[role],role==='revoked'?`<p class="notice">이 가사에 접근할 수 없습니다. 종료 원인을 구별해 노출하지 않아요.</p><div class="actions" style="margin-top:20px">${btn('내 미전송 내용 복구','recovery','','primary')}${goBtn('내 작업 공간','home')}</div>`:`<div class="row"><div><strong>${esc(l.title)}</strong><small>${role==='public'?'공개된 정보만 표시':role==='guest'?'계정 없이 현재 탭에서 작성 · 2026.09.29까지':esc(db.profile.name)+'님의 공유 가사'}</small></div><span class="label-tag">${can?'본문 편집 가능':'읽기 전용'}</span></div><textarea id="shared-body" ${can?'':'readonly'} style="min-height:260px">${esc(l.body)}</textarea><p class="notice" style="margin-top:15px">${can?'연결 상태와 서버 저장은 다릅니다. 이 시연 입력은 전송되지 않으며 복구함에만 보관합니다.':'본문을 읽고 복사할 수 있습니다.'}${role==='guest'?' 게스트 복구는 현재 탭에 한정됩니다. 닫기 전 복사하거나 파일로 보관하세요.':''}</p><div class="modal-footer">${btn('본문 복사','copy-shared')}${can?btn('미전송 입력 보관','save-shared','','primary'):''}${btn('공유 관리로','share')}</div>`)}
function lab(){modal('시연 도구',`<p class="notice">합성 데이터와 이 브라우저의 localStorage만 사용합니다. API·서버·OAuth·실제 공유·CRDT 동기화·사진 처리는 연결하지 않았습니다.</p><h3 style="margin:20px 0 12px">상태와 복구 흐름</h3><div class="demo-grid">${[['normal','정상 목록'],['empty','빈 목록'],['loading','불러오는 중'],['error','조회 실패'],['offline','오프라인 초안'],['session','세션 만료'],['conflict','최신본 충돌'],['profile-failure','프로필 저장 실패'],['profile-partial','이름만 저장 성공'],['pwa','PWA / 업데이트'],['recovery','미전송 복구함'],['auth','로그인 / 가입'],['notfound','찾을 수 없음'],['withdraw','탈퇴 시연']].map(([v,t])=>btn(t,'demo-state',`data-value="${v}"`,'small')).join('')}</div><hr><div class="actions">${btn('207개 기능 계약 보기','coverage','','small')}${btn('도크 접기 / 펼치기','dock-size','','small')}${btn('합성 데이터 초기화','reset','','danger small')}</div><p class="footnote">홈 이어쓰기, 중앙 도크, 명시 생성 버튼은 이 디자인의 재배치·개선 제안입니다. 서버 기능의 완료를 뜻하지 않습니다.</p>`)}
function demo(value){if(['normal','empty','loading','error'].includes(value)){if(!['songs','rhymes','prompts'].includes(page))page='songs';demoState=value;close();render();return}if(value==='auth'){navigate('auth');return}if(value==='recovery'){recovery();return}if(value==='withdraw'){account('withdraw');return}if(value.startsWith('profile-')){demoState=value;modal(value==='profile-partial'?'이름만 저장된 경우':'프로필 저장 실패',`<p class="notice">${value==='profile-partial'?'합성 시나리오: 이름은 반영되었지만 사진 업로드가 실패했습니다. 선택 사진과 입력을 유지하며 사진만 다시 저장합니다.':'합성 시나리오: 변경한 이름과 선택한 사진이 유지됩니다. 다시 저장하거나 취소할 수 있습니다.'}</p><div class="modal-footer">${goBtn('프로필로 이동','settings')}${btn('시연 상태 적용','close','','primary')}</div>`);return}const configs={offline:['오프라인 · 기기에만 보관','네트워크 없이 작성한 본문은 기기 초안으로 남아 있어요. 연결 복구와 서버 저장 완료는 서로 다른 단계입니다.'],session:['세션이 만료됐어요','새 탭에서 다시 로그인한 뒤 이 창으로 돌아오세요. 현재 입력은 보존합니다.'],conflict:['최신 내용이 바뀌었어요','내 입력과 최신 버전을 비교한 뒤 다시 적용하세요. 변환 미리보기는 다시 만들며 원문을 버리지 않습니다.'],pwa:['설치와 업데이트','일반 웹 모드입니다. 미전송 입력이 있으면 준비된 업데이트를 보류합니다. 이 목업은 실제 PWA를 설치하지 않습니다.'],notfound:['페이지를 찾을 수 없어요','주소가 달라졌거나 이 화면에 접근할 수 없습니다.']};let [title,body]=configs[value]||configs.offline;modal(title,`<div class="notice">${body}</div>${value==='conflict'?`<div class="two" style="margin-top:20px"><pre class="diff">내 입력\n${esc(db.recovery)}</pre><pre class="diff old">최신 버전 (합성)\n다시 시작하는 작은 문장</pre></div>`:''}<div class="modal-footer">${btn('초안 파일 저장','download-draft')}${btn('현재 입력 복사','copy-recovery')}${value==='session'?btn('새 탭 로그인 안내','auth-in-new-tab'):''}${btn(value==='pwa'?'업데이트 보류하고 계속':'입력 유지하고 다시 시도','close','','primary')}</div>`)}
function recovery(){modal('내 미전송 내용 복구함',`<p class="notice">내가 썼지만 서버에 보내지 못한 내용만 표시합니다. 다시 허용되어도 자동 재전송하지 않아요.</p><textarea id="recovery-text" style="margin-top:20px" readonly>${esc(db.recovery||'보관된 내용이 없어요.')}</textarea><p class="footnote">기기 초안 · 현재 탭의 게스트 내용 · 수정 기록 · 휴지통은 서로 다른 복구 경로입니다.</p><div class="modal-footer">${btn('내용 복사','copy-recovery')}${btn('파일 저장','download-draft')}${btn('보관함에서 제거','clear-recovery','','danger')}</div>`)}
function account(mode=''){modal(mode==='withdraw'?'탈퇴 검토':'계정과 내 자료',mode==='withdraw'?`<p class="notice warning">합성 탈퇴 시연: 요청 즉시 세션과 자료 접근이 차단되며 7일 안에 철회할 수 있습니다. 실제 계정·서버 자료를 삭제하지 않습니다.</p><div class="stack" style="margin-top:20px">${btn('Google 재인증 (시연)','reauth')}<label class="check"><input id="withdraw-check" type="checkbox">전체 내보내기를 하지 않았을 때의 결과를 이해했어요.</label><label>확인을 위해 ‘탈퇴’ 입력<input id="withdraw-text"></label></div><div class="modal-footer">${btn('취소','close')}${btn('탈퇴 요청 시연','withdraw-submit','','danger')}</div>`:`<div class="modal-list"><button data-action="export"><span>⇩</span><div><strong>전체 자료 내보내기</strong><small>실제 서버 ZIP 내보내기의 범위 안내</small></div></button><button data-action="recovery"><span>▧</span><div><strong>기기 미전송 초안</strong><small>지금 보내지 못한 텍스트를 별도 보관</small></div></button><button data-action="logout"><span>↗</span><div><strong>로그아웃</strong><small>미전송 입력 확인 후 진행</small></div></button><button data-action="withdraw"><span>⊖</span><div><strong>계정 탈퇴 검토</strong><small>7일 철회 기간과 자료 접근 차단</small></div></button></div>`)}
function enableDrag(){if(!window.Draggable||page!=='prompt'||!$('#prompt-tags'))return;Draggable.create('#prompt-tags .tag',{type:'x',trigger:'.handle',onDragEnd(){const from=Number(this.target.dataset.tagIndex),delta=this.x>25?1:this.x< -25?-1:0;if(delta){const p=find('prompts'),to=Math.max(0,Math.min(p.tags.length-1,from+delta));checkpoint('prompts',p);[p.tags[from],p.tags[to]]=[p.tags[to],p.tags[from]];persist();render()}else gsap.to(this.target,{x:0,duration:.2})}})}
function bindForm(){const input=(id,fn)=>{const el=$('#'+id);if(el)el.oninput=fn};const change=(id,fn)=>{const el=$('#'+id);if(el)el.onchange=fn};input('list-search',e=>{filter=e.target.value;refreshPreserving('list-search')});change('sort',e=>{sort=e.target.value;render()});change('status-filter',e=>{statusFilter=e.target.value;render()});change('song-filter',e=>{songFilter=e.target.value;render()});input('search-input',e=>{filter=e.target.value;refreshPreserving('search-input')});change('template-source',e=>{templateSource=e.target.value;render()});change('template-sort',e=>{db.templates.sort((a,b)=>e.target.value==='이름순'?a.title.localeCompare(b.title):Number(b.favorite)-Number(a.favorite));render()});change('trash-kind',e=>$$('[data-trash-kind]').forEach(n=>n.classList.toggle('hidden',e.target.value&&n.dataset.trashKind!==e.target.value)));input('resource-search',e=>{resourceQueries[resourceTab]=e.target.value;let cursor=e.target.selectionStart;$('#resources').innerHTML=`<div class="module-top"><h3>작업 자료</h3>${btn('접기','hide-resources','','small')}</div>`+resourcePanel(find('lyrics'));bindForm();$('#resource-search').focus();$('#resource-search').setSelectionRange(cursor,cursor)});
const auto=(id,kind,prop)=>input(id,e=>{let x=find(kind);if(e.isComposing){return}if(!e.target.dataset.checkpoint){checkpoint(kind,x);e.target.dataset.checkpoint='1'}x[prop]=e.target.value;persist();if(kind==='lyrics'){$('#char-count').textContent=[...x.body].length+'자';$('#save-state').textContent='기기에 보관 · 로컬 시연'}if(kind==='rhymes')$('#rhyme-count').textContent=[...x.body].length+'자 · 기기에 보관';if(kind==='prompts'){$('#prompt-preview').textContent=plain(x);$('#prompt-count').textContent=[...plain(x)].length+'자'}});
auto('lyric-title','lyrics','title');auto('lyric-body','lyrics','body');auto('rhyme-title','rhymes','title');auto('rhyme-body','rhymes','body');auto('prompt-title','prompts','title');auto('prompt-body','prompts','body');if($('#lyric-body'))['select','click','keyup'].forEach(ev=>$('#lyric-body').addEventListener(ev,e=>selection={start:e.target.selectionStart,end:e.target.selectionEnd}));input('profile-name',e=>{profileDraft.name=e.target.value;$('#profile-result').textContent='아직 저장하지 않은 변경이 있어요.'});change('photo-input',e=>{const f=e.target.files?.[0];if(!f)return;if(!['image/png','image/jpeg','image/webp'].includes(f.type)||f.size>2*1024*1024){$('#profile-result').textContent='PNG/JPEG/WebP, 2MiB 이하 사진을 선택해 주세요.';e.target.value='';return}const r=new FileReader;r.onload=()=>{profileDraft.photo=r.result;$('#draft-photo').style.backgroundImage=`url("${r.result}")`;$('#draft-photo').textContent='';$('#profile-result').textContent=f.name+' · 미저장 미리보기'};r.readAsDataURL(f)});change('theme-select',e=>{displayDraft.theme=e.target.value;theme(e.target.value)});['font','size','line','space'].forEach(k=>input('display-'+k,e=>{displayDraft[k]=k==='font'?e.target.value:Number(e.target.value);const p=$('#setting-preview');p.style.fontFamily=displayDraft.font;p.style.fontSize=displayDraft.size+'px';p.style.lineHeight=displayDraft.line;p.style.letterSpacing=displayDraft.space+'em'}));change('rhyme-color',e=>{find('rhymes').color=e.target.value;persist();toast('이 노트의 색상을 기기에 보관했어요.')})}
function refreshPreserving(id){const p=$('#'+id).selectionStart;const scroll=window.scrollY;render();$('#'+id).focus();$('#'+id).setSelectionRange(p,p);window.scrollTo(0,scroll)}
function download(text,name='lyricscloud-local-draft.txt'){const blob=new Blob([text],{type:'text/plain;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast('합성 초안 텍스트 파일을 준비했어요.')}
document.addEventListener('click',async e=>{const el=e.target.closest('[data-go],[data-action]');if(!el)return;if(el.dataset.go){navigate(el.dataset.go,el.dataset.id);return}const a=el.dataset.action,k=el.dataset.kind,id=el.dataset.id,v=el.dataset.value,i=Number(el.dataset.index),dir=Number(el.dataset.dir);switch(a){
case 'close':close();break;
case 'theme':theme(document.body.classList.contains('dark')?'light':'dark');if(displayDraft)displayDraft.theme=themePreview;toast('테마 미리보기 · 설정에서 별도로 저장할 수 있어요.');break;
case 'more':modal('모든 공간',`<div class="demo-grid">${menus.map(([p,ic,l])=>goBtn(ic+' '+l,p)).join('')}</div><hr><div class="actions">${btn('빠른 추가','quick')}${btn('개인 공간','account')}${btn('단축키','help')}${btn('로그아웃','logout')}${btn('시연 도구','lab')}</div>`);break;
case 'dock-size':$('#dock').classList.toggle('compact');toast('도크 이름은 마우스 도움말과 접근성 이름으로도 유지됩니다.');break;
case 'help':modal('키보드로 빠르게',`<input id="help-search" aria-label="단축키 검색" placeholder="명령 또는 키 검색"><div id="help-rows">${[['Ctrl / ⌘ + K','통합 검색'],['Alt + N','빠른 추가'],['Escape','현재 대화상자 닫기'],['Tab / Shift + Tab','다음 / 이전 컨트롤'],['프롬프트 태그 ← → 버튼','선택 태그 순서 이동']].map(([key,text])=>`<div class="row"><strong>${text}</strong><small>${key}</small></div>`).join('')}</div>`);$('#help-search').oninput=e=>{$$('#help-rows .row').forEach(n=>n.classList.toggle('hidden',!n.textContent.toLowerCase().includes(e.target.value.toLowerCase())))};break;
case 'lab':lab();break;case 'demo-state':demo(v);break;
case 'coverage':modal('207개 기능 · 계약 탐색',`<p class="notice">V 시각 · I 상호작용 · C 내용 계약 · R 실제 시스템 필요. 이 탐색기는 누락된 세부 상태도 계약으로 공개합니다. 모든 항목이 실제 제품 수준으로 구현된 것은 아닙니다.</p><input id="feature-search" placeholder="ID, 화면, 기능 검색" aria-label="기능 계약 검색" style="margin-top:17px"><div id="feature-rows">${(window.FEATURES||[]).map(f=>`<details class="feature-row"><summary>${f.id} · ${esc(f.name)}</summary><p>${esc(f.requirement)}</p><p>시연 수준: ${esc(f.level)} · ${esc(f.location)}</p></details>`).join('')}</div>`);$('#feature-search').oninput=e=>$$('.feature-row').forEach(n=>n.classList.toggle('hidden',!n.textContent.toLowerCase().includes(e.target.value.toLowerCase())));break;
case 'quick':modal('새로운 조각',`<div class="modal-list">${[['songs','♫','새 곡','가사와 자료를 모으는 작업 공간'],['lyrics','▤','새 가사','곡을 선택해 새 버전 작성'],['rhymes','≋','새 라임 노트','단어와 운율을 자유롭게 기록'],['prompts','✧','새 프롬프트','태그나 문장으로 사운드 설계']].map(([kind,icon,t,d])=>`<button data-action="create" data-kind="${kind}"><span>${icon}</span><div><strong>${t}</strong><small>${d}</small></div></button>`).join('')}</div><div class="modal-footer">${btn('빠른 아이디어 담기','idea')}</div>`);break;
case 'create':create(k);break;case 'submit-create':submitCreate(k);break;
case 'cancel-create':if($('#new-title')?.value||$('#new-body')?.value){const prior=snapshotModal();modal('이 초안을 어떻게 할까요?',`<p>계속 작성하거나 이 합성 초안을 버리고 나갈 수 있어요.</p><div class="modal-footer">${btn('계속 작성','restore-form')}${btn('초안 삭제 후 나가기','close','','danger')}</div>`);window.__restoreForm=prior}else close();break;
case 'restore-form':$('#modal-content').innerHTML=window.__restoreForm;break;
case 'idea':modal('빠른 아이디어',`<div class="stack"><label>자료 유형<select id="idea-kind"><option value="rhymes">라임 노트</option><option value="prompts">프롬프트 · 문장형</option></select></label><label>떠오른 한 줄<textarea id="idea-body" placeholder="아직 다듬지 않아도 괜찮아요."></textarea></label><small>현재 문맥: ${esc(find('songs',current)?.title||'개인 공간')} · 기기 초안으로 보관</small><div id="idea-error" role="alert"></div><div class="modal-footer">${btn('취소','close')}${btn('아이디어 담기','save-idea','','primary')}</div></div>`);break;
case 'save-idea':{const body=$('#idea-body').value;if(!body.trim()){$('#idea-error').textContent='한 줄을 적어 주세요.';break}const kind=$('#idea-kind').value,x={id:uid('i'),title:body.split('\n')[0].slice(0,30),body,tags:[],songs:[],favorite:false,pin:false,...(kind==='prompts'?{mode:'sentence'}:{color:''})};db[kind].unshift(x);persist();close();toast('아이디어를 합성 기기 초안에 담았어요.');break}
case 'view':db.views[page]=v;persist();render();break;
case 'tag-filter':tagFilter=v==='전체'?'':v;render();break;
case 'status-filter':statusFilter=v;render();break;
case 'clear-filter':filter='';statusFilter='전체';tagFilter='';songFilter='';render();break;
case 'retry':demoState='normal';render();break;
case 'favorite':case 'pin':{const x=find(k,id);x[a]=!x[a];persist();render();toast(a==='pin'?'핀 목록을 바꿨어요.':'즐겨찾기를 바꿨어요.');break}
case 'move-item':{const ar=db[k],ix=ar.findIndex(x=>x.id===id),to=Math.max(0,Math.min(ar.length-1,ix+dir));[ar[ix],ar[to]]=[ar[to],ar[ix]];persist();render();break}
case 'copy-item':{const x=find(k,id);await copy(k==='prompts'?plain(x):x.body);break}
case 'copy-suno':await copy(sunoText(find('lyrics').body));break;
case 'select-copy':$('#manual-copy').select();break;
case 'copy-selection':{const t=$('#'+el.dataset.source);if(t.selectionStart===t.selectionEnd){toast('먼저 복사할 부분을 선택해 주세요.');break}await copy(t.value.slice(t.selectionStart,t.selectionEnd));break}
case 'note':{const x=find(k,id);modal(k==='songs'?'곡 작업 메모':'가사 작업 메모',`<label>메모<textarea id="note-body">${esc(x.note||'')}</textarea></label><div class="modal-footer">${btn('메모 삭제','delete-note',`data-kind="${k}" data-id="${id}"`,'danger')}${btn('취소','close')}${btn('메모 저장','save-note',`data-kind="${k}" data-id="${id}"`,'primary')}</div>`);break}
case 'save-note':find(k,id).note=$('#note-body').value;persist();close();render();break;
case 'delete-note':find(k,id).note='';persist();close();render();break;
case 'edit-song':{const s=find('songs');modal('곡 정보 수정',`<div class="stack"><label>곡 제목<input id="edit-song-title" value="${esc(s.title)}"></label><label>설명<textarea id="edit-song-body">${esc(s.body)}</textarea></label><label>작업 상태<select id="edit-song-status">${['아이디어','가사 작성 중','수정 중','Suno 생성 중','믹싱 중','완성','보류'].map(x=>`<option ${x===s.status?'selected':''}>${x}</option>`).join('')}</select></label><label>색상<input id="edit-song-color" type="color" value="${s.color}"></label><div class="modal-footer">${btn('취소','close')}${btn('변경 저장','save-song','','primary')}</div></div>`);break}
case 'save-song':{if(!$('#edit-song-title').value.trim()){toast('제목을 입력해 주세요.');break}const s=find('songs');Object.assign(s,{title:$('#edit-song-title').value,body:$('#edit-song-body').value,status:$('#edit-song-status').value,color:$('#edit-song-color').value});persist();close();render();break}
case 'song-resource':resourceTab=v;render();break;
case 'links':links(k);break;
case 'apply-links':{const checked=$$('[data-link-kind]:checked');if(page==='song'){['rhymes','prompts'].forEach(kind=>db[kind].forEach(x=>{const c=checked.some(n=>n.value===x.id);x.songs=x.songs.filter(s=>s!==current);if(c)x.songs.push(current)}))}else find(k).songs=checked.map(n=>n.value);persist();close();render();toast('원본을 유지하고 곡 연결만 바꿨어요.');break}
case 'save-model':find('songs').model=$('#suno-model').value==='사용자 지정'?$('#custom-model').value:$('#suno-model').value;persist();toast('사용 모델 기록을 기기에 보관했어요.');break;
case 'suno-link':{const s=find('songs'),link=el.dataset.index!==undefined?s.links[i]:null;modal(link?'Suno 링크 수정':'Suno 링크 추가',`<div class="stack"><label>URL<input id="link-url" type="url" value="${esc(link?.url||'')}" placeholder="https://suno.com/song/…"></label><label>표시 제목<input id="link-title" value="${esc(link?.title||'')}"></label><label>메모<textarea id="link-note">${esc(link?.note||'')}</textarea></label><p class="footnote">직접 보관하는 URL과 메모예요. 원곡은 변경하지 않아요.</p><div class="modal-footer">${btn('취소','close')}${btn('링크 저장','save-suno-link',link?`data-index="${i}"`:'','primary')}</div></div>`);break}
case 'save-suno-link':{let url=$('#link-url').value;try{if(!['http:','https:'].includes(new URL(url).protocol))throw 0}catch{toast('http 또는 https URL을 입력해 주세요.');break}const l={url,title:$('#link-title').value||'새 링크',note:$('#link-note').value};if(el.dataset.index!==undefined)find('songs').links[i]=l;else find('songs').links.push(l);persist();close();render();break}
case 'suno-move':{const ls=find('songs').links;[ls[i],ls[i+dir]]=[ls[i+dir],ls[i]];persist();render();break}
case 'suno-remove':modal('작업 링크 제거',`<p>LyricsCloud의 링크·제목·메모만 제거합니다. Suno 원곡은 삭제되지 않아요.</p><div class="modal-footer">${btn('취소','close')}${btn('링크 제거','confirm-suno-remove',`data-index="${i}"`,'danger')}</div>`);break;
case 'confirm-suno-remove':find('songs').links.splice(i,1);persist();close();render();break;
case 'suno-open':modal('합성 Suno 링크',`<p>이 목업의 링크는 합성 예시입니다. 외부 네트워크를 호출하지 않아요.</p><pre class="preview">${esc(find('songs').links[i].url)}</pre><div class="modal-footer">${btn('닫기','close')}</div>`);break;
case 'focus':focus=!focus;$('#app').classList.toggle('focus-mode',focus);el.textContent=focus?'집중 종료':'집중 모드';break;
case 'resources':$('#resources')?.classList.remove('hidden');$('#resources')?.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth',block:'start'});break;
case 'hide-resources':$('#resources').classList.add('hidden');toast('상단의 ‘자료 4탭’에서 다시 열 수 있어요.');break;
case 'resource-tab':resourceTab=v;render();break;
case 'resource-scope':resourceScope=v;render();break;
case 'jump-section':{const t=$('#lyric-body');t.focus();t.setSelectionRange(i,i);selection={start:i,end:i};t.scrollTop=Math.max(0,find('lyrics').body.slice(0,i).split('\n').length*35-50);break}
case 'outline':modal('송폼 목차',`${sections(find('lyrics').body).map(s=>`<label class="check" style="margin:13px 0"><input type="checkbox" data-modal-section="${s.n}">${esc(s.title)} · ${s.n+1}번째</label>`).join('')}<div class="modal-footer">${btn('선택 복사','copy-sections')}${btn('송폼 삽입','songform')}</div>`);break;
case 'copy-sections':{const ns=$$('#modal[open] [data-modal-section]:checked,#app [data-section]:checked').map(n=>Number(n.dataset.modalSection??n.dataset.section));if(!ns.length){toast('복사할 구간을 먼저 선택해 주세요.');break}await copy(sections(find('lyrics').body).filter(s=>ns.includes(s.n)).map(s=>s.text).join(''));break}
case 'songform':modal('송폼 삽입',`<p class="muted">가사에서 마지막으로 선택한 커서 위치에 삽입합니다.</p><div class="demo-grid" style="margin-top:20px">${['Intro','Verse','Pre-Chorus','Chorus','Hook','Bridge','Outro'].map(t=>btn(t,'insert-songform',`data-value="${t}"`)).join('')}</div>`);break;
case 'insert-songform':insert('\n['+v+']\n');break;
case 'insert-rhyme':insert(find('rhymes',id).body);break;
case 'select-rhyme':modal('라임 선택 삽입',`<p class="notice">대상: ${esc(find('lyrics').title)} · 선택한 본문만 커서 위치에 넣습니다.</p><label style="margin-top:18px">삽입할 원문 부분을 선택하세요<textarea id="rhyme-selection" style="min-height:180px">${esc(find('rhymes',id).body)}</textarea></label><div class="modal-footer">${btn('취소','close')}${btn('선택 영역 삽입','insert-selected-rhyme','','primary')}</div>`);break;
case 'insert-selected-rhyme':{const t=$('#rhyme-selection');if(t.selectionStart===t.selectionEnd){toast('삽입할 원문 부분을 선택해 주세요.');break}insert(t.value.slice(t.selectionStart,t.selectionEnd));break}
case 'rhyme-target':modal('삽입할 가사 선택',`<p class="notice">명시한 가사의 끝에 이 라임 전체를 추가합니다. 다른 탭의 최근 가사를 추정하지 않아요.</p><label style="margin-top:18px">가사 대상<select id="target-lyric">${db.lyrics.map(l=>`<option value="${l.id}">${esc(l.title)}</option>`).join('')}</select></label><pre class="preview">${esc(find('rhymes').body)}</pre><div class="modal-footer">${btn('취소','close')}${btn('이 가사 끝에 삽입','insert-target-rhyme','','primary')}</div>`);break;
case 'insert-target-rhyme':{const text=find('rhymes').body,target=$('#target-lyric').value;insert('\n'+text,target);break}
case 'rhyme-tag-add':{const value=$('#rhyme-tag').value.trim();if(value){const r=find('rhymes');if(!r.tags.includes(value))r.tags.push(value);persist();render()}break}
case 'rhyme-tag-remove':find('rhymes').tags.splice(i,1);persist();render();break;
case 'prompt-mode':find('prompts').mode=v;persist();render();toast('저장된 '+(v==='tags'?'태그':'문장')+' 표현을 열었어요. 자동 변환하지 않았습니다.');break;
case 'prompt-tag-add':{const p=find('prompts'),values=$('#prompt-tag').value.split(',').map(x=>x.trim()).filter(Boolean);if(values.length){checkpoint('prompts',p);p.tags.push(...values);persist();render()}break}
case 'tag-move':{const p=find('prompts');checkpoint('prompts',p);[p.tags[i],p.tags[i+dir]]=[p.tags[i+dir],p.tags[i]];persist();render();break}
case 'prompt-tag-remove':checkpoint('prompts',find('prompts'));find('prompts').tags.splice(i,1);persist();render();break;
case 'dedupe':{const p=find('prompts');checkpoint('prompts',p);const seen=new Set;p.tags=p.tags.filter(t=>{const key=t.toLowerCase();if(seen.has(key))return false;seen.add(key);return true});persist();render();toast('정리 전 기록을 남기고 첫 표시값을 보존했어요.');break}
case 'convert':{const p=find('prompts'),text=plain(p),out=p.mode==='tags'?p.tags.join(', '):p.body.split(/[,.\n]+/).map(x=>x.trim()).filter(Boolean).join(', ');previewRevision=JSON.stringify(p);modal('구조 변환 미리보기',`<div class="two"><div><h3>현재 원문</h3><pre class="preview">${esc(text)}</pre></div><div><h3>${p.mode==='tags'?'문장형':'태그형'}으로 변환</h3><pre class="preview">${esc(out)}</pre></div></div><p class="notice">확인하기 전에는 원문을 바꾸지 않아요. 변환 후에도 undo로 원래 형식을 되돌릴 수 있어요.</p><div class="modal-footer">${btn('원문 유지','close')}${btn('확인하고 변환','confirm-convert','','primary')}</div>`);break}
case 'confirm-convert':{let p=find('prompts');if(JSON.stringify(p)!==previewRevision){toast('원문이 바뀌어 미리보기를 다시 만들어야 해요.');break}promptUndo=structuredClone(p);checkpoint('prompts',p);if(p.mode==='tags'){p.body=p.tags.join(', ');p.mode='sentence'}else{p.tags=p.body.split(/[,.\n]+/).map(x=>x.trim()).filter(Boolean);p.mode='tags'}persist();close();render();break}
case 'undo-convert':if(promptUndo){db.prompts[db.prompts.findIndex(p=>p.id===current)]=promptUndo;promptUndo=null;persist();render();toast('변환 전 원문과 형식을 복원했어요.')}break;
case 'append-template':{const p=find('prompts');modal('같은 형식 템플릿 뒤에 추가',`<p class="notice">현재 ${p.mode==='tags'?'태그형':'문장형'} 내용은 유지하고 같은 형식의 템플릿을 끝에 추가해요. 다른 형식은 먼저 명시적으로 변환해야 합니다.</p><div class="modal-list" style="margin-top:18px">${db.templates.filter(t=>t.type==='prompt').map(t=>`<button data-action="append-template-confirm" data-id="${t.id}"><div><strong>${esc(t.title)} · ${t.mode==='tags'?'태그형':'문장형'} · ${t.source}</strong><small>${esc(t.body)}</small><small>${t.mode!==p.mode?'다른 형식 · 적용 불가':'현재 끝에 추가'}</small></div></button>`).join('')}</div>`);break}
case 'append-template-confirm':{const p=find('prompts'),t=find('templates',id);if(t.mode!==p.mode){toast('다른 형식입니다. 원문은 유지되며 적용하지 않았어요.');break}checkpoint('prompts',p);if(p.mode==='tags')p.tags.push(...t.body.split(',').map(s=>s.trim()).filter(Boolean));else p.body+='\n'+t.body;persist();close();render();toast('기존 원문을 유지하고 끝에 추가했어요.');break}
case 'history':history(k);break;
case 'restore-history':{const item=find(k),old=window.__historyList[Number($('#history-select').value)].value;checkpoint(k,item);if(k==='prompts')Object.assign(item,{title:old.title,body:old.body,mode:old.mode,tags:structuredClone(old.tags)});else item.body=old.body;persist();close();render();toast('현재 내용을 보존한 뒤 선택 기록을 복원했어요.');break}
case 'version-list':{let l=find('lyrics');modal('이름 있는 가사 버전',`<div class="modal-list">${db.lyrics.filter(x=>x.song===l.song).map(x=>`<button data-go="lyric" data-id="${x.id}"><div><strong>${esc(x.title)}</strong><small>독립된 가사 · ${esc(x.status)}</small></div></button>`).join('')}</div><p class="footnote">수정 기록과 달리 각 버전은 독립 가사예요.</p>`);break}
case 'lyric-meta':{const l=find('lyrics');modal('현재 가사 설정',`<label>작업 상태<select id="lyric-status">${['아이디어','가사 작성 중','수정 중','완성','보류'].map(x=>`<option ${l.status===x?'selected':''}>${x}</option>`).join('')}</select></label><div class="actions" style="margin-top:18px">${btn(l.favorite?'★ 즐겨찾기 해제':'☆ 즐겨찾기','favorite',`data-kind="lyrics" data-id="${l.id}"`)}${btn(l.pin?'◆ 핀 해제':'◇ 핀','pin',`data-kind="lyrics" data-id="${l.id}"`)}${btn('작업 메모','note',`data-kind="lyrics" data-id="${l.id}"`)}${btn('표시 설정','display')}${btn('복제','duplicate',`data-kind="lyrics" data-id="${l.id}"`)}${btn('삭제','delete',`data-kind="lyrics" data-id="${l.id}"`,'danger')}</div><div class="modal-footer">${btn('상태 저장','save-lyric-meta','','primary')}</div>`);break}
case 'save-lyric-meta':find('lyrics').status=$('#lyric-status').value;persist();close();render();break;
case 'display':{const d=find('lyrics').display||db.display;modal('이 가사의 표시 설정',`<p class="notice">이 가사의 표시만 바꿉니다. 원문과 계정 기본값은 유지해요.</p><div class="stack" style="margin-top:20px"><label>글꼴<select id="lyric-font"><option value="sans-serif">산세리프</option><option value="serif">세리프</option><option value="monospace">고정폭</option></select></label><div class="three"><label>글자 크기 14–28<input id="lyric-size" type="number" min="14" max="28" value="${d.size}"></label><label>줄 간격 1.2–2.4<input id="lyric-line" type="number" min="1.2" max="2.4" step=".1" value="${d.line}"></label><label>자간 −.05–.2<input id="lyric-space" type="number" min="-.05" max=".2" step=".01" value="${d.space}"></label></div><p id="lyric-display-preview" class="setting-preview">한 줄의 노래, a little light. 小さな光 ✦</p></div><div class="modal-footer">${btn('계정 기본값으로','lyric-display-reset')}${btn('취소','close')}${btn('이 가사에 저장','lyric-display-save','','primary')}</div>`);['font','size','line','space'].forEach(z=>$('#lyric-'+z).oninput=()=>{let q=$('#lyric-display-preview');q.style.fontSize=$('#lyric-size').value+'px';q.style.lineHeight=$('#lyric-line').value;q.style.letterSpacing=$('#lyric-space').value+'em';q.style.fontFamily=$('#lyric-font').value});break}
case 'lyric-display-reset':delete find('lyrics').display;persist();close();render();break;
case 'lyric-display-save':find('lyrics').display={font:$('#lyric-font').value,size:Math.max(14,Math.min(28,Number($('#lyric-size').value))),line:Math.max(1.2,Math.min(2.4,Number($('#lyric-line').value))),space:Math.max(-.05,Math.min(.2,Number($('#lyric-space').value)))};persist();close();render();break;
case 'duplicate':{const x=structuredClone(find(k,id));x.id=uid('d');x.title+=' · 복제';if(k==='templates')x.source='내';db[k].unshift(x);persist();close();if(k==='templates'){render();toast('내 템플릿에 복제했어요.')}else navigate({songs:'song',lyrics:'lyric',rhymes:'rhyme',prompts:'prompt'}[k],x.id);break}
case 'delete':modal('휴지통으로 이동',`<p>‘${esc(find(k,id).title)}’을 휴지통에 넣을까요? 30일 동안 복원할 수 있어요.</p><p class="footnote">${k==='songs'?'소속 가사도 함께 숨겨집니다. 독립 라임·프롬프트 원본은 유지해요.':'원문과 연결을 숨기고 복원할 때 되살립니다.'}</p><div class="modal-footer">${btn('취소','close')}${btn('휴지통으로 이동','confirm-delete',`data-kind="${k}" data-id="${id}"`,'danger')}</div>`);break;
case 'confirm-delete':{const ar=db[k],x=find(k,id);db.trash.unshift({...x,kind:k,date:'2026.09.22',days:30});ar.splice(ar.indexOf(x),1);if(k==='songs'){db.lyrics.filter(l=>l.song===id).forEach(l=>db.trash.unshift({...l,kind:'lyrics',date:'2026.09.22',days:30}));db.lyrics=db.lyrics.filter(l=>l.song!==id)}persist();navigate(k==='lyrics'?'songs':k);break}
case 'template-type':templateType=v;render();break;
case 'template-start':{const t=find('templates',id);create(t.type==='lyric'?'lyrics':'prompts',id);break}
case 'new-template':{const t=id?find('templates',id):null;modal(t?'내 템플릿 수정':'새 템플릿',`<div class="stack"><label>제목<input id="template-title" value="${esc(t?.title||'')}"></label><label>자료 유형<select id="template-kind"><option value="lyric" ${t?.type==='lyric'?'selected':''}>가사 구조</option><option value="prompt" ${t?.type==='prompt'?'selected':''}>프롬프트</option></select></label><label>프롬프트 형식<select id="template-mode"><option value="tags" ${t?.mode==='tags'?'selected':''}>태그형</option><option value="sentence" ${t?.mode==='sentence'?'selected':''}>문장형</option></select></label><label>원문<textarea id="template-body">${esc(t?.body||'')}</textarea></label><p class="footnote">형식 선택을 바꿔도 입력 원문은 바뀌지 않아요.</p><div class="modal-footer">${btn('취소','cancel-template')}${btn('저장','save-template',id?`data-id="${id}"`:'','primary')}</div></div>`);break}
case 'cancel-template':if($('#template-title').value||$('#template-body').value){const h=snapshotModal();modal('작성 중인 템플릿',`<p>작성 중인 입력을 버리고 나갈까요?</p><div class="modal-footer">${btn('계속 작성','restore-form')}${btn('버리고 닫기','close','','danger')}</div>`);window.__restoreForm=h}else close();break;
case 'save-template':{const title=$('#template-title').value;if(!title.trim()){toast('제목을 입력해 주세요.');break}const t={id:id||uid('t'),title,type:$('#template-kind').value,mode:$('#template-mode').value,body:$('#template-body').value,source:'내',favorite:false};if(id)Object.assign(find('templates',id),t);else db.templates.push(t);templateType=t.type;persist();close();render();break}
case 'delete-template':modal('내 템플릿 삭제',`<p>이 합성 템플릿을 삭제할까요? 기존 자료 본문에는 영향을 주지 않아요.</p><div class="modal-footer">${btn('취소','close')}${btn('삭제','confirm-template-delete',`data-id="${id}"`,'danger')}</div>`);break;
case 'confirm-template-delete':db.templates=db.templates.filter(t=>t.id!==id);persist();close();render();break;
case 'search-kind':searchKind=v;render();break;
case 'clear-search':filter='';render();break;
case 'recent-search':filter=v;render();break;
case 'clear-recent-search':db.recentSearch=[];persist();render();break;
case 'remove-recent-search':db.recentSearch=db.recentSearch.filter(x=>x!==v);persist();render();break;
case 'favorite-scope':favoriteScope=v;render();break;
case 'trash-select':{const boxes=$$('[data-trash]').filter(n=>!n.closest('.row').classList.contains('hidden')),checked=boxes.every(n=>n.checked);boxes.forEach(n=>n.checked=!checked);break}
case 'trash-restore':case 'trash-delete':{const ids=id?[id]:$$('[data-trash]:checked').map(n=>n.dataset.trash);if(!ids.length){toast('먼저 자료를 선택해 주세요.');break}window.__trashIds=ids;const items=db.trash.filter(x=>ids.includes(x.id)),rest=a==='trash-restore',word=items.length===1?items[0].title:'완전 삭제';modal(rest?'자료 복원':'완전 삭제 확인',`<p>${items.map(x=>esc(x.title)).join(', ')}</p>${rest?`<p class="notice" style="margin-top:17px">삭제된 부모 곡이 있으면 함께 복원하거나 활성 곡으로 옮겨야 해요.</p><label style="margin-top:17px">복원 위치<select id="restore-target"><option value="parent">부모 곡과 함께 복원</option>${db.songs.map(s=>`<option value="${s.id}">${esc(s.title)}로 이동</option>`).join('')}</select></label>`:`<p class="notice warning" style="margin-top:17px">합성 자료를 완전히 삭제합니다. 되돌릴 수 없어요.</p><label style="margin-top:17px">‘${esc(word)}’ 입력<input id="delete-confirm" data-word="${esc(word)}"></label>`}<div class="modal-footer">${btn('취소','close')}${btn(rest?'복원':'완전 삭제',rest?'confirm-restore':'confirm-trash-delete','',''+(rest?'primary':'danger'))}</div>`);break}
case 'confirm-restore':{let ids=window.__trashIds,target=$('#restore-target').value;if(target==='parent'){const parents=db.trash.filter(x=>ids.includes(x.id)&&x.song).map(x=>x.song);ids=[...new Set([...ids,...parents])];const songs=db.trash.filter(x=>ids.includes(x.id)&&x.kind==='songs').map(x=>x.id);ids=[...new Set([...ids,...db.trash.filter(x=>songs.includes(x.song)).map(x=>x.id)])]}const items=db.trash.filter(x=>ids.includes(x.id));items.forEach(x=>{let kind=x.kind;const y={...x};delete y.kind;delete y.date;delete y.days;if(kind==='lyrics'&&target!=='parent')y.song=target;if(kind==='songs')Object.assign(y,{status:y.status||'아이디어',note:y.note||'',links:y.links||[],color:y.color||'#b7e5d4'});if(kind==='lyrics')Object.assign(y,{status:y.status||'아이디어',note:y.note||''});db[kind].unshift(y)});db.trash=db.trash.filter(x=>!ids.includes(x.id));persist();close();render();toast('선택한 합성 자료를 함께 복원했어요.');break}
case 'confirm-trash-delete':if($('#delete-confirm').value!==$('#delete-confirm').dataset.word){toast('확인 문구를 정확히 입력해 주세요.');break}db.trash=db.trash.filter(x=>!window.__trashIds.includes(x.id));persist();close();render();break;
case 'focus-switch':displayDraft.focus=!displayDraft.focus;el.setAttribute('aria-checked',String(displayDraft.focus));if(window.gsap&&!matchMedia('(prefers-reduced-motion: reduce)').matches)gsap.fromTo(el.querySelector('i'),{scaleX:1.22},{scaleX:1,duration:.5,ease:'elastic.out(1,.55)'});break;
case 'display-default':displayDraft=structuredClone(seed.display);theme(displayDraft.theme);render();break;
case 'display-cancel':displayDraft=structuredClone(db.display);theme(displayDraft.theme);render();toast('화면 설정 변경을 취소했어요.');break;
case 'display-save':displayDraft.size=Math.min(28,Math.max(14,displayDraft.size));displayDraft.line=Math.min(2.4,Math.max(1.2,displayDraft.line));displayDraft.space=Math.min(.2,Math.max(-.05,displayDraft.space));db.display=structuredClone(displayDraft);persist();toast('화면 및 작성 설정만 기기에 보관했어요.');break;
case 'photo-default':profileDraft.photo='';$('#draft-photo').style.backgroundImage='';$('#draft-photo').textContent=profileDraft.name[0]||'유';$('#profile-result').textContent='기본 사진으로 돌아갈 예정 · 아직 저장하지 않았어요.';break;
case 'name-default':profileDraft.name=db.profile.google;$('#profile-name').value=profileDraft.name;$('#profile-result').textContent='Google 기본 이름으로 돌아갈 예정 · 미저장';break;
case 'profile-cancel':profileDraft=structuredClone(db.profile);render();toast('프로필의 미저장 변경을 취소했어요.');break;
case 'profile-save':{const name=profileDraft.name;if(!name.trim()||[...name].length>120){$('#profile-result').textContent='닉네임은 1–120자로 입력해 주세요.';break}if(demoState==='profile-failure'){$('#profile-result').textContent='프로필 저장 실패 (시연). 입력과 선택 사진을 유지했습니다. 시연 도구에서 정상 상태로 바꾼 뒤 재시도하세요.';break}if(demoState==='profile-partial'){db.profile.name=name;persist();nav();$('#profile-result').textContent='이름만 반영 (시연). 사진 업로드 실패: 선택한 사진을 유지합니다. 사진만 재시도해 주세요.';demoState='normal';break}db.profile=structuredClone(profileDraft);persist();nav();render();$('#profile-result').textContent='이 브라우저에 프로필을 보관했어요. 화면 설정은 별도 저장입니다.';break}
case 'discard-profile':profileDraft=null;navigate(el.dataset.page,el.dataset.id,true);break;
case 'share':sharing();break;
case 'copy-code':await copy('LC-DEMO-YUNA');break;
case 'share-account':{const code=$('#share-code').value.trim();if(!code||code.includes('@')){toast('이메일이 아닌 상대 계정의 공유 코드를 입력해 주세요.');break}if(db.share.guest){toast('게스트 쓰기를 중지한 뒤 계정 공유를 추가해 주세요.');break}db.share.accounts.push({code,period:$('#share-period').value,write:false});persist();sharing();break}
case 'share-permission':db.share.accounts[i].write=!db.share.accounts[i].write;persist();sharing();break;
case 'share-revoke-account':db.share.accounts.splice(i,1);persist();sharing();break;
case 'public-create':modal('공개 범위 선택',`<p class="notice">제목과 본문은 기본 공개됩니다. 이전 링크를 교체하면 기존 링크는 무효화됩니다.</p><div class="stack" style="margin-top:18px"><label>기간<select id="public-period"><option>7일</option><option>30일</option><option>90일</option><option>회수까지</option></select></label><label class="check"><input id="public-status" type="checkbox" ${db.share.fields?.status?'checked':''}>작업 상태</label><label class="check"><input id="public-name" type="checkbox" ${db.share.fields?.name?'checked':''}>이름</label><label class="check"><input id="public-time" type="checkbox" ${db.share.fields?.time?'checked':''}>수정 시각</label></div><div class="modal-footer">${btn('취소','share')}${btn('범위 확인','public-confirm','','primary')}</div>`);break;
case 'public-confirm':{const fields={status:$('#public-status').checked,name:$('#public-name').checked,time:$('#public-time').checked},period=$('#public-period').value;window.__publicDraft={fields,period};modal('이 범위로 공개할까요?',`<div class="preview">기간: ${esc(period)}\n공개: 제목, 본문${fields.status?', 작업 상태':''}${fields.name?', 이름':''}${fields.time?', 수정 시각':''}\n비공개: 메모, 자료 연결, 수정 기록, 프로필 사진</div><div class="modal-footer">${btn('취소','share')}${btn('합성 링크 발행','public-publish','','primary')}</div>`);break}
case 'public-publish':Object.assign(db.share,{public:true,...window.__publicDraft});persist();window.__onceURL='https://example.invalid/lyrics/chroma-demo-'+Date.now().toString(36);modal('지금 한 번만 복사',`<p class="notice">외부에서 열리지 않는 합성 URL입니다. 이 창을 닫으면 URL 원문을 다시 표시하지 않습니다.</p><input id="once-url" readonly value="${window.__onceURL}"><div class="modal-footer">${btn('링크 복사','copy-once','','primary')}${btn('닫기','close')}</div>`);modalCleanup=()=>{window.__onceURL=null};break;
case 'copy-once':await copy(window.__onceURL||'');break;
case 'public-revoke':db.share.public=false;db.share.guest=false;persist();sharing();toast('합성 공개 링크를 회수했어요.');break;
case 'guest-toggle':if(db.share.guest){db.share.guest=false;persist();sharing();toast('게스트 쓰기를 중지했어요. 읽기는 유지되며 이전 입력은 자동 전송되지 않아요.')}else if(db.share.accounts.length)toast('선택 공유 계정을 회수한 뒤 게스트 쓰기를 허용할 수 있어요.');else modal('누구나 본문을 수정할 수 있어요',`<p class="notice warning">계정 없이 링크를 가진 누구나 가사 본문을 수정합니다. 제목·메모·자료·권한 변경은 허용되지 않아요.</p><div class="modal-footer">${btn('취소','share')}${btn('위험을 이해하고 허용','confirm-guest','','danger')}</div>`);break;
case 'confirm-guest':db.share.guest=true;persist();sharing();break;
case 'share-role':roleView(v);break;
case 'copy-shared':await copy($('#shared-body').value);break;
case 'save-shared':db.recovery=$('#shared-body').value;persist();toast('합성 미전송 복구함에 보관했어요. 서버로 전송하지 않았습니다.');break;
case 'recovery':recovery();break;
case 'copy-recovery':await copy(db.recovery||'');break;
case 'download-draft':if(!db.recovery){toast('보관된 미전송 초안이 없어요.');break}download(db.recovery);break;
case 'clear-recovery':modal('미전송 보관함에서 제거',`<p>이 기기의 합성 복구본만 제거합니다. 서버의 가사를 삭제하는 동작이 아닙니다.</p><div class="modal-footer">${btn('취소','recovery')}${btn('보관본 제거','confirm-clear-recovery','','danger')}</div>`);break;
case 'confirm-clear-recovery':db.recovery='';persist();recovery();break;
case 'account':account();break;
case 'export':modal('전체 내보내기',`<p class="notice">제품에서는 서버에 저장된 곡·가사·라임·프롬프트·설정을 ZIP으로 내려받습니다. 기기 미전송 초안은 별도 내보내기 대상입니다. 이 목업은 서버 ZIP을 만들지 않습니다.</p><div class="modal-footer">${btn('합성 데이터 JSON 내려받기','export-local')}${btn('기기 초안 열기','recovery')}</div>`);break;
case 'export-local':download(JSON.stringify(db,null,2),'chroma-dock-synthetic.json');break;
case 'withdraw':account('withdraw');break;
case 'reauth':window.__reauth=Date.now();toast('Google 재인증 완료 상태를 합성으로 선택했어요. 실제 인증은 하지 않았습니다.');break;
case 'withdraw-submit':if(!window.__reauth||Date.now()-window.__reauth>600000){toast('최근 10분 안의 재인증 시연을 먼저 선택해 주세요.');break}if(!$('#withdraw-check').checked||$('#withdraw-text').value!=='탈퇴'){toast('결과 이해 체크와 ‘탈퇴’ 입력이 필요해요.');break}modal('탈퇴 예약 · 합성 상태',`<p class="notice">자료 접근이 차단된 예약 상태를 시연합니다. 철회 기한: 2026.09.29 23:59 KST. 실제 계정과 자료는 변경되지 않습니다.</p><label style="margin-top:18px">‘철회’ 입력<input id="revoke-withdraw"></label><div class="modal-footer">${btn('철회하고 공간 복구','withdraw-revoke','','primary')}</div>`);break;
case 'withdraw-revoke':if($('#revoke-withdraw').value!=='철회'){toast('‘철회’를 입력해 주세요.');break}navigate('home');toast('합성 예약을 철회하고 작업 공간으로 돌아왔어요.');break;
case 'logout':modal('로그아웃 전 확인',`<p class="notice">미전송 입력이 ${db.recovery?'있어요':'없어요'}. 기기 초안을 내려받은 뒤 로그아웃할 수 있습니다.</p><div class="modal-footer">${btn('초안 내려받기','download-draft')}${btn('계속 작성','close')}${btn('미전송 보관본 지우고 로그아웃 시연','confirm-logout','','danger')}</div>`);break;
case 'confirm-logout':db.recovery='';persist();navigate('auth');break;
case 'signup':modal('초대 베타 가입',`<div class="stack"><label>초대 코드 · 6자<input id="invite-code" maxlength="6" autocomplete="off"></label><label>Google 계정 이메일<input id="invite-email" type="email"></label><p id="signup-result" role="alert"></p><div class="modal-footer">${btn('입력 지우고 취소','close')}${btn('가입 시작 시연','signup-submit','','primary')}</div></div>`);break;
case 'signup-submit':if($('#invite-code').value.length!==6||!$('#invite-email').value.includes('@')){$('#signup-result').textContent='6자 코드와 이메일 형식을 확인해 주세요.';break}$('#signup-result').textContent='초대 확인·Google 인증은 실제 시스템이 필요합니다. 이 합성 코드는 사용되지 않았습니다.';break;
case 'auth-login':modal('Google 로그인 · 합성 시연',`<p class="notice">실제 OAuth에 연결하지 않았어요. 합성 개인 공간으로 이동하거나 인증 오류 상태를 볼 수 있습니다.</p><div class="modal-footer">${btn('인증 오류 예시','auth-error')}${goBtn('합성 공간으로','home','','primary')}</div>`);break;
case 'auth-error':modal('로그인 상태 확인',`<label>오류 상태<select id="auth-error-select"><option>사용자가 취소함 · 코드 사용 안 됨</option><option>상태 만료 · 다시 시작</option><option>초대 계정 아님 · 코드 사용 안 됨</option><option>코드 거부 / 계정 불일치</option><option>일시 제한 · 잠시 후 다시 시도</option></select></label><p class="notice" style="margin-top:18px">오류 원인에 따라 가입을 다시 시작할 수 있습니다. 실제 초대 코드는 사용하지 않았어요.</p><div class="modal-footer">${btn('다시 시도','auth-login')}</div>`);break;
case 'auth-in-new-tab':modal('새 탭에서 다시 로그인',`<p>실제 제품에서는 기존 편집 탭을 보존하고 새 탭에서 Google 인증을 마칩니다. 이 목업은 합성 로그인 화면만 제공합니다.</p><div class="modal-footer">${btn('편집 내용 유지','close')}${btn('합성 로그인 보기','auth-login')}</div>`);break;
case 'legal':modal(k==='privacy'?'개인정보 안내':'이용 안내',`<div class="preview">${k==='privacy'?'합성 검토 화면입니다. 프로필 사진은 비공개이며 가사 공개 시 이름 표시는 별도로 선택합니다. 실제 개인정보 처리방침·보관·삭제·권리 행사는 제품의 정식 문서를 확인해야 합니다.':'초대 베타를 위한 가사·자료 작성 공간입니다. 본 시연의 모든 창작물과 계정은 합성이며 실제 서비스 약관을 대신하지 않습니다.'}</div>`);break;
case 'new-window':window.open(location.href.split('#')[0]+'#'+el.dataset.page+'/'+id,'_blank','noopener');break;
case 'reset':modal('합성 데이터 초기화',`<p>이 디자인의 브라우저 시연 데이터만 처음 상태로 되돌립니다.</p><div class="modal-footer">${btn('취소','close')}${btn('초기화','confirm-reset','','danger')}</div>`);break;
case 'confirm-reset':db=structuredClone(seed);persist();profileDraft=null;displayDraft=null;theme(db.display.theme);navigate('home',null,true);break;
}}
);
document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();navigate('search');$('#search-input')?.focus()}if(e.altKey&&e.key.toLowerCase()==='n'){e.preventDefault();document.querySelector('[data-action="quick"]').click()}});
window.addEventListener('beforeunload',e=>{if(profileDraft&&JSON.stringify(profileDraft)!==JSON.stringify(db.profile)){e.preventDefault();e.returnValue=''}});
window.addEventListener('hashchange',()=>{const [p,id]=location.hash.slice(1).split('/');if(p)navigate(p,id)});
matchMedia('(prefers-reduced-motion: reduce)').addEventListener('change',e=>{if(e.matches&&window.gsap){gsap.globalTimeline.getChildren().forEach(t=>t.progress(1).kill())}});
window.ChromaDock={navigate,render,copy,sunoText,get db(){return db},get lastCopy(){return lastCopy},get page(){return page},get resourceTab(){return resourceTab},reset(){db=structuredClone(seed);persist();profileDraft=null;displayDraft=null;theme(db.display.theme);navigate('home',null,true)}};
if(window.CHROMA_COLOR_INITIAL_THEME)db.display.theme=window.CHROMA_COLOR_INITIAL_THEME;theme(db.display.theme);const [initialPage,initialId]=location.hash.slice(1).split('/');if(initialPage){page=initialPage;current=initialId||current}render();
})();
