# Security Policy

LyricsCloud는 공개 소스 여부와 관계없이 가사·라임·프롬프트를 비공개 창작물로 취급합니다.

## 민감정보 취급

- 실제 창작물, 개인정보, 세션, OAuth 토큰, 암호화 키를 이 저장소·fixture·로그·스크린샷에 넣지 않습니다.
- `.env*`, PostgreSQL 데이터, 백업, 사용자 export 파일은 커밋하지 않습니다.
- 브라우저에는 공개가 허용된 설정만 전달하고 서버 비밀은 번들에 포함하지 않습니다.
- 오류·성능 정보에는 자료 ID와 상태만 남기고 제목·본문·태그는 제거합니다.

## 보안 변경 기준

- 인증·세션·소유권 변경은 서로 다른 두 계정의 교차 접근 차단 테스트가 필수입니다.
- DB 변경은 migration, 롤백 또는 복구 절차, 삭제 자료 노출 여부 검증을 포함합니다.
- CRDT 연결은 문서마다 인증·소유권을 다시 확인하며 문서 ID만 알아서는 접속할 수 없어야 합니다.
- 내보내기·탈퇴·완전 삭제·백업 복원은 서버 측 권한과 감사 가능한 결과를 확인합니다.
- 의존성 취약점은 영향을 평가한 뒤 현재 Phase 범위 안에서 수정하거나 릴리즈 차단 사항으로 기록합니다.

## 문제 보고

보안 문제에는 실제 사용자 내용을 넣지 말고 재현 가능한 최소 예와 영향 범위를 기록합니다. 공개 Issue에 토큰, 계정 ID, 운영 주소, DB 덤프를 첨부하지 않습니다. 공식 비공개 보고 채널은 `0.9.1` 운영 준비 단계에서 배포 환경과 함께 지정합니다.

